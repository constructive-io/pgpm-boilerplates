packages:
  - 'packages/*'

# 14 days, in minutes. A release younger than this is not installable; add a
# package to minimumReleaseAgeExclude to skip the wait for it.
minimumReleaseAge: 20160

# Dependencies allowed to run install scripts. Nothing is, until it is listed.
onlyBuiltDependencies: []
