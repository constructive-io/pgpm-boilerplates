/**
 * Single item query hook for AppLimitDefault
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type AppLimitDefault } from '../types';

// Re-export entity type for convenience
export type { AppLimitDefault };

// ============================================================================
// GraphQL Document
// ============================================================================
export const appLimitDefaultQueryDocument = `
query AppLimitDefaultQuery($id: UUID!) {
  appLimitDefault(id: $id) {
    id
    name
    max
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface AppLimitDefaultQueryVariables {
  id: string;
}

export interface AppLimitDefaultQueryResult {
  appLimitDefault: AppLimitDefault | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const appLimitDefaultQueryKey = (id: string) =>
  ['applimitdefault', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single AppLimitDefault by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppLimitDefaultQuery('value-here');
 *
 * if (data?.appLimitDefault) {
 *   console.log(data.appLimitDefault.id);
 * }
 * ```
 */
export function useAppLimitDefaultQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<AppLimitDefaultQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: appLimitDefaultQueryKey(id),
    queryFn: () =>
      execute<AppLimitDefaultQueryResult, AppLimitDefaultQueryVariables>(
        appLimitDefaultQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single AppLimitDefault by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchAppLimitDefaultQuery('value-here');
 * ```
 */
export async function fetchAppLimitDefaultQuery(
  id: string,
  options?: ExecuteOptions
): Promise<AppLimitDefaultQueryResult> {
  return execute<AppLimitDefaultQueryResult, AppLimitDefaultQueryVariables>(
    appLimitDefaultQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single AppLimitDefault for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchAppLimitDefaultQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchAppLimitDefaultQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: appLimitDefaultQueryKey(id),
    queryFn: () =>
      execute<AppLimitDefaultQueryResult, AppLimitDefaultQueryVariables>(
        appLimitDefaultQueryDocument,
        { id },
        options
      ),
  });
}
