/**
 * Single item query hook for SiteModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type SiteModule } from '../types';

// Re-export entity type for convenience
export type { SiteModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const siteModuleQueryDocument = `
query SiteModuleQuery($id: UUID!) {
  siteModule(id: $id) {
    id
    databaseId
    siteId
    name
    data
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface SiteModuleQueryVariables {
  id: string;
}

export interface SiteModuleQueryResult {
  siteModule: SiteModule | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const siteModuleQueryKey = (id: string) =>
  ['sitemodule', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single SiteModule by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useSiteModuleQuery('value-here');
 *
 * if (data?.siteModule) {
 *   console.log(data.siteModule.id);
 * }
 * ```
 */
export function useSiteModuleQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<SiteModuleQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: siteModuleQueryKey(id),
    queryFn: () =>
      execute<SiteModuleQueryResult, SiteModuleQueryVariables>(
        siteModuleQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single SiteModule by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchSiteModuleQuery('value-here');
 * ```
 */
export async function fetchSiteModuleQuery(
  id: string,
  options?: ExecuteOptions
): Promise<SiteModuleQueryResult> {
  return execute<SiteModuleQueryResult, SiteModuleQueryVariables>(
    siteModuleQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single SiteModule for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchSiteModuleQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchSiteModuleQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: siteModuleQueryKey(id),
    queryFn: () =>
      execute<SiteModuleQueryResult, SiteModuleQueryVariables>(
        siteModuleQueryDocument,
        { id },
        options
      ),
  });
}
