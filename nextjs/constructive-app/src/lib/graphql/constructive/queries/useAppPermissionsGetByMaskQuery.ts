/**
 * Custom query hook for appPermissionsGetByMask
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type AppPermissionFilter,
  type AppPermissionsConnection,
} from '../schema-types';

/** GraphQL query document */
export const appPermissionsGetByMaskQueryDocument = `
query AppPermissionsGetByMaskQuery($mask: BitString, $first: Int, $last: Int, $offset: Int, $before: Cursor, $after: Cursor, $filter: AppPermissionFilter) {
  appPermissionsGetByMask(
    mask: $mask
    first: $first
    last: $last
    offset: $offset
    before: $before
    after: $after
    filter: $filter
  ) {
    totalCount
    nodes {
      id
      name
      bitnum
      bitstr
      description
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

export interface AppPermissionsGetByMaskQueryVariables {
  mask?: string;
  /** Only read the first `n` values of the set. */
  first?: number;
  /** Only read the last `n` values of the set. */
  last?: number;
  /**
   * Skip the first `n` values from our `after` cursor, an alternative to cursor
   * based pagination. May not be used with `last`.
   */
  offset?: number;
  /** Read all values in the set before (above) this cursor. */
  before?: string;
  /** Read all values in the set after (below) this cursor. */
  after?: string;
  /** A filter to be used in determining which values should be returned by the collection. */
  filter?: AppPermissionFilter;
}

export interface AppPermissionsGetByMaskQueryResult {
  appPermissionsGetByMask: AppPermissionsConnection;
}

/** Query key factory for caching */
export const appPermissionsGetByMaskQueryKey = (
  variables?: AppPermissionsGetByMaskQueryVariables
) => ['appPermissionsGetByMask', variables] as const;

/**
 * Reads and enables pagination through a set of `AppPermission`.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppPermissionsGetByMaskQuery({ mask, first, last, offset, before, after, filter });
 *
 * if (data?.appPermissionsGetByMask) {
 *   console.log(data.appPermissionsGetByMask);
 * }
 * ```
 */
export function useAppPermissionsGetByMaskQuery(
  variables?: AppPermissionsGetByMaskQueryVariables,
  options?: Omit<
    UseQueryOptions<AppPermissionsGetByMaskQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: appPermissionsGetByMaskQueryKey(variables),
    queryFn: () =>
      execute<
        AppPermissionsGetByMaskQueryResult,
        AppPermissionsGetByMaskQueryVariables
      >(appPermissionsGetByMaskQueryDocument, variables),

    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch appPermissionsGetByMask without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchAppPermissionsGetByMaskQuery({ mask, first, last, offset, before, after, filter });
 * ```
 */
export async function fetchAppPermissionsGetByMaskQuery(
  variables?: AppPermissionsGetByMaskQueryVariables,
  options?: ExecuteOptions
): Promise<AppPermissionsGetByMaskQueryResult> {
  return execute<
    AppPermissionsGetByMaskQueryResult,
    AppPermissionsGetByMaskQueryVariables
  >(appPermissionsGetByMaskQueryDocument, variables, options);
}

/**
 * Prefetch appPermissionsGetByMask for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchAppPermissionsGetByMaskQuery(queryClient, { mask, first, last, offset, before, after, filter });
 * ```
 */
export async function prefetchAppPermissionsGetByMaskQuery(
  queryClient: QueryClient,
  variables?: AppPermissionsGetByMaskQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: appPermissionsGetByMaskQueryKey(variables),
    queryFn: () =>
      execute<
        AppPermissionsGetByMaskQueryResult,
        AppPermissionsGetByMaskQueryVariables
      >(appPermissionsGetByMaskQueryDocument, variables, options),
  });
}
