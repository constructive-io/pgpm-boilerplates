/**
 * Single item query hook for AppLevel
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type AppLevel } from '../types';

// Re-export entity type for convenience
export type { AppLevel };

// ============================================================================
// GraphQL Document
// ============================================================================
export const appLevelQueryDocument = `
query AppLevelQuery($id: UUID!) {
  appLevel(id: $id) {
    id
    name
    description
    image
    ownerId
    createdAt
    updatedAt
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface AppLevelQueryVariables {
  id: string;
}

export interface AppLevelQueryResult {
  appLevel: AppLevel | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const appLevelQueryKey = (id: string) =>
  ['applevel', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single AppLevel by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppLevelQuery('value-here');
 *
 * if (data?.appLevel) {
 *   console.log(data.appLevel.id);
 * }
 * ```
 */
export function useAppLevelQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<AppLevelQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: appLevelQueryKey(id),
    queryFn: () =>
      execute<AppLevelQueryResult, AppLevelQueryVariables>(
        appLevelQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single AppLevel by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchAppLevelQuery('value-here');
 * ```
 */
export async function fetchAppLevelQuery(
  id: string,
  options?: ExecuteOptions
): Promise<AppLevelQueryResult> {
  return execute<AppLevelQueryResult, AppLevelQueryVariables>(
    appLevelQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single AppLevel for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchAppLevelQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchAppLevelQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: appLevelQueryKey(id),
    queryFn: () =>
      execute<AppLevelQueryResult, AppLevelQueryVariables>(
        appLevelQueryDocument,
        { id },
        options
      ),
  });
}
