/**
 * Single item query hook for RlsModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type RlsModule } from '../types';

// Re-export entity type for convenience
export type { RlsModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const rlsModuleQueryDocument = `
query RlsModuleQuery($id: UUID!) {
  rlsModule(id: $id) {
    id
    databaseId
    apiId
    schemaId
    privateSchemaId
    tokensTableId
    usersTableId
    authenticate
    authenticateStrict
    currentRole
    currentRoleId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface RlsModuleQueryVariables {
  id: string;
}

export interface RlsModuleQueryResult {
  rlsModule: RlsModule | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const rlsModuleQueryKey = (id: string) =>
  ['rlsmodule', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single RlsModule by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useRlsModuleQuery('value-here');
 *
 * if (data?.rlsModule) {
 *   console.log(data.rlsModule.id);
 * }
 * ```
 */
export function useRlsModuleQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<RlsModuleQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: rlsModuleQueryKey(id),
    queryFn: () =>
      execute<RlsModuleQueryResult, RlsModuleQueryVariables>(
        rlsModuleQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single RlsModule by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchRlsModuleQuery('value-here');
 * ```
 */
export async function fetchRlsModuleQuery(
  id: string,
  options?: ExecuteOptions
): Promise<RlsModuleQueryResult> {
  return execute<RlsModuleQueryResult, RlsModuleQueryVariables>(
    rlsModuleQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single RlsModule for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchRlsModuleQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchRlsModuleQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: rlsModuleQueryKey(id),
    queryFn: () =>
      execute<RlsModuleQueryResult, RlsModuleQueryVariables>(
        rlsModuleQueryDocument,
        { id },
        options
      ),
  });
}
