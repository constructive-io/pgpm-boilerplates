/**
 * Single item query hook for FieldModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type FieldModule } from '../types';

// Re-export entity type for convenience
export type { FieldModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const fieldModuleQueryDocument = `
query FieldModuleQuery($id: UUID!) {
  fieldModule(id: $id) {
    id
    databaseId
    privateSchemaId
    tableId
    fieldId
    data
    triggers
    functions
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface FieldModuleQueryVariables {
  id: string;
}

export interface FieldModuleQueryResult {
  fieldModule: FieldModule | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const fieldModuleQueryKey = (id: string) =>
  ['fieldmodule', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single FieldModule by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useFieldModuleQuery('value-here');
 *
 * if (data?.fieldModule) {
 *   console.log(data.fieldModule.id);
 * }
 * ```
 */
export function useFieldModuleQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<FieldModuleQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: fieldModuleQueryKey(id),
    queryFn: () =>
      execute<FieldModuleQueryResult, FieldModuleQueryVariables>(
        fieldModuleQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single FieldModule by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchFieldModuleQuery('value-here');
 * ```
 */
export async function fetchFieldModuleQuery(
  id: string,
  options?: ExecuteOptions
): Promise<FieldModuleQueryResult> {
  return execute<FieldModuleQueryResult, FieldModuleQueryVariables>(
    fieldModuleQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single FieldModule for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchFieldModuleQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchFieldModuleQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: fieldModuleQueryKey(id),
    queryFn: () =>
      execute<FieldModuleQueryResult, FieldModuleQueryVariables>(
        fieldModuleQueryDocument,
        { id },
        options
      ),
  });
}
