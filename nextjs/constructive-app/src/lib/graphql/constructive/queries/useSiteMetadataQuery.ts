/**
 * List query hook for SiteMetadatum
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type SiteMetadatum,
  type UUIDFilter,
  type StringFilter,
  type JSONFilter,
} from '../types';

// Re-export entity type for convenience
export type { SiteMetadatum };

// ============================================================================
// GraphQL Document
// ============================================================================
export const siteMetadataQueryDocument = `
query SiteMetadataQuery($first: Int, $offset: Int, $filter: SiteMetadatumFilter, $orderBy: [SiteMetadataOrderBy!]) {
  siteMetadata(first: $first, offset: $offset, filter: $filter, orderBy: $orderBy) {
    totalCount
    nodes {
      id
      databaseId
      siteId
      title
      description
      ogImage
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface SiteMetadatumFilter {
  id?: UUIDFilter;
  databaseId?: UUIDFilter;
  siteId?: UUIDFilter;
  title?: StringFilter;
  description?: StringFilter;
  ogImage?: JSONFilter;
  /** Logical AND */
  and?: SiteMetadatumFilter[];
  /** Logical OR */
  or?: SiteMetadatumFilter[];
  /** Logical NOT */
  not?: SiteMetadatumFilter;
}

type SiteMetadataOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'DATABASE_ID_ASC'
  | 'DATABASE_ID_DESC'
  | 'SITE_ID_ASC'
  | 'SITE_ID_DESC'
  | 'TITLE_ASC'
  | 'TITLE_DESC'
  | 'DESCRIPTION_ASC'
  | 'DESCRIPTION_DESC'
  | 'OG_IMAGE_ASC'
  | 'OG_IMAGE_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface SiteMetadataQueryVariables {
  first?: number;
  offset?: number;
  filter?: SiteMetadatumFilter;
  orderBy?: SiteMetadataOrderBy[];
}

export interface SiteMetadataQueryResult {
  siteMetadata: {
    totalCount: number;
    nodes: SiteMetadatum[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const siteMetadataQueryKey = (variables?: SiteMetadataQueryVariables) =>
  ['sitemetadatum', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching SiteMetadatum list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useSiteMetadataQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useSiteMetadataQuery(
  variables?: SiteMetadataQueryVariables,
  options?: Omit<
    UseQueryOptions<SiteMetadataQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: siteMetadataQueryKey(variables),
    queryFn: () =>
      execute<SiteMetadataQueryResult, SiteMetadataQueryVariables>(
        siteMetadataQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch SiteMetadatum list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchSiteMetadataQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: siteMetadataQueryKey(variables),
 *   queryFn: () => fetchSiteMetadataQuery(variables),
 * });
 * ```
 */
export async function fetchSiteMetadataQuery(
  variables?: SiteMetadataQueryVariables,
  options?: ExecuteOptions
): Promise<SiteMetadataQueryResult> {
  return execute<SiteMetadataQueryResult, SiteMetadataQueryVariables>(
    siteMetadataQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch SiteMetadatum list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchSiteMetadataQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchSiteMetadataQuery(
  queryClient: QueryClient,
  variables?: SiteMetadataQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: siteMetadataQueryKey(variables),
    queryFn: () =>
      execute<SiteMetadataQueryResult, SiteMetadataQueryVariables>(
        siteMetadataQueryDocument,
        variables,
        options
      ),
  });
}
