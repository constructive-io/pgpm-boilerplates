/**
 * List query hook for CheckConstraint
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type CheckConstraint,
  type UUIDFilter,
  type StringFilter,
  type UUIDListFilter,
  type JSONFilter,
  type IntFilter,
  type StringListFilter,
  type DatetimeFilter,
} from '../types';

// Re-export entity type for convenience
export type { CheckConstraint };

// ============================================================================
// GraphQL Document
// ============================================================================
export const checkConstraintsQueryDocument = `
query CheckConstraintsQuery($first: Int, $offset: Int, $filter: CheckConstraintFilter, $orderBy: [CheckConstraintsOrderBy!]) {
  checkConstraints(
    first: $first
    offset: $offset
    filter: $filter
    orderBy: $orderBy
  ) {
    totalCount
    nodes {
      id
      databaseId
      tableId
      name
      type
      fieldIds
      expr
      smartTags
      category
      module
      scope
      tags
      createdAt
      updatedAt
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface CheckConstraintFilter {
  id?: UUIDFilter;
  databaseId?: UUIDFilter;
  tableId?: UUIDFilter;
  name?: StringFilter;
  type?: StringFilter;
  fieldIds?: UUIDListFilter;
  expr?: JSONFilter;
  smartTags?: JSONFilter;
  module?: StringFilter;
  scope?: IntFilter;
  tags?: StringListFilter;
  createdAt?: DatetimeFilter;
  updatedAt?: DatetimeFilter;
  /** Logical AND */
  and?: CheckConstraintFilter[];
  /** Logical OR */
  or?: CheckConstraintFilter[];
  /** Logical NOT */
  not?: CheckConstraintFilter;
}

type CheckConstraintsOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'DATABASE_ID_ASC'
  | 'DATABASE_ID_DESC'
  | 'TABLE_ID_ASC'
  | 'TABLE_ID_DESC'
  | 'NAME_ASC'
  | 'NAME_DESC'
  | 'TYPE_ASC'
  | 'TYPE_DESC'
  | 'FIELD_IDS_ASC'
  | 'FIELD_IDS_DESC'
  | 'EXPR_ASC'
  | 'EXPR_DESC'
  | 'SMART_TAGS_ASC'
  | 'SMART_TAGS_DESC'
  | 'CATEGORY_ASC'
  | 'CATEGORY_DESC'
  | 'MODULE_ASC'
  | 'MODULE_DESC'
  | 'SCOPE_ASC'
  | 'SCOPE_DESC'
  | 'TAGS_ASC'
  | 'TAGS_DESC'
  | 'CREATED_AT_ASC'
  | 'CREATED_AT_DESC'
  | 'UPDATED_AT_ASC'
  | 'UPDATED_AT_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface CheckConstraintsQueryVariables {
  first?: number;
  offset?: number;
  filter?: CheckConstraintFilter;
  orderBy?: CheckConstraintsOrderBy[];
}

export interface CheckConstraintsQueryResult {
  checkConstraints: {
    totalCount: number;
    nodes: CheckConstraint[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const checkConstraintsQueryKey = (
  variables?: CheckConstraintsQueryVariables
) => ['checkconstraint', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching CheckConstraint list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useCheckConstraintsQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useCheckConstraintsQuery(
  variables?: CheckConstraintsQueryVariables,
  options?: Omit<
    UseQueryOptions<CheckConstraintsQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: checkConstraintsQueryKey(variables),
    queryFn: () =>
      execute<CheckConstraintsQueryResult, CheckConstraintsQueryVariables>(
        checkConstraintsQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch CheckConstraint list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchCheckConstraintsQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: checkConstraintsQueryKey(variables),
 *   queryFn: () => fetchCheckConstraintsQuery(variables),
 * });
 * ```
 */
export async function fetchCheckConstraintsQuery(
  variables?: CheckConstraintsQueryVariables,
  options?: ExecuteOptions
): Promise<CheckConstraintsQueryResult> {
  return execute<CheckConstraintsQueryResult, CheckConstraintsQueryVariables>(
    checkConstraintsQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch CheckConstraint list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchCheckConstraintsQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchCheckConstraintsQuery(
  queryClient: QueryClient,
  variables?: CheckConstraintsQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: checkConstraintsQueryKey(variables),
    queryFn: () =>
      execute<CheckConstraintsQueryResult, CheckConstraintsQueryVariables>(
        checkConstraintsQueryDocument,
        variables,
        options
      ),
  });
}
