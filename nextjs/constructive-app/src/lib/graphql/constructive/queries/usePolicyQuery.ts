/**
 * Single item query hook for Policy
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type Policy } from '../types';

// Re-export entity type for convenience
export type { Policy };

// ============================================================================
// GraphQL Document
// ============================================================================
export const policyQueryDocument = `
query PolicyQuery($id: UUID!) {
  policy(id: $id) {
    id
    databaseId
    tableId
    name
    roleName
    privilege
    permissive
    disabled
    template
    data
    smartTags
    category
    module
    scope
    tags
    createdAt
    updatedAt
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface PolicyQueryVariables {
  id: string;
}

export interface PolicyQueryResult {
  policy: Policy | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const policyQueryKey = (id: string) => ['policy', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single Policy by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePolicyQuery('value-here');
 *
 * if (data?.policy) {
 *   console.log(data.policy.id);
 * }
 * ```
 */
export function usePolicyQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<PolicyQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: policyQueryKey(id),
    queryFn: () =>
      execute<PolicyQueryResult, PolicyQueryVariables>(policyQueryDocument, {
        id,
      }),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single Policy by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchPolicyQuery('value-here');
 * ```
 */
export async function fetchPolicyQuery(
  id: string,
  options?: ExecuteOptions
): Promise<PolicyQueryResult> {
  return execute<PolicyQueryResult, PolicyQueryVariables>(
    policyQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single Policy for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchPolicyQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchPolicyQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: policyQueryKey(id),
    queryFn: () =>
      execute<PolicyQueryResult, PolicyQueryVariables>(
        policyQueryDocument,
        { id },
        options
      ),
  });
}
