/**
 * List query hook for Store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type Store,
  type UUIDFilter,
  type StringFilter,
  type DatetimeFilter,
} from '../types';

// Re-export entity type for convenience
export type { Store };

// ============================================================================
// GraphQL Document
// ============================================================================
export const storesQueryDocument = `
query StoresQuery($first: Int, $offset: Int, $filter: StoreFilter, $orderBy: [StoresOrderBy!]) {
  stores(first: $first, offset: $offset, filter: $filter, orderBy: $orderBy) {
    totalCount
    nodes {
      id
      name
      databaseId
      hash
      createdAt
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface StoreFilter {
  id?: UUIDFilter;
  name?: StringFilter;
  databaseId?: UUIDFilter;
  hash?: UUIDFilter;
  createdAt?: DatetimeFilter;
  /** Logical AND */
  and?: StoreFilter[];
  /** Logical OR */
  or?: StoreFilter[];
  /** Logical NOT */
  not?: StoreFilter;
}

type StoresOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'NAME_ASC'
  | 'NAME_DESC'
  | 'DATABASE_ID_ASC'
  | 'DATABASE_ID_DESC'
  | 'HASH_ASC'
  | 'HASH_DESC'
  | 'CREATED_AT_ASC'
  | 'CREATED_AT_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface StoresQueryVariables {
  first?: number;
  offset?: number;
  filter?: StoreFilter;
  orderBy?: StoresOrderBy[];
}

export interface StoresQueryResult {
  stores: {
    totalCount: number;
    nodes: Store[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const storesQueryKey = (variables?: StoresQueryVariables) =>
  ['store', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching Store list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useStoresQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useStoresQuery(
  variables?: StoresQueryVariables,
  options?: Omit<
    UseQueryOptions<StoresQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: storesQueryKey(variables),
    queryFn: () =>
      execute<StoresQueryResult, StoresQueryVariables>(
        storesQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch Store list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchStoresQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: storesQueryKey(variables),
 *   queryFn: () => fetchStoresQuery(variables),
 * });
 * ```
 */
export async function fetchStoresQuery(
  variables?: StoresQueryVariables,
  options?: ExecuteOptions
): Promise<StoresQueryResult> {
  return execute<StoresQueryResult, StoresQueryVariables>(
    storesQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch Store list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchStoresQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchStoresQuery(
  queryClient: QueryClient,
  variables?: StoresQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: storesQueryKey(variables),
    queryFn: () =>
      execute<StoresQueryResult, StoresQueryVariables>(
        storesQueryDocument,
        variables,
        options
      ),
  });
}
