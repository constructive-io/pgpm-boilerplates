/**
 * Single item query hook for Procedure
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type Procedure } from '../types';

// Re-export entity type for convenience
export type { Procedure };

// ============================================================================
// GraphQL Document
// ============================================================================
export const procedureQueryDocument = `
query ProcedureQuery($id: UUID!) {
  procedure(id: $id) {
    id
    databaseId
    name
    argnames
    argtypes
    argdefaults
    langName
    definition
    smartTags
    category
    module
    scope
    tags
    createdAt
    updatedAt
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface ProcedureQueryVariables {
  id: string;
}

export interface ProcedureQueryResult {
  procedure: Procedure | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const procedureQueryKey = (id: string) =>
  ['procedure', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single Procedure by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useProcedureQuery('value-here');
 *
 * if (data?.procedure) {
 *   console.log(data.procedure.id);
 * }
 * ```
 */
export function useProcedureQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<ProcedureQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: procedureQueryKey(id),
    queryFn: () =>
      execute<ProcedureQueryResult, ProcedureQueryVariables>(
        procedureQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single Procedure by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchProcedureQuery('value-here');
 * ```
 */
export async function fetchProcedureQuery(
  id: string,
  options?: ExecuteOptions
): Promise<ProcedureQueryResult> {
  return execute<ProcedureQueryResult, ProcedureQueryVariables>(
    procedureQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single Procedure for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchProcedureQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchProcedureQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: procedureQueryKey(id),
    queryFn: () =>
      execute<ProcedureQueryResult, ProcedureQueryVariables>(
        procedureQueryDocument,
        { id },
        options
      ),
  });
}
