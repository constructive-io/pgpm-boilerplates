/**
 * Single item query hook for UserSettingsModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type UserSettingsModule } from '../types';

// Re-export entity type for convenience
export type { UserSettingsModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const userSettingsModuleQueryDocument = `
query UserSettingsModuleQuery($id: UUID!) {
  userSettingsModule(id: $id) {
    id
    databaseId
    schemaId
    privateSchemaId
    tableId
    ownerTableId
    tableName
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface UserSettingsModuleQueryVariables {
  id: string;
}

export interface UserSettingsModuleQueryResult {
  userSettingsModule: UserSettingsModule | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const userSettingsModuleQueryKey = (id: string) =>
  ['usersettingsmodule', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single UserSettingsModule by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useUserSettingsModuleQuery('value-here');
 *
 * if (data?.userSettingsModule) {
 *   console.log(data.userSettingsModule.id);
 * }
 * ```
 */
export function useUserSettingsModuleQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<UserSettingsModuleQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: userSettingsModuleQueryKey(id),
    queryFn: () =>
      execute<UserSettingsModuleQueryResult, UserSettingsModuleQueryVariables>(
        userSettingsModuleQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single UserSettingsModule by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchUserSettingsModuleQuery('value-here');
 * ```
 */
export async function fetchUserSettingsModuleQuery(
  id: string,
  options?: ExecuteOptions
): Promise<UserSettingsModuleQueryResult> {
  return execute<
    UserSettingsModuleQueryResult,
    UserSettingsModuleQueryVariables
  >(userSettingsModuleQueryDocument, { id }, options);
}

/**
 * Prefetch a single UserSettingsModule for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchUserSettingsModuleQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchUserSettingsModuleQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: userSettingsModuleQueryKey(id),
    queryFn: () =>
      execute<UserSettingsModuleQueryResult, UserSettingsModuleQueryVariables>(
        userSettingsModuleQueryDocument,
        { id },
        options
      ),
  });
}
