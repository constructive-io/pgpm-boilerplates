/**
 * List query hook for OrgGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type OrgGrant,
  type UUIDFilter,
  type BitStringFilter,
  type BooleanFilter,
  type DatetimeFilter,
} from '../types';

// Re-export entity type for convenience
export type { OrgGrant };

// ============================================================================
// GraphQL Document
// ============================================================================
export const orgGrantsQueryDocument = `
query OrgGrantsQuery($first: Int, $offset: Int, $filter: OrgGrantFilter, $orderBy: [OrgGrantsOrderBy!]) {
  orgGrants(first: $first, offset: $offset, filter: $filter, orderBy: $orderBy) {
    totalCount
    nodes {
      id
      permissions
      isGrant
      actorId
      entityId
      grantorId
      createdAt
      updatedAt
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface OrgGrantFilter {
  id?: UUIDFilter;
  permissions?: BitStringFilter;
  isGrant?: BooleanFilter;
  actorId?: UUIDFilter;
  entityId?: UUIDFilter;
  grantorId?: UUIDFilter;
  createdAt?: DatetimeFilter;
  updatedAt?: DatetimeFilter;
  /** Logical AND */
  and?: OrgGrantFilter[];
  /** Logical OR */
  or?: OrgGrantFilter[];
  /** Logical NOT */
  not?: OrgGrantFilter;
}

type OrgGrantsOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'PERMISSIONS_ASC'
  | 'PERMISSIONS_DESC'
  | 'IS_GRANT_ASC'
  | 'IS_GRANT_DESC'
  | 'ACTOR_ID_ASC'
  | 'ACTOR_ID_DESC'
  | 'ENTITY_ID_ASC'
  | 'ENTITY_ID_DESC'
  | 'GRANTOR_ID_ASC'
  | 'GRANTOR_ID_DESC'
  | 'CREATED_AT_ASC'
  | 'CREATED_AT_DESC'
  | 'UPDATED_AT_ASC'
  | 'UPDATED_AT_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface OrgGrantsQueryVariables {
  first?: number;
  offset?: number;
  filter?: OrgGrantFilter;
  orderBy?: OrgGrantsOrderBy[];
}

export interface OrgGrantsQueryResult {
  orgGrants: {
    totalCount: number;
    nodes: OrgGrant[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const orgGrantsQueryKey = (variables?: OrgGrantsQueryVariables) =>
  ['orggrant', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching OrgGrant list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgGrantsQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useOrgGrantsQuery(
  variables?: OrgGrantsQueryVariables,
  options?: Omit<
    UseQueryOptions<OrgGrantsQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: orgGrantsQueryKey(variables),
    queryFn: () =>
      execute<OrgGrantsQueryResult, OrgGrantsQueryVariables>(
        orgGrantsQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch OrgGrant list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchOrgGrantsQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: orgGrantsQueryKey(variables),
 *   queryFn: () => fetchOrgGrantsQuery(variables),
 * });
 * ```
 */
export async function fetchOrgGrantsQuery(
  variables?: OrgGrantsQueryVariables,
  options?: ExecuteOptions
): Promise<OrgGrantsQueryResult> {
  return execute<OrgGrantsQueryResult, OrgGrantsQueryVariables>(
    orgGrantsQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch OrgGrant list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchOrgGrantsQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchOrgGrantsQuery(
  queryClient: QueryClient,
  variables?: OrgGrantsQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: orgGrantsQueryKey(variables),
    queryFn: () =>
      execute<OrgGrantsQueryResult, OrgGrantsQueryVariables>(
        orgGrantsQueryDocument,
        variables,
        options
      ),
  });
}
