/**
 * Single item query hook for AppAchievement
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type AppAchievement } from '../types';

// Re-export entity type for convenience
export type { AppAchievement };

// ============================================================================
// GraphQL Document
// ============================================================================
export const appAchievementQueryDocument = `
query AppAchievementQuery($id: UUID!) {
  appAchievement(id: $id) {
    id
    actorId
    name
    count
    createdAt
    updatedAt
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface AppAchievementQueryVariables {
  id: string;
}

export interface AppAchievementQueryResult {
  appAchievement: AppAchievement | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const appAchievementQueryKey = (id: string) =>
  ['appachievement', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single AppAchievement by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppAchievementQuery('value-here');
 *
 * if (data?.appAchievement) {
 *   console.log(data.appAchievement.id);
 * }
 * ```
 */
export function useAppAchievementQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<AppAchievementQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: appAchievementQueryKey(id),
    queryFn: () =>
      execute<AppAchievementQueryResult, AppAchievementQueryVariables>(
        appAchievementQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single AppAchievement by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchAppAchievementQuery('value-here');
 * ```
 */
export async function fetchAppAchievementQuery(
  id: string,
  options?: ExecuteOptions
): Promise<AppAchievementQueryResult> {
  return execute<AppAchievementQueryResult, AppAchievementQueryVariables>(
    appAchievementQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single AppAchievement for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchAppAchievementQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchAppAchievementQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: appAchievementQueryKey(id),
    queryFn: () =>
      execute<AppAchievementQueryResult, AppAchievementQueryVariables>(
        appAchievementQueryDocument,
        { id },
        options
      ),
  });
}
