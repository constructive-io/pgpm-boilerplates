/**
 * Single item query hook for UserProfile
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type UserProfile } from '../types';

// Re-export entity type for convenience
export type { UserProfile };

// ============================================================================
// GraphQL Document
// ============================================================================
export const userProfileQueryDocument = `
query UserProfileQuery($id: UUID!) {
  userProfile(id: $id) {
    id
    ownerId
    profilePicture
    bio
    firstName
    lastName
    tags
    desired
    createdAt
    updatedAt
    createdBy
    updatedBy
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface UserProfileQueryVariables {
  id: string;
}

export interface UserProfileQueryResult {
  userProfile: UserProfile | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const userProfileQueryKey = (id: string) =>
  ['userprofile', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single UserProfile by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useUserProfileQuery('value-here');
 *
 * if (data?.userProfile) {
 *   console.log(data.userProfile.id);
 * }
 * ```
 */
export function useUserProfileQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<UserProfileQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: userProfileQueryKey(id),
    queryFn: () =>
      execute<UserProfileQueryResult, UserProfileQueryVariables>(
        userProfileQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single UserProfile by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchUserProfileQuery('value-here');
 * ```
 */
export async function fetchUserProfileQuery(
  id: string,
  options?: ExecuteOptions
): Promise<UserProfileQueryResult> {
  return execute<UserProfileQueryResult, UserProfileQueryVariables>(
    userProfileQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single UserProfile for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchUserProfileQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchUserProfileQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: userProfileQueryKey(id),
    queryFn: () =>
      execute<UserProfileQueryResult, UserProfileQueryVariables>(
        userProfileQueryDocument,
        { id },
        options
      ),
  });
}
