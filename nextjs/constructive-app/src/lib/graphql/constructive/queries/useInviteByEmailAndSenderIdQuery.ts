/**
 * Custom query hook for inviteByEmailAndSenderId
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type Invite } from '../types';

/** GraphQL query document */
export const inviteByEmailAndSenderIdQueryDocument = `
query InviteByEmailAndSenderIdQuery($email: String!, $senderId: UUID!) {
  inviteByEmailAndSenderId(email: $email, senderId: $senderId) {
    id
    email
    senderId
    inviteToken
    inviteValid
    inviteLimit
    inviteCount
    multiple
    data
    expiresAt
    createdAt
    updatedAt
    sender {
      id
      username
      displayName
      profilePicture
      searchTsv
      type
      createdAt
      updatedAt
      searchTsvRank
    }
  }
}
`;

export interface InviteByEmailAndSenderIdQueryVariables {
  email: string;
  senderId: string;
}

export interface InviteByEmailAndSenderIdQueryResult {
  inviteByEmailAndSenderId: Invite;
}

/** Query key factory for caching */
export const inviteByEmailAndSenderIdQueryKey = (
  variables?: InviteByEmailAndSenderIdQueryVariables
) => ['inviteByEmailAndSenderId', variables] as const;

/**
 * Query hook for inviteByEmailAndSenderId
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useInviteByEmailAndSenderIdQuery({ email, senderId });
 *
 * if (data?.inviteByEmailAndSenderId) {
 *   console.log(data.inviteByEmailAndSenderId);
 * }
 * ```
 */
export function useInviteByEmailAndSenderIdQuery(
  variables: InviteByEmailAndSenderIdQueryVariables,
  options?: Omit<
    UseQueryOptions<InviteByEmailAndSenderIdQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: inviteByEmailAndSenderIdQueryKey(variables),
    queryFn: () =>
      execute<
        InviteByEmailAndSenderIdQueryResult,
        InviteByEmailAndSenderIdQueryVariables
      >(inviteByEmailAndSenderIdQueryDocument, variables),
    enabled: !!variables && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch inviteByEmailAndSenderId without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchInviteByEmailAndSenderIdQuery({ email, senderId });
 * ```
 */
export async function fetchInviteByEmailAndSenderIdQuery(
  variables: InviteByEmailAndSenderIdQueryVariables,
  options?: ExecuteOptions
): Promise<InviteByEmailAndSenderIdQueryResult> {
  return execute<
    InviteByEmailAndSenderIdQueryResult,
    InviteByEmailAndSenderIdQueryVariables
  >(inviteByEmailAndSenderIdQueryDocument, variables, options);
}

/**
 * Prefetch inviteByEmailAndSenderId for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchInviteByEmailAndSenderIdQuery(queryClient, { email, senderId });
 * ```
 */
export async function prefetchInviteByEmailAndSenderIdQuery(
  queryClient: QueryClient,
  variables: InviteByEmailAndSenderIdQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: inviteByEmailAndSenderIdQueryKey(variables),
    queryFn: () =>
      execute<
        InviteByEmailAndSenderIdQueryResult,
        InviteByEmailAndSenderIdQueryVariables
      >(inviteByEmailAndSenderIdQueryDocument, variables, options),
  });
}
