/**
 * Single item query hook for Index
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type Index } from '../types';

// Re-export entity type for convenience
export type { Index };

// ============================================================================
// GraphQL Document
// ============================================================================
export const indexQueryDocument = `
query IndexQuery($id: UUID!) {
  index(id: $id) {
    id
    databaseId
    tableId
    name
    fieldIds
    includeFieldIds
    accessMethod
    indexParams
    whereClause
    isUnique
    smartTags
    category
    module
    scope
    tags
    createdAt
    updatedAt
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface IndexQueryVariables {
  id: string;
}

export interface IndexQueryResult {
  index: Index | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const indexQueryKey = (id: string) => ['index', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single Index by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useIndexQuery('value-here');
 *
 * if (data?.index) {
 *   console.log(data.index.id);
 * }
 * ```
 */
export function useIndexQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<IndexQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: indexQueryKey(id),
    queryFn: () =>
      execute<IndexQueryResult, IndexQueryVariables>(indexQueryDocument, {
        id,
      }),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single Index by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchIndexQuery('value-here');
 * ```
 */
export async function fetchIndexQuery(
  id: string,
  options?: ExecuteOptions
): Promise<IndexQueryResult> {
  return execute<IndexQueryResult, IndexQueryVariables>(
    indexQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single Index for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchIndexQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchIndexQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: indexQueryKey(id),
    queryFn: () =>
      execute<IndexQueryResult, IndexQueryVariables>(
        indexQueryDocument,
        { id },
        options
      ),
  });
}
