/**
 * List query hook for OrgMember
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type OrgMember, type UUIDFilter, type BooleanFilter } from '../types';

// Re-export entity type for convenience
export type { OrgMember };

// ============================================================================
// GraphQL Document
// ============================================================================
export const orgMembersQueryDocument = `
query OrgMembersQuery($first: Int, $offset: Int, $filter: OrgMemberFilter, $orderBy: [OrgMembersOrderBy!]) {
  orgMembers(first: $first, offset: $offset, filter: $filter, orderBy: $orderBy) {
    totalCount
    nodes {
      id
      isAdmin
      actorId
      entityId
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface OrgMemberFilter {
  id?: UUIDFilter;
  isAdmin?: BooleanFilter;
  actorId?: UUIDFilter;
  entityId?: UUIDFilter;
  /** Logical AND */
  and?: OrgMemberFilter[];
  /** Logical OR */
  or?: OrgMemberFilter[];
  /** Logical NOT */
  not?: OrgMemberFilter;
}

type OrgMembersOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'IS_ADMIN_ASC'
  | 'IS_ADMIN_DESC'
  | 'ACTOR_ID_ASC'
  | 'ACTOR_ID_DESC'
  | 'ENTITY_ID_ASC'
  | 'ENTITY_ID_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface OrgMembersQueryVariables {
  first?: number;
  offset?: number;
  filter?: OrgMemberFilter;
  orderBy?: OrgMembersOrderBy[];
}

export interface OrgMembersQueryResult {
  orgMembers: {
    totalCount: number;
    nodes: OrgMember[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const orgMembersQueryKey = (variables?: OrgMembersQueryVariables) =>
  ['orgmember', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching OrgMember list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgMembersQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useOrgMembersQuery(
  variables?: OrgMembersQueryVariables,
  options?: Omit<
    UseQueryOptions<OrgMembersQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: orgMembersQueryKey(variables),
    queryFn: () =>
      execute<OrgMembersQueryResult, OrgMembersQueryVariables>(
        orgMembersQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch OrgMember list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchOrgMembersQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: orgMembersQueryKey(variables),
 *   queryFn: () => fetchOrgMembersQuery(variables),
 * });
 * ```
 */
export async function fetchOrgMembersQuery(
  variables?: OrgMembersQueryVariables,
  options?: ExecuteOptions
): Promise<OrgMembersQueryResult> {
  return execute<OrgMembersQueryResult, OrgMembersQueryVariables>(
    orgMembersQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch OrgMember list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchOrgMembersQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchOrgMembersQuery(
  queryClient: QueryClient,
  variables?: OrgMembersQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: orgMembersQueryKey(variables),
    queryFn: () =>
      execute<OrgMembersQueryResult, OrgMembersQueryVariables>(
        orgMembersQueryDocument,
        variables,
        options
      ),
  });
}
