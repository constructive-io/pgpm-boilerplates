/**
 * Single item query hook for UsersModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type UsersModule } from '../types';

// Re-export entity type for convenience
export type { UsersModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const usersModuleQueryDocument = `
query UsersModuleQuery($id: UUID!) {
  usersModule(id: $id) {
    id
    databaseId
    schemaId
    tableId
    tableName
    typeTableId
    typeTableName
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface UsersModuleQueryVariables {
  id: string;
}

export interface UsersModuleQueryResult {
  usersModule: UsersModule | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const usersModuleQueryKey = (id: string) =>
  ['usersmodule', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single UsersModule by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useUsersModuleQuery('value-here');
 *
 * if (data?.usersModule) {
 *   console.log(data.usersModule.id);
 * }
 * ```
 */
export function useUsersModuleQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<UsersModuleQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: usersModuleQueryKey(id),
    queryFn: () =>
      execute<UsersModuleQueryResult, UsersModuleQueryVariables>(
        usersModuleQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single UsersModule by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchUsersModuleQuery('value-here');
 * ```
 */
export async function fetchUsersModuleQuery(
  id: string,
  options?: ExecuteOptions
): Promise<UsersModuleQueryResult> {
  return execute<UsersModuleQueryResult, UsersModuleQueryVariables>(
    usersModuleQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single UsersModule for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchUsersModuleQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchUsersModuleQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: usersModuleQueryKey(id),
    queryFn: () =>
      execute<UsersModuleQueryResult, UsersModuleQueryVariables>(
        usersModuleQueryDocument,
        { id },
        options
      ),
  });
}
