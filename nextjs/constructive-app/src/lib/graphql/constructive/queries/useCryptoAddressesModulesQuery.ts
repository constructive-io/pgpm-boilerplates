/**
 * List query hook for CryptoAddressesModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type CryptoAddressesModule,
  type UUIDFilter,
  type StringFilter,
} from '../types';

// Re-export entity type for convenience
export type { CryptoAddressesModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const cryptoAddressesModulesQueryDocument = `
query CryptoAddressesModulesQuery($first: Int, $offset: Int, $filter: CryptoAddressesModuleFilter, $orderBy: [CryptoAddressesModulesOrderBy!]) {
  cryptoAddressesModules(
    first: $first
    offset: $offset
    filter: $filter
    orderBy: $orderBy
  ) {
    totalCount
    nodes {
      id
      databaseId
      schemaId
      privateSchemaId
      tableId
      ownerTableId
      tableName
      cryptoNetwork
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface CryptoAddressesModuleFilter {
  id?: UUIDFilter;
  databaseId?: UUIDFilter;
  schemaId?: UUIDFilter;
  privateSchemaId?: UUIDFilter;
  tableId?: UUIDFilter;
  ownerTableId?: UUIDFilter;
  tableName?: StringFilter;
  cryptoNetwork?: StringFilter;
  /** Logical AND */
  and?: CryptoAddressesModuleFilter[];
  /** Logical OR */
  or?: CryptoAddressesModuleFilter[];
  /** Logical NOT */
  not?: CryptoAddressesModuleFilter;
}

type CryptoAddressesModulesOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'DATABASE_ID_ASC'
  | 'DATABASE_ID_DESC'
  | 'SCHEMA_ID_ASC'
  | 'SCHEMA_ID_DESC'
  | 'PRIVATE_SCHEMA_ID_ASC'
  | 'PRIVATE_SCHEMA_ID_DESC'
  | 'TABLE_ID_ASC'
  | 'TABLE_ID_DESC'
  | 'OWNER_TABLE_ID_ASC'
  | 'OWNER_TABLE_ID_DESC'
  | 'TABLE_NAME_ASC'
  | 'TABLE_NAME_DESC'
  | 'CRYPTO_NETWORK_ASC'
  | 'CRYPTO_NETWORK_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface CryptoAddressesModulesQueryVariables {
  first?: number;
  offset?: number;
  filter?: CryptoAddressesModuleFilter;
  orderBy?: CryptoAddressesModulesOrderBy[];
}

export interface CryptoAddressesModulesQueryResult {
  cryptoAddressesModules: {
    totalCount: number;
    nodes: CryptoAddressesModule[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const cryptoAddressesModulesQueryKey = (
  variables?: CryptoAddressesModulesQueryVariables
) => ['cryptoaddressesmodule', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching CryptoAddressesModule list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useCryptoAddressesModulesQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useCryptoAddressesModulesQuery(
  variables?: CryptoAddressesModulesQueryVariables,
  options?: Omit<
    UseQueryOptions<CryptoAddressesModulesQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: cryptoAddressesModulesQueryKey(variables),
    queryFn: () =>
      execute<
        CryptoAddressesModulesQueryResult,
        CryptoAddressesModulesQueryVariables
      >(cryptoAddressesModulesQueryDocument, variables),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch CryptoAddressesModule list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchCryptoAddressesModulesQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: cryptoAddressesModulesQueryKey(variables),
 *   queryFn: () => fetchCryptoAddressesModulesQuery(variables),
 * });
 * ```
 */
export async function fetchCryptoAddressesModulesQuery(
  variables?: CryptoAddressesModulesQueryVariables,
  options?: ExecuteOptions
): Promise<CryptoAddressesModulesQueryResult> {
  return execute<
    CryptoAddressesModulesQueryResult,
    CryptoAddressesModulesQueryVariables
  >(cryptoAddressesModulesQueryDocument, variables, options);
}

/**
 * Prefetch CryptoAddressesModule list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchCryptoAddressesModulesQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchCryptoAddressesModulesQuery(
  queryClient: QueryClient,
  variables?: CryptoAddressesModulesQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: cryptoAddressesModulesQueryKey(variables),
    queryFn: () =>
      execute<
        CryptoAddressesModulesQueryResult,
        CryptoAddressesModulesQueryVariables
      >(cryptoAddressesModulesQueryDocument, variables, options),
  });
}
