/**
 * Single item query hook for Schema
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type Schema } from '../types';

// Re-export entity type for convenience
export type { Schema };

// ============================================================================
// GraphQL Document
// ============================================================================
export const schemaQueryDocument = `
query SchemaQuery($id: UUID!) {
  schema(id: $id) {
    id
    databaseId
    name
    schemaName
    label
    description
    smartTags
    category
    module
    scope
    tags
    isPublic
    createdAt
    updatedAt
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface SchemaQueryVariables {
  id: string;
}

export interface SchemaQueryResult {
  schema: Schema | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const schemaQueryKey = (id: string) => ['schema', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single Schema by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useSchemaQuery('value-here');
 *
 * if (data?.schema) {
 *   console.log(data.schema.id);
 * }
 * ```
 */
export function useSchemaQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<SchemaQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: schemaQueryKey(id),
    queryFn: () =>
      execute<SchemaQueryResult, SchemaQueryVariables>(schemaQueryDocument, {
        id,
      }),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single Schema by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchSchemaQuery('value-here');
 * ```
 */
export async function fetchSchemaQuery(
  id: string,
  options?: ExecuteOptions
): Promise<SchemaQueryResult> {
  return execute<SchemaQueryResult, SchemaQueryVariables>(
    schemaQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single Schema for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchSchemaQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchSchemaQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: schemaQueryKey(id),
    queryFn: () =>
      execute<SchemaQueryResult, SchemaQueryVariables>(
        schemaQueryDocument,
        { id },
        options
      ),
  });
}
