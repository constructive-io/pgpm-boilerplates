/**
 * Single item query hook for Extension
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type Extension } from '../types';

// Re-export entity type for convenience
export type { Extension };

// ============================================================================
// GraphQL Document
// ============================================================================
export const extensionQueryDocument = `
query ExtensionQuery($id: UUID!) {
  extension(id: $id) {
    name
    publicSchemas
    privateSchemas
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface ExtensionQueryVariables {
  id: string;
}

export interface ExtensionQueryResult {
  extension: Extension | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const extensionQueryKey = (id: string) =>
  ['extension', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single Extension by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useExtensionQuery('value-here');
 *
 * if (data?.extension) {
 *   console.log(data.extension.id);
 * }
 * ```
 */
export function useExtensionQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<ExtensionQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: extensionQueryKey(id),
    queryFn: () =>
      execute<ExtensionQueryResult, ExtensionQueryVariables>(
        extensionQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single Extension by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchExtensionQuery('value-here');
 * ```
 */
export async function fetchExtensionQuery(
  id: string,
  options?: ExecuteOptions
): Promise<ExtensionQueryResult> {
  return execute<ExtensionQueryResult, ExtensionQueryVariables>(
    extensionQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single Extension for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchExtensionQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchExtensionQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: extensionQueryKey(id),
    queryFn: () =>
      execute<ExtensionQueryResult, ExtensionQueryVariables>(
        extensionQueryDocument,
        { id },
        options
      ),
  });
}
