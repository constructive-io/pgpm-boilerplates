/**
 * Single item query hook for AuditLog
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type AuditLog } from '../types';

// Re-export entity type for convenience
export type { AuditLog };

// ============================================================================
// GraphQL Document
// ============================================================================
export const auditLogQueryDocument = `
query AuditLogQuery($id: UUID!) {
  auditLog(id: $id) {
    id
    event
    actorId
    origin
    userAgent
    ipAddress
    success
    createdAt
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface AuditLogQueryVariables {
  id: string;
}

export interface AuditLogQueryResult {
  auditLog: AuditLog | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const auditLogQueryKey = (id: string) =>
  ['auditlog', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single AuditLog by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAuditLogQuery('value-here');
 *
 * if (data?.auditLog) {
 *   console.log(data.auditLog.id);
 * }
 * ```
 */
export function useAuditLogQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<AuditLogQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: auditLogQueryKey(id),
    queryFn: () =>
      execute<AuditLogQueryResult, AuditLogQueryVariables>(
        auditLogQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single AuditLog by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchAuditLogQuery('value-here');
 * ```
 */
export async function fetchAuditLogQuery(
  id: string,
  options?: ExecuteOptions
): Promise<AuditLogQueryResult> {
  return execute<AuditLogQueryResult, AuditLogQueryVariables>(
    auditLogQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single AuditLog for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchAuditLogQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchAuditLogQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: auditLogQueryKey(id),
    queryFn: () =>
      execute<AuditLogQueryResult, AuditLogQueryVariables>(
        auditLogQueryDocument,
        { id },
        options
      ),
  });
}
