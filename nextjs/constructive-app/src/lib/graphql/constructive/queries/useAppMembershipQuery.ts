/**
 * Single item query hook for AppMembership
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type AppMembership } from '../types';

// Re-export entity type for convenience
export type { AppMembership };

// ============================================================================
// GraphQL Document
// ============================================================================
export const appMembershipQueryDocument = `
query AppMembershipQuery($id: UUID!) {
  appMembership(id: $id) {
    id
    createdAt
    updatedAt
    createdBy
    updatedBy
    isApproved
    isBanned
    isDisabled
    isVerified
    isActive
    isOwner
    isAdmin
    permissions
    granted
    actorId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface AppMembershipQueryVariables {
  id: string;
}

export interface AppMembershipQueryResult {
  appMembership: AppMembership | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const appMembershipQueryKey = (id: string) =>
  ['appmembership', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single AppMembership by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppMembershipQuery('value-here');
 *
 * if (data?.appMembership) {
 *   console.log(data.appMembership.id);
 * }
 * ```
 */
export function useAppMembershipQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<AppMembershipQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: appMembershipQueryKey(id),
    queryFn: () =>
      execute<AppMembershipQueryResult, AppMembershipQueryVariables>(
        appMembershipQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single AppMembership by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchAppMembershipQuery('value-here');
 * ```
 */
export async function fetchAppMembershipQuery(
  id: string,
  options?: ExecuteOptions
): Promise<AppMembershipQueryResult> {
  return execute<AppMembershipQueryResult, AppMembershipQueryVariables>(
    appMembershipQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single AppMembership for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchAppMembershipQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchAppMembershipQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: appMembershipQueryKey(id),
    queryFn: () =>
      execute<AppMembershipQueryResult, AppMembershipQueryVariables>(
        appMembershipQueryDocument,
        { id },
        options
      ),
  });
}
