/**
 * List query hook for AppLimit
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type AppLimit,
  type UUIDFilter,
  type StringFilter,
  type IntFilter,
} from '../types';

// Re-export entity type for convenience
export type { AppLimit };

// ============================================================================
// GraphQL Document
// ============================================================================
export const appLimitsQueryDocument = `
query AppLimitsQuery($first: Int, $offset: Int, $filter: AppLimitFilter, $orderBy: [AppLimitsOrderBy!]) {
  appLimits(first: $first, offset: $offset, filter: $filter, orderBy: $orderBy) {
    totalCount
    nodes {
      id
      name
      actorId
      num
      max
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface AppLimitFilter {
  id?: UUIDFilter;
  name?: StringFilter;
  actorId?: UUIDFilter;
  num?: IntFilter;
  max?: IntFilter;
  /** Logical AND */
  and?: AppLimitFilter[];
  /** Logical OR */
  or?: AppLimitFilter[];
  /** Logical NOT */
  not?: AppLimitFilter;
}

type AppLimitsOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'NAME_ASC'
  | 'NAME_DESC'
  | 'ACTOR_ID_ASC'
  | 'ACTOR_ID_DESC'
  | 'NUM_ASC'
  | 'NUM_DESC'
  | 'MAX_ASC'
  | 'MAX_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface AppLimitsQueryVariables {
  first?: number;
  offset?: number;
  filter?: AppLimitFilter;
  orderBy?: AppLimitsOrderBy[];
}

export interface AppLimitsQueryResult {
  appLimits: {
    totalCount: number;
    nodes: AppLimit[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const appLimitsQueryKey = (variables?: AppLimitsQueryVariables) =>
  ['applimit', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching AppLimit list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppLimitsQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useAppLimitsQuery(
  variables?: AppLimitsQueryVariables,
  options?: Omit<
    UseQueryOptions<AppLimitsQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: appLimitsQueryKey(variables),
    queryFn: () =>
      execute<AppLimitsQueryResult, AppLimitsQueryVariables>(
        appLimitsQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch AppLimit list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchAppLimitsQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: appLimitsQueryKey(variables),
 *   queryFn: () => fetchAppLimitsQuery(variables),
 * });
 * ```
 */
export async function fetchAppLimitsQuery(
  variables?: AppLimitsQueryVariables,
  options?: ExecuteOptions
): Promise<AppLimitsQueryResult> {
  return execute<AppLimitsQueryResult, AppLimitsQueryVariables>(
    appLimitsQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch AppLimit list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchAppLimitsQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchAppLimitsQuery(
  queryClient: QueryClient,
  variables?: AppLimitsQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: appLimitsQueryKey(variables),
    queryFn: () =>
      execute<AppLimitsQueryResult, AppLimitsQueryVariables>(
        appLimitsQueryDocument,
        variables,
        options
      ),
  });
}
