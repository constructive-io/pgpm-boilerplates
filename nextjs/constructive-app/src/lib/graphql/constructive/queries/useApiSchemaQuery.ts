/**
 * Single item query hook for ApiSchema
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type ApiSchema } from '../types';

// Re-export entity type for convenience
export type { ApiSchema };

// ============================================================================
// GraphQL Document
// ============================================================================
export const apiSchemaQueryDocument = `
query ApiSchemaQuery($id: UUID!) {
  apiSchema(id: $id) {
    id
    databaseId
    schemaId
    apiId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface ApiSchemaQueryVariables {
  id: string;
}

export interface ApiSchemaQueryResult {
  apiSchema: ApiSchema | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const apiSchemaQueryKey = (id: string) =>
  ['apischema', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single ApiSchema by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useApiSchemaQuery('value-here');
 *
 * if (data?.apiSchema) {
 *   console.log(data.apiSchema.id);
 * }
 * ```
 */
export function useApiSchemaQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<ApiSchemaQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: apiSchemaQueryKey(id),
    queryFn: () =>
      execute<ApiSchemaQueryResult, ApiSchemaQueryVariables>(
        apiSchemaQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single ApiSchema by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchApiSchemaQuery('value-here');
 * ```
 */
export async function fetchApiSchemaQuery(
  id: string,
  options?: ExecuteOptions
): Promise<ApiSchemaQueryResult> {
  return execute<ApiSchemaQueryResult, ApiSchemaQueryVariables>(
    apiSchemaQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single ApiSchema for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchApiSchemaQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchApiSchemaQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: apiSchemaQueryKey(id),
    queryFn: () =>
      execute<ApiSchemaQueryResult, ApiSchemaQueryVariables>(
        apiSchemaQueryDocument,
        { id },
        options
      ),
  });
}
