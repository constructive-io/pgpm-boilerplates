/**
 * Single item query hook for SecretsModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type SecretsModule } from '../types';

// Re-export entity type for convenience
export type { SecretsModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const secretsModuleQueryDocument = `
query SecretsModuleQuery($id: UUID!) {
  secretsModule(id: $id) {
    id
    databaseId
    schemaId
    tableId
    tableName
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface SecretsModuleQueryVariables {
  id: string;
}

export interface SecretsModuleQueryResult {
  secretsModule: SecretsModule | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const secretsModuleQueryKey = (id: string) =>
  ['secretsmodule', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single SecretsModule by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useSecretsModuleQuery('value-here');
 *
 * if (data?.secretsModule) {
 *   console.log(data.secretsModule.id);
 * }
 * ```
 */
export function useSecretsModuleQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<SecretsModuleQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: secretsModuleQueryKey(id),
    queryFn: () =>
      execute<SecretsModuleQueryResult, SecretsModuleQueryVariables>(
        secretsModuleQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single SecretsModule by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchSecretsModuleQuery('value-here');
 * ```
 */
export async function fetchSecretsModuleQuery(
  id: string,
  options?: ExecuteOptions
): Promise<SecretsModuleQueryResult> {
  return execute<SecretsModuleQueryResult, SecretsModuleQueryVariables>(
    secretsModuleQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single SecretsModule for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchSecretsModuleQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchSecretsModuleQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: secretsModuleQueryKey(id),
    queryFn: () =>
      execute<SecretsModuleQueryResult, SecretsModuleQueryVariables>(
        secretsModuleQueryDocument,
        { id },
        options
      ),
  });
}
