/**
 * List query hook for OrgPermissionDefault
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type OrgPermissionDefault,
  type UUIDFilter,
  type BitStringFilter,
} from '../types';

// Re-export entity type for convenience
export type { OrgPermissionDefault };

// ============================================================================
// GraphQL Document
// ============================================================================
export const orgPermissionDefaultsQueryDocument = `
query OrgPermissionDefaultsQuery($first: Int, $offset: Int, $filter: OrgPermissionDefaultFilter, $orderBy: [OrgPermissionDefaultsOrderBy!]) {
  orgPermissionDefaults(
    first: $first
    offset: $offset
    filter: $filter
    orderBy: $orderBy
  ) {
    totalCount
    nodes {
      id
      permissions
      entityId
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface OrgPermissionDefaultFilter {
  id?: UUIDFilter;
  permissions?: BitStringFilter;
  entityId?: UUIDFilter;
  /** Logical AND */
  and?: OrgPermissionDefaultFilter[];
  /** Logical OR */
  or?: OrgPermissionDefaultFilter[];
  /** Logical NOT */
  not?: OrgPermissionDefaultFilter;
}

type OrgPermissionDefaultsOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'PERMISSIONS_ASC'
  | 'PERMISSIONS_DESC'
  | 'ENTITY_ID_ASC'
  | 'ENTITY_ID_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface OrgPermissionDefaultsQueryVariables {
  first?: number;
  offset?: number;
  filter?: OrgPermissionDefaultFilter;
  orderBy?: OrgPermissionDefaultsOrderBy[];
}

export interface OrgPermissionDefaultsQueryResult {
  orgPermissionDefaults: {
    totalCount: number;
    nodes: OrgPermissionDefault[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const orgPermissionDefaultsQueryKey = (
  variables?: OrgPermissionDefaultsQueryVariables
) => ['orgpermissiondefault', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching OrgPermissionDefault list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgPermissionDefaultsQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useOrgPermissionDefaultsQuery(
  variables?: OrgPermissionDefaultsQueryVariables,
  options?: Omit<
    UseQueryOptions<OrgPermissionDefaultsQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: orgPermissionDefaultsQueryKey(variables),
    queryFn: () =>
      execute<
        OrgPermissionDefaultsQueryResult,
        OrgPermissionDefaultsQueryVariables
      >(orgPermissionDefaultsQueryDocument, variables),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch OrgPermissionDefault list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchOrgPermissionDefaultsQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: orgPermissionDefaultsQueryKey(variables),
 *   queryFn: () => fetchOrgPermissionDefaultsQuery(variables),
 * });
 * ```
 */
export async function fetchOrgPermissionDefaultsQuery(
  variables?: OrgPermissionDefaultsQueryVariables,
  options?: ExecuteOptions
): Promise<OrgPermissionDefaultsQueryResult> {
  return execute<
    OrgPermissionDefaultsQueryResult,
    OrgPermissionDefaultsQueryVariables
  >(orgPermissionDefaultsQueryDocument, variables, options);
}

/**
 * Prefetch OrgPermissionDefault list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchOrgPermissionDefaultsQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchOrgPermissionDefaultsQuery(
  queryClient: QueryClient,
  variables?: OrgPermissionDefaultsQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: orgPermissionDefaultsQueryKey(variables),
    queryFn: () =>
      execute<
        OrgPermissionDefaultsQueryResult,
        OrgPermissionDefaultsQueryVariables
      >(orgPermissionDefaultsQueryDocument, variables, options),
  });
}
