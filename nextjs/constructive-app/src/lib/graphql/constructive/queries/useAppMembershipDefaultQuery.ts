/**
 * Single item query hook for AppMembershipDefault
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type AppMembershipDefault } from '../types';

// Re-export entity type for convenience
export type { AppMembershipDefault };

// ============================================================================
// GraphQL Document
// ============================================================================
export const appMembershipDefaultQueryDocument = `
query AppMembershipDefaultQuery($id: UUID!) {
  appMembershipDefault(id: $id) {
    id
    createdAt
    updatedAt
    createdBy
    updatedBy
    isApproved
    isVerified
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface AppMembershipDefaultQueryVariables {
  id: string;
}

export interface AppMembershipDefaultQueryResult {
  appMembershipDefault: AppMembershipDefault | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const appMembershipDefaultQueryKey = (id: string) =>
  ['appmembershipdefault', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single AppMembershipDefault by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppMembershipDefaultQuery('value-here');
 *
 * if (data?.appMembershipDefault) {
 *   console.log(data.appMembershipDefault.id);
 * }
 * ```
 */
export function useAppMembershipDefaultQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<AppMembershipDefaultQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: appMembershipDefaultQueryKey(id),
    queryFn: () =>
      execute<
        AppMembershipDefaultQueryResult,
        AppMembershipDefaultQueryVariables
      >(appMembershipDefaultQueryDocument, { id }),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single AppMembershipDefault by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchAppMembershipDefaultQuery('value-here');
 * ```
 */
export async function fetchAppMembershipDefaultQuery(
  id: string,
  options?: ExecuteOptions
): Promise<AppMembershipDefaultQueryResult> {
  return execute<
    AppMembershipDefaultQueryResult,
    AppMembershipDefaultQueryVariables
  >(appMembershipDefaultQueryDocument, { id }, options);
}

/**
 * Prefetch a single AppMembershipDefault for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchAppMembershipDefaultQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchAppMembershipDefaultQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: appMembershipDefaultQueryKey(id),
    queryFn: () =>
      execute<
        AppMembershipDefaultQueryResult,
        AppMembershipDefaultQueryVariables
      >(appMembershipDefaultQueryDocument, { id }, options),
  });
}
