/**
 * Single item query hook for OrgMember
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type OrgMember } from '../types';

// Re-export entity type for convenience
export type { OrgMember };

// ============================================================================
// GraphQL Document
// ============================================================================
export const orgMemberQueryDocument = `
query OrgMemberQuery($id: UUID!) {
  orgMember(id: $id) {
    id
    isAdmin
    actorId
    entityId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface OrgMemberQueryVariables {
  id: string;
}

export interface OrgMemberQueryResult {
  orgMember: OrgMember | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const orgMemberQueryKey = (id: string) =>
  ['orgmember', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single OrgMember by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgMemberQuery('value-here');
 *
 * if (data?.orgMember) {
 *   console.log(data.orgMember.id);
 * }
 * ```
 */
export function useOrgMemberQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<OrgMemberQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: orgMemberQueryKey(id),
    queryFn: () =>
      execute<OrgMemberQueryResult, OrgMemberQueryVariables>(
        orgMemberQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single OrgMember by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchOrgMemberQuery('value-here');
 * ```
 */
export async function fetchOrgMemberQuery(
  id: string,
  options?: ExecuteOptions
): Promise<OrgMemberQueryResult> {
  return execute<OrgMemberQueryResult, OrgMemberQueryVariables>(
    orgMemberQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single OrgMember for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchOrgMemberQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchOrgMemberQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: orgMemberQueryKey(id),
    queryFn: () =>
      execute<OrgMemberQueryResult, OrgMemberQueryVariables>(
        orgMemberQueryDocument,
        { id },
        options
      ),
  });
}
