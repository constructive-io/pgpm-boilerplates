/**
 * Single item query hook for LimitsModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type LimitsModule } from '../types';

// Re-export entity type for convenience
export type { LimitsModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const limitsModuleQueryDocument = `
query LimitsModuleQuery($id: UUID!) {
  limitsModule(id: $id) {
    id
    databaseId
    schemaId
    privateSchemaId
    tableId
    tableName
    defaultTableId
    defaultTableName
    limitIncrementFunction
    limitDecrementFunction
    limitIncrementTrigger
    limitDecrementTrigger
    limitUpdateTrigger
    limitCheckFunction
    prefix
    membershipType
    entityTableId
    actorTableId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface LimitsModuleQueryVariables {
  id: string;
}

export interface LimitsModuleQueryResult {
  limitsModule: LimitsModule | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const limitsModuleQueryKey = (id: string) =>
  ['limitsmodule', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single LimitsModule by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useLimitsModuleQuery('value-here');
 *
 * if (data?.limitsModule) {
 *   console.log(data.limitsModule.id);
 * }
 * ```
 */
export function useLimitsModuleQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<LimitsModuleQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: limitsModuleQueryKey(id),
    queryFn: () =>
      execute<LimitsModuleQueryResult, LimitsModuleQueryVariables>(
        limitsModuleQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single LimitsModule by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchLimitsModuleQuery('value-here');
 * ```
 */
export async function fetchLimitsModuleQuery(
  id: string,
  options?: ExecuteOptions
): Promise<LimitsModuleQueryResult> {
  return execute<LimitsModuleQueryResult, LimitsModuleQueryVariables>(
    limitsModuleQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single LimitsModule for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchLimitsModuleQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchLimitsModuleQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: limitsModuleQueryKey(id),
    queryFn: () =>
      execute<LimitsModuleQueryResult, LimitsModuleQueryVariables>(
        limitsModuleQueryDocument,
        { id },
        options
      ),
  });
}
