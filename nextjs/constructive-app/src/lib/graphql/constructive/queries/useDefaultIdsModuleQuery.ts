/**
 * Single item query hook for DefaultIdsModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type DefaultIdsModule } from '../types';

// Re-export entity type for convenience
export type { DefaultIdsModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const defaultIdsModuleQueryDocument = `
query DefaultIdsModuleQuery($id: UUID!) {
  defaultIdsModule(id: $id) {
    id
    databaseId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DefaultIdsModuleQueryVariables {
  id: string;
}

export interface DefaultIdsModuleQueryResult {
  defaultIdsModule: DefaultIdsModule | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const defaultIdsModuleQueryKey = (id: string) =>
  ['defaultidsmodule', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single DefaultIdsModule by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useDefaultIdsModuleQuery('value-here');
 *
 * if (data?.defaultIdsModule) {
 *   console.log(data.defaultIdsModule.id);
 * }
 * ```
 */
export function useDefaultIdsModuleQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<DefaultIdsModuleQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: defaultIdsModuleQueryKey(id),
    queryFn: () =>
      execute<DefaultIdsModuleQueryResult, DefaultIdsModuleQueryVariables>(
        defaultIdsModuleQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single DefaultIdsModule by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchDefaultIdsModuleQuery('value-here');
 * ```
 */
export async function fetchDefaultIdsModuleQuery(
  id: string,
  options?: ExecuteOptions
): Promise<DefaultIdsModuleQueryResult> {
  return execute<DefaultIdsModuleQueryResult, DefaultIdsModuleQueryVariables>(
    defaultIdsModuleQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single DefaultIdsModule for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchDefaultIdsModuleQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchDefaultIdsModuleQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: defaultIdsModuleQueryKey(id),
    queryFn: () =>
      execute<DefaultIdsModuleQueryResult, DefaultIdsModuleQueryVariables>(
        defaultIdsModuleQueryDocument,
        { id },
        options
      ),
  });
}
