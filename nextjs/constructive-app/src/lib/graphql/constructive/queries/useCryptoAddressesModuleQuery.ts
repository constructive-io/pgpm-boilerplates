/**
 * Single item query hook for CryptoAddressesModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type CryptoAddressesModule } from '../types';

// Re-export entity type for convenience
export type { CryptoAddressesModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const cryptoAddressesModuleQueryDocument = `
query CryptoAddressesModuleQuery($id: UUID!) {
  cryptoAddressesModule(id: $id) {
    id
    databaseId
    schemaId
    privateSchemaId
    tableId
    ownerTableId
    tableName
    cryptoNetwork
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface CryptoAddressesModuleQueryVariables {
  id: string;
}

export interface CryptoAddressesModuleQueryResult {
  cryptoAddressesModule: CryptoAddressesModule | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const cryptoAddressesModuleQueryKey = (id: string) =>
  ['cryptoaddressesmodule', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single CryptoAddressesModule by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useCryptoAddressesModuleQuery('value-here');
 *
 * if (data?.cryptoAddressesModule) {
 *   console.log(data.cryptoAddressesModule.id);
 * }
 * ```
 */
export function useCryptoAddressesModuleQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<CryptoAddressesModuleQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: cryptoAddressesModuleQueryKey(id),
    queryFn: () =>
      execute<
        CryptoAddressesModuleQueryResult,
        CryptoAddressesModuleQueryVariables
      >(cryptoAddressesModuleQueryDocument, { id }),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single CryptoAddressesModule by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchCryptoAddressesModuleQuery('value-here');
 * ```
 */
export async function fetchCryptoAddressesModuleQuery(
  id: string,
  options?: ExecuteOptions
): Promise<CryptoAddressesModuleQueryResult> {
  return execute<
    CryptoAddressesModuleQueryResult,
    CryptoAddressesModuleQueryVariables
  >(cryptoAddressesModuleQueryDocument, { id }, options);
}

/**
 * Prefetch a single CryptoAddressesModule for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchCryptoAddressesModuleQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchCryptoAddressesModuleQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: cryptoAddressesModuleQueryKey(id),
    queryFn: () =>
      execute<
        CryptoAddressesModuleQueryResult,
        CryptoAddressesModuleQueryVariables
      >(cryptoAddressesModuleQueryDocument, { id }, options),
  });
}
