/**
 * Single item query hook for OrgLimitDefault
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type OrgLimitDefault } from '../types';

// Re-export entity type for convenience
export type { OrgLimitDefault };

// ============================================================================
// GraphQL Document
// ============================================================================
export const orgLimitDefaultQueryDocument = `
query OrgLimitDefaultQuery($id: UUID!) {
  orgLimitDefault(id: $id) {
    id
    name
    max
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface OrgLimitDefaultQueryVariables {
  id: string;
}

export interface OrgLimitDefaultQueryResult {
  orgLimitDefault: OrgLimitDefault | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const orgLimitDefaultQueryKey = (id: string) =>
  ['orglimitdefault', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single OrgLimitDefault by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgLimitDefaultQuery('value-here');
 *
 * if (data?.orgLimitDefault) {
 *   console.log(data.orgLimitDefault.id);
 * }
 * ```
 */
export function useOrgLimitDefaultQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<OrgLimitDefaultQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: orgLimitDefaultQueryKey(id),
    queryFn: () =>
      execute<OrgLimitDefaultQueryResult, OrgLimitDefaultQueryVariables>(
        orgLimitDefaultQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single OrgLimitDefault by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchOrgLimitDefaultQuery('value-here');
 * ```
 */
export async function fetchOrgLimitDefaultQuery(
  id: string,
  options?: ExecuteOptions
): Promise<OrgLimitDefaultQueryResult> {
  return execute<OrgLimitDefaultQueryResult, OrgLimitDefaultQueryVariables>(
    orgLimitDefaultQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single OrgLimitDefault for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchOrgLimitDefaultQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchOrgLimitDefaultQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: orgLimitDefaultQueryKey(id),
    queryFn: () =>
      execute<OrgLimitDefaultQueryResult, OrgLimitDefaultQueryVariables>(
        orgLimitDefaultQueryDocument,
        { id },
        options
      ),
  });
}
