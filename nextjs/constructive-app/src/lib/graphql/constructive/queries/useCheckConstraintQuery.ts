/**
 * Single item query hook for CheckConstraint
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type CheckConstraint } from '../types';

// Re-export entity type for convenience
export type { CheckConstraint };

// ============================================================================
// GraphQL Document
// ============================================================================
export const checkConstraintQueryDocument = `
query CheckConstraintQuery($id: UUID!) {
  checkConstraint(id: $id) {
    id
    databaseId
    tableId
    name
    type
    fieldIds
    expr
    smartTags
    category
    module
    scope
    tags
    createdAt
    updatedAt
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface CheckConstraintQueryVariables {
  id: string;
}

export interface CheckConstraintQueryResult {
  checkConstraint: CheckConstraint | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const checkConstraintQueryKey = (id: string) =>
  ['checkconstraint', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single CheckConstraint by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useCheckConstraintQuery('value-here');
 *
 * if (data?.checkConstraint) {
 *   console.log(data.checkConstraint.id);
 * }
 * ```
 */
export function useCheckConstraintQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<CheckConstraintQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: checkConstraintQueryKey(id),
    queryFn: () =>
      execute<CheckConstraintQueryResult, CheckConstraintQueryVariables>(
        checkConstraintQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single CheckConstraint by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchCheckConstraintQuery('value-here');
 * ```
 */
export async function fetchCheckConstraintQuery(
  id: string,
  options?: ExecuteOptions
): Promise<CheckConstraintQueryResult> {
  return execute<CheckConstraintQueryResult, CheckConstraintQueryVariables>(
    checkConstraintQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single CheckConstraint for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchCheckConstraintQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchCheckConstraintQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: checkConstraintQueryKey(id),
    queryFn: () =>
      execute<CheckConstraintQueryResult, CheckConstraintQueryVariables>(
        checkConstraintQueryDocument,
        { id },
        options
      ),
  });
}
