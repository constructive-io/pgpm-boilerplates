/**
 * List query hook for AppPermission
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type AppPermission,
  type UUIDFilter,
  type StringFilter,
  type IntFilter,
  type BitStringFilter,
} from '../types';

// Re-export entity type for convenience
export type { AppPermission };

// ============================================================================
// GraphQL Document
// ============================================================================
export const appPermissionsQueryDocument = `
query AppPermissionsQuery($first: Int, $offset: Int, $filter: AppPermissionFilter, $orderBy: [AppPermissionsOrderBy!]) {
  appPermissions(
    first: $first
    offset: $offset
    filter: $filter
    orderBy: $orderBy
  ) {
    totalCount
    nodes {
      id
      name
      bitnum
      bitstr
      description
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface AppPermissionFilter {
  id?: UUIDFilter;
  name?: StringFilter;
  bitnum?: IntFilter;
  bitstr?: BitStringFilter;
  description?: StringFilter;
  /** Logical AND */
  and?: AppPermissionFilter[];
  /** Logical OR */
  or?: AppPermissionFilter[];
  /** Logical NOT */
  not?: AppPermissionFilter;
}

type AppPermissionsOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'NAME_ASC'
  | 'NAME_DESC'
  | 'BITNUM_ASC'
  | 'BITNUM_DESC'
  | 'BITSTR_ASC'
  | 'BITSTR_DESC'
  | 'DESCRIPTION_ASC'
  | 'DESCRIPTION_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface AppPermissionsQueryVariables {
  first?: number;
  offset?: number;
  filter?: AppPermissionFilter;
  orderBy?: AppPermissionsOrderBy[];
}

export interface AppPermissionsQueryResult {
  appPermissions: {
    totalCount: number;
    nodes: AppPermission[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const appPermissionsQueryKey = (
  variables?: AppPermissionsQueryVariables
) => ['apppermission', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching AppPermission list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppPermissionsQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useAppPermissionsQuery(
  variables?: AppPermissionsQueryVariables,
  options?: Omit<
    UseQueryOptions<AppPermissionsQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: appPermissionsQueryKey(variables),
    queryFn: () =>
      execute<AppPermissionsQueryResult, AppPermissionsQueryVariables>(
        appPermissionsQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch AppPermission list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchAppPermissionsQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: appPermissionsQueryKey(variables),
 *   queryFn: () => fetchAppPermissionsQuery(variables),
 * });
 * ```
 */
export async function fetchAppPermissionsQuery(
  variables?: AppPermissionsQueryVariables,
  options?: ExecuteOptions
): Promise<AppPermissionsQueryResult> {
  return execute<AppPermissionsQueryResult, AppPermissionsQueryVariables>(
    appPermissionsQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch AppPermission list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchAppPermissionsQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchAppPermissionsQuery(
  queryClient: QueryClient,
  variables?: AppPermissionsQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: appPermissionsQueryKey(variables),
    queryFn: () =>
      execute<AppPermissionsQueryResult, AppPermissionsQueryVariables>(
        appPermissionsQueryDocument,
        variables,
        options
      ),
  });
}
