/**
 * Single item query hook for Object
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type Object } from '../types';

// Re-export entity type for convenience
export type { Object };

// ============================================================================
// GraphQL Document
// ============================================================================
export const objectQueryDocument = `
query ObjectQuery($id: UUID!) {
  object(id: $id) {
    id
    databaseId
    kids
    ktree
    data
    frzn
    createdAt
    hashUuid
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface ObjectQueryVariables {
  id: string;
}

export interface ObjectQueryResult {
  object: Object | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const objectQueryKey = (id: string) => ['object', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single Object by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useObjectQuery('value-here');
 *
 * if (data?.object) {
 *   console.log(data.object.id);
 * }
 * ```
 */
export function useObjectQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<ObjectQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: objectQueryKey(id),
    queryFn: () =>
      execute<ObjectQueryResult, ObjectQueryVariables>(objectQueryDocument, {
        id,
      }),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single Object by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchObjectQuery('value-here');
 * ```
 */
export async function fetchObjectQuery(
  id: string,
  options?: ExecuteOptions
): Promise<ObjectQueryResult> {
  return execute<ObjectQueryResult, ObjectQueryVariables>(
    objectQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single Object for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchObjectQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchObjectQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: objectQueryKey(id),
    queryFn: () =>
      execute<ObjectQueryResult, ObjectQueryVariables>(
        objectQueryDocument,
        { id },
        options
      ),
  });
}
