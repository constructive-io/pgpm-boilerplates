/**
 * List query hook for Policy
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type Policy,
  type UUIDFilter,
  type StringFilter,
  type BooleanFilter,
  type JSONFilter,
  type IntFilter,
  type StringListFilter,
  type DatetimeFilter,
} from '../types';

// Re-export entity type for convenience
export type { Policy };

// ============================================================================
// GraphQL Document
// ============================================================================
export const policiesQueryDocument = `
query PoliciesQuery($first: Int, $offset: Int, $filter: PolicyFilter, $orderBy: [PoliciesOrderBy!]) {
  policies(first: $first, offset: $offset, filter: $filter, orderBy: $orderBy) {
    totalCount
    nodes {
      id
      databaseId
      tableId
      name
      roleName
      privilege
      permissive
      disabled
      template
      data
      smartTags
      category
      module
      scope
      tags
      createdAt
      updatedAt
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface PolicyFilter {
  id?: UUIDFilter;
  databaseId?: UUIDFilter;
  tableId?: UUIDFilter;
  name?: StringFilter;
  roleName?: StringFilter;
  privilege?: StringFilter;
  permissive?: BooleanFilter;
  disabled?: BooleanFilter;
  template?: StringFilter;
  data?: JSONFilter;
  smartTags?: JSONFilter;
  module?: StringFilter;
  scope?: IntFilter;
  tags?: StringListFilter;
  createdAt?: DatetimeFilter;
  updatedAt?: DatetimeFilter;
  /** Logical AND */
  and?: PolicyFilter[];
  /** Logical OR */
  or?: PolicyFilter[];
  /** Logical NOT */
  not?: PolicyFilter;
}

type PoliciesOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'DATABASE_ID_ASC'
  | 'DATABASE_ID_DESC'
  | 'TABLE_ID_ASC'
  | 'TABLE_ID_DESC'
  | 'NAME_ASC'
  | 'NAME_DESC'
  | 'ROLE_NAME_ASC'
  | 'ROLE_NAME_DESC'
  | 'PRIVILEGE_ASC'
  | 'PRIVILEGE_DESC'
  | 'PERMISSIVE_ASC'
  | 'PERMISSIVE_DESC'
  | 'DISABLED_ASC'
  | 'DISABLED_DESC'
  | 'TEMPLATE_ASC'
  | 'TEMPLATE_DESC'
  | 'DATA_ASC'
  | 'DATA_DESC'
  | 'SMART_TAGS_ASC'
  | 'SMART_TAGS_DESC'
  | 'CATEGORY_ASC'
  | 'CATEGORY_DESC'
  | 'MODULE_ASC'
  | 'MODULE_DESC'
  | 'SCOPE_ASC'
  | 'SCOPE_DESC'
  | 'TAGS_ASC'
  | 'TAGS_DESC'
  | 'CREATED_AT_ASC'
  | 'CREATED_AT_DESC'
  | 'UPDATED_AT_ASC'
  | 'UPDATED_AT_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface PoliciesQueryVariables {
  first?: number;
  offset?: number;
  filter?: PolicyFilter;
  orderBy?: PoliciesOrderBy[];
}

export interface PoliciesQueryResult {
  policies: {
    totalCount: number;
    nodes: Policy[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const policiesQueryKey = (variables?: PoliciesQueryVariables) =>
  ['policy', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching Policy list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePoliciesQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function usePoliciesQuery(
  variables?: PoliciesQueryVariables,
  options?: Omit<
    UseQueryOptions<PoliciesQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: policiesQueryKey(variables),
    queryFn: () =>
      execute<PoliciesQueryResult, PoliciesQueryVariables>(
        policiesQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch Policy list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchPoliciesQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: policiesQueryKey(variables),
 *   queryFn: () => fetchPoliciesQuery(variables),
 * });
 * ```
 */
export async function fetchPoliciesQuery(
  variables?: PoliciesQueryVariables,
  options?: ExecuteOptions
): Promise<PoliciesQueryResult> {
  return execute<PoliciesQueryResult, PoliciesQueryVariables>(
    policiesQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch Policy list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchPoliciesQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchPoliciesQuery(
  queryClient: QueryClient,
  variables?: PoliciesQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: policiesQueryKey(variables),
    queryFn: () =>
      execute<PoliciesQueryResult, PoliciesQueryVariables>(
        policiesQueryDocument,
        variables,
        options
      ),
  });
}
