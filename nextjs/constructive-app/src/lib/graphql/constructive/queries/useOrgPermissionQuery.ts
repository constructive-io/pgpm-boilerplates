/**
 * Single item query hook for OrgPermission
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type OrgPermission } from '../types';

// Re-export entity type for convenience
export type { OrgPermission };

// ============================================================================
// GraphQL Document
// ============================================================================
export const orgPermissionQueryDocument = `
query OrgPermissionQuery($id: UUID!) {
  orgPermission(id: $id) {
    id
    name
    bitnum
    bitstr
    description
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface OrgPermissionQueryVariables {
  id: string;
}

export interface OrgPermissionQueryResult {
  orgPermission: OrgPermission | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const orgPermissionQueryKey = (id: string) =>
  ['orgpermission', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single OrgPermission by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgPermissionQuery('value-here');
 *
 * if (data?.orgPermission) {
 *   console.log(data.orgPermission.id);
 * }
 * ```
 */
export function useOrgPermissionQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<OrgPermissionQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: orgPermissionQueryKey(id),
    queryFn: () =>
      execute<OrgPermissionQueryResult, OrgPermissionQueryVariables>(
        orgPermissionQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single OrgPermission by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchOrgPermissionQuery('value-here');
 * ```
 */
export async function fetchOrgPermissionQuery(
  id: string,
  options?: ExecuteOptions
): Promise<OrgPermissionQueryResult> {
  return execute<OrgPermissionQueryResult, OrgPermissionQueryVariables>(
    orgPermissionQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single OrgPermission for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchOrgPermissionQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchOrgPermissionQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: orgPermissionQueryKey(id),
    queryFn: () =>
      execute<OrgPermissionQueryResult, OrgPermissionQueryVariables>(
        orgPermissionQueryDocument,
        { id },
        options
      ),
  });
}
