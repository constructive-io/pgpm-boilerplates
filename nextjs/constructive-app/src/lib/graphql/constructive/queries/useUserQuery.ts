/**
 * Single item query hook for User
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type User } from '../types';

// Re-export entity type for convenience
export type { User };

// ============================================================================
// GraphQL Document
// ============================================================================
export const userQueryDocument = `
query UserQuery($id: UUID!) {
  user(id: $id) {
    id
    username
    displayName
    profilePicture
    searchTsv
    type
    createdAt
    updatedAt
    searchTsvRank
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface UserQueryVariables {
  id: string;
}

export interface UserQueryResult {
  user: User | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const userQueryKey = (id: string) => ['user', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single User by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useUserQuery('value-here');
 *
 * if (data?.user) {
 *   console.log(data.user.id);
 * }
 * ```
 */
export function useUserQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<UserQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: userQueryKey(id),
    queryFn: () =>
      execute<UserQueryResult, UserQueryVariables>(userQueryDocument, { id }),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single User by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchUserQuery('value-here');
 * ```
 */
export async function fetchUserQuery(
  id: string,
  options?: ExecuteOptions
): Promise<UserQueryResult> {
  return execute<UserQueryResult, UserQueryVariables>(
    userQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single User for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchUserQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchUserQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: userQueryKey(id),
    queryFn: () =>
      execute<UserQueryResult, UserQueryVariables>(
        userQueryDocument,
        { id },
        options
      ),
  });
}
