/**
 * Single item query hook for RoleType
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type RoleType } from '../types';

// Re-export entity type for convenience
export type { RoleType };

// ============================================================================
// GraphQL Document
// ============================================================================
export const roleTypeQueryDocument = `
query RoleTypeQuery($id: Int!) {
  roleType(id: $id) {
    id
    name
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface RoleTypeQueryVariables {
  id: number;
}

export interface RoleTypeQueryResult {
  roleType: RoleType | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const roleTypeQueryKey = (id: number) =>
  ['roletype', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single RoleType by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useRoleTypeQuery(123);
 *
 * if (data?.roleType) {
 *   console.log(data.roleType.id);
 * }
 * ```
 */
export function useRoleTypeQuery(
  id: number,
  options?: Omit<
    UseQueryOptions<RoleTypeQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: roleTypeQueryKey(id),
    queryFn: () =>
      execute<RoleTypeQueryResult, RoleTypeQueryVariables>(
        roleTypeQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single RoleType by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchRoleTypeQuery(123);
 * ```
 */
export async function fetchRoleTypeQuery(
  id: number,
  options?: ExecuteOptions
): Promise<RoleTypeQueryResult> {
  return execute<RoleTypeQueryResult, RoleTypeQueryVariables>(
    roleTypeQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single RoleType for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchRoleTypeQuery(queryClient, 123);
 * ```
 */
export async function prefetchRoleTypeQuery(
  queryClient: QueryClient,
  id: number,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: roleTypeQueryKey(id),
    queryFn: () =>
      execute<RoleTypeQueryResult, RoleTypeQueryVariables>(
        roleTypeQueryDocument,
        { id },
        options
      ),
  });
}
