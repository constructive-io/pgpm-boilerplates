/**
 * List query hook for Object
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type Object,
  type UUIDFilter,
  type UUIDListFilter,
  type StringListFilter,
  type JSONFilter,
  type BooleanFilter,
  type DatetimeFilter,
} from '../types';

// Re-export entity type for convenience
export type { Object };

// ============================================================================
// GraphQL Document
// ============================================================================
export const objectsQueryDocument = `
query ObjectsQuery($first: Int, $offset: Int, $filter: ObjectFilter, $orderBy: [ObjectsOrderBy!]) {
  objects(first: $first, offset: $offset, filter: $filter, orderBy: $orderBy) {
    totalCount
    nodes {
      id
      databaseId
      kids
      ktree
      data
      frzn
      createdAt
      hashUuid
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface ObjectFilter {
  id?: UUIDFilter;
  databaseId?: UUIDFilter;
  kids?: UUIDListFilter;
  ktree?: StringListFilter;
  data?: JSONFilter;
  frzn?: BooleanFilter;
  createdAt?: DatetimeFilter;
  hashUuid?: UUIDFilter;
  /** Logical AND */
  and?: ObjectFilter[];
  /** Logical OR */
  or?: ObjectFilter[];
  /** Logical NOT */
  not?: ObjectFilter;
}

type ObjectsOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'DATABASE_ID_ASC'
  | 'DATABASE_ID_DESC'
  | 'KIDS_ASC'
  | 'KIDS_DESC'
  | 'KTREE_ASC'
  | 'KTREE_DESC'
  | 'DATA_ASC'
  | 'DATA_DESC'
  | 'FRZN_ASC'
  | 'FRZN_DESC'
  | 'CREATED_AT_ASC'
  | 'CREATED_AT_DESC'
  | 'HASH_UUID_ASC'
  | 'HASH_UUID_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface ObjectsQueryVariables {
  first?: number;
  offset?: number;
  filter?: ObjectFilter;
  orderBy?: ObjectsOrderBy[];
}

export interface ObjectsQueryResult {
  objects: {
    totalCount: number;
    nodes: Object[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const objectsQueryKey = (variables?: ObjectsQueryVariables) =>
  ['object', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching Object list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useObjectsQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useObjectsQuery(
  variables?: ObjectsQueryVariables,
  options?: Omit<
    UseQueryOptions<ObjectsQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: objectsQueryKey(variables),
    queryFn: () =>
      execute<ObjectsQueryResult, ObjectsQueryVariables>(
        objectsQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch Object list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchObjectsQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: objectsQueryKey(variables),
 *   queryFn: () => fetchObjectsQuery(variables),
 * });
 * ```
 */
export async function fetchObjectsQuery(
  variables?: ObjectsQueryVariables,
  options?: ExecuteOptions
): Promise<ObjectsQueryResult> {
  return execute<ObjectsQueryResult, ObjectsQueryVariables>(
    objectsQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch Object list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchObjectsQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchObjectsQuery(
  queryClient: QueryClient,
  variables?: ObjectsQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: objectsQueryKey(variables),
    queryFn: () =>
      execute<ObjectsQueryResult, ObjectsQueryVariables>(
        objectsQueryDocument,
        variables,
        options
      ),
  });
}
