/**
 * Single item query hook for ApiExtension
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type ApiExtension } from '../types';

// Re-export entity type for convenience
export type { ApiExtension };

// ============================================================================
// GraphQL Document
// ============================================================================
export const apiExtensionQueryDocument = `
query ApiExtensionQuery($id: UUID!) {
  apiExtension(id: $id) {
    id
    schemaName
    databaseId
    apiId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface ApiExtensionQueryVariables {
  id: string;
}

export interface ApiExtensionQueryResult {
  apiExtension: ApiExtension | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const apiExtensionQueryKey = (id: string) =>
  ['apiextension', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single ApiExtension by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useApiExtensionQuery('value-here');
 *
 * if (data?.apiExtension) {
 *   console.log(data.apiExtension.id);
 * }
 * ```
 */
export function useApiExtensionQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<ApiExtensionQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: apiExtensionQueryKey(id),
    queryFn: () =>
      execute<ApiExtensionQueryResult, ApiExtensionQueryVariables>(
        apiExtensionQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single ApiExtension by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchApiExtensionQuery('value-here');
 * ```
 */
export async function fetchApiExtensionQuery(
  id: string,
  options?: ExecuteOptions
): Promise<ApiExtensionQueryResult> {
  return execute<ApiExtensionQueryResult, ApiExtensionQueryVariables>(
    apiExtensionQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single ApiExtension for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchApiExtensionQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchApiExtensionQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: apiExtensionQueryKey(id),
    queryFn: () =>
      execute<ApiExtensionQueryResult, ApiExtensionQueryVariables>(
        apiExtensionQueryDocument,
        { id },
        options
      ),
  });
}
