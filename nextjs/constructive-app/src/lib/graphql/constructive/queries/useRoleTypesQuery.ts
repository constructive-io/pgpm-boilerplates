/**
 * List query hook for RoleType
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type RoleType, type IntFilter, type StringFilter } from '../types';

// Re-export entity type for convenience
export type { RoleType };

// ============================================================================
// GraphQL Document
// ============================================================================
export const roleTypesQueryDocument = `
query RoleTypesQuery($first: Int, $offset: Int, $filter: RoleTypeFilter, $orderBy: [RoleTypesOrderBy!]) {
  roleTypes(first: $first, offset: $offset, filter: $filter, orderBy: $orderBy) {
    totalCount
    nodes {
      id
      name
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface RoleTypeFilter {
  id?: IntFilter;
  name?: StringFilter;
  /** Logical AND */
  and?: RoleTypeFilter[];
  /** Logical OR */
  or?: RoleTypeFilter[];
  /** Logical NOT */
  not?: RoleTypeFilter;
}

type RoleTypesOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'NAME_ASC'
  | 'NAME_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface RoleTypesQueryVariables {
  first?: number;
  offset?: number;
  filter?: RoleTypeFilter;
  orderBy?: RoleTypesOrderBy[];
}

export interface RoleTypesQueryResult {
  roleTypes: {
    totalCount: number;
    nodes: RoleType[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const roleTypesQueryKey = (variables?: RoleTypesQueryVariables) =>
  ['roletype', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching RoleType list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useRoleTypesQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useRoleTypesQuery(
  variables?: RoleTypesQueryVariables,
  options?: Omit<
    UseQueryOptions<RoleTypesQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: roleTypesQueryKey(variables),
    queryFn: () =>
      execute<RoleTypesQueryResult, RoleTypesQueryVariables>(
        roleTypesQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch RoleType list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchRoleTypesQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: roleTypesQueryKey(variables),
 *   queryFn: () => fetchRoleTypesQuery(variables),
 * });
 * ```
 */
export async function fetchRoleTypesQuery(
  variables?: RoleTypesQueryVariables,
  options?: ExecuteOptions
): Promise<RoleTypesQueryResult> {
  return execute<RoleTypesQueryResult, RoleTypesQueryVariables>(
    roleTypesQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch RoleType list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchRoleTypesQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchRoleTypesQuery(
  queryClient: QueryClient,
  variables?: RoleTypesQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: roleTypesQueryKey(variables),
    queryFn: () =>
      execute<RoleTypesQueryResult, RoleTypesQueryVariables>(
        roleTypesQueryDocument,
        variables,
        options
      ),
  });
}
