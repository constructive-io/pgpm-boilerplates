/**
 * List query hook for Api
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type Api,
  type UUIDFilter,
  type StringFilter,
  type BooleanFilter,
} from '../types';

// Re-export entity type for convenience
export type { Api };

// ============================================================================
// GraphQL Document
// ============================================================================
export const apisQueryDocument = `
query ApisQuery($first: Int, $offset: Int, $filter: ApiFilter, $orderBy: [ApisOrderBy!]) {
  apis(first: $first, offset: $offset, filter: $filter, orderBy: $orderBy) {
    totalCount
    nodes {
      id
      databaseId
      name
      dbname
      roleName
      anonRole
      isPublic
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface ApiFilter {
  id?: UUIDFilter;
  databaseId?: UUIDFilter;
  name?: StringFilter;
  dbname?: StringFilter;
  roleName?: StringFilter;
  anonRole?: StringFilter;
  isPublic?: BooleanFilter;
  /** Logical AND */
  and?: ApiFilter[];
  /** Logical OR */
  or?: ApiFilter[];
  /** Logical NOT */
  not?: ApiFilter;
}

type ApisOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'DATABASE_ID_ASC'
  | 'DATABASE_ID_DESC'
  | 'NAME_ASC'
  | 'NAME_DESC'
  | 'DBNAME_ASC'
  | 'DBNAME_DESC'
  | 'ROLE_NAME_ASC'
  | 'ROLE_NAME_DESC'
  | 'ANON_ROLE_ASC'
  | 'ANON_ROLE_DESC'
  | 'IS_PUBLIC_ASC'
  | 'IS_PUBLIC_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface ApisQueryVariables {
  first?: number;
  offset?: number;
  filter?: ApiFilter;
  orderBy?: ApisOrderBy[];
}

export interface ApisQueryResult {
  apis: {
    totalCount: number;
    nodes: Api[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const apisQueryKey = (variables?: ApisQueryVariables) =>
  ['api', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching Api list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useApisQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useApisQuery(
  variables?: ApisQueryVariables,
  options?: Omit<
    UseQueryOptions<ApisQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: apisQueryKey(variables),
    queryFn: () =>
      execute<ApisQueryResult, ApisQueryVariables>(
        apisQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch Api list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchApisQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: apisQueryKey(variables),
 *   queryFn: () => fetchApisQuery(variables),
 * });
 * ```
 */
export async function fetchApisQuery(
  variables?: ApisQueryVariables,
  options?: ExecuteOptions
): Promise<ApisQueryResult> {
  return execute<ApisQueryResult, ApisQueryVariables>(
    apisQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch Api list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchApisQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchApisQuery(
  queryClient: QueryClient,
  variables?: ApisQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: apisQueryKey(variables),
    queryFn: () =>
      execute<ApisQueryResult, ApisQueryVariables>(
        apisQueryDocument,
        variables,
        options
      ),
  });
}
