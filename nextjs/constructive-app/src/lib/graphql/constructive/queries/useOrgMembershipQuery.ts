/**
 * Single item query hook for OrgMembership
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type OrgMembership } from '../types';

// Re-export entity type for convenience
export type { OrgMembership };

// ============================================================================
// GraphQL Document
// ============================================================================
export const orgMembershipQueryDocument = `
query OrgMembershipQuery($id: UUID!) {
  orgMembership(id: $id) {
    id
    createdAt
    updatedAt
    createdBy
    updatedBy
    isApproved
    isBanned
    isDisabled
    isActive
    isOwner
    isAdmin
    permissions
    granted
    actorId
    entityId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface OrgMembershipQueryVariables {
  id: string;
}

export interface OrgMembershipQueryResult {
  orgMembership: OrgMembership | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const orgMembershipQueryKey = (id: string) =>
  ['orgmembership', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single OrgMembership by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgMembershipQuery('value-here');
 *
 * if (data?.orgMembership) {
 *   console.log(data.orgMembership.id);
 * }
 * ```
 */
export function useOrgMembershipQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<OrgMembershipQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: orgMembershipQueryKey(id),
    queryFn: () =>
      execute<OrgMembershipQueryResult, OrgMembershipQueryVariables>(
        orgMembershipQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single OrgMembership by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchOrgMembershipQuery('value-here');
 * ```
 */
export async function fetchOrgMembershipQuery(
  id: string,
  options?: ExecuteOptions
): Promise<OrgMembershipQueryResult> {
  return execute<OrgMembershipQueryResult, OrgMembershipQueryVariables>(
    orgMembershipQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single OrgMembership for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchOrgMembershipQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchOrgMembershipQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: orgMembershipQueryKey(id),
    queryFn: () =>
      execute<OrgMembershipQueryResult, OrgMembershipQueryVariables>(
        orgMembershipQueryDocument,
        { id },
        options
      ),
  });
}
