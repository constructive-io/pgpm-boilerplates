/**
 * List query hook for UserSetting
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type UserSetting,
  type UUIDFilter,
  type BigFloatFilter,
  type IntFilter,
  type DatetimeFilter,
} from '../types';

// Re-export entity type for convenience
export type { UserSetting };

// ============================================================================
// GraphQL Document
// ============================================================================
export const userSettingsQueryDocument = `
query UserSettingsQuery($first: Int, $offset: Int, $filter: UserSettingFilter, $orderBy: [UserSettingsOrderBy!]) {
  userSettings(first: $first, offset: $offset, filter: $filter, orderBy: $orderBy) {
    totalCount
    nodes {
      id
      ownerId
      searchRadius
      zip
      location
      createdAt
      updatedAt
      createdBy
      updatedBy
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface UserSettingFilter {
  id?: UUIDFilter;
  ownerId?: UUIDFilter;
  searchRadius?: BigFloatFilter;
  zip?: IntFilter;
  createdAt?: DatetimeFilter;
  updatedAt?: DatetimeFilter;
  createdBy?: UUIDFilter;
  updatedBy?: UUIDFilter;
  /** Logical AND */
  and?: UserSettingFilter[];
  /** Logical OR */
  or?: UserSettingFilter[];
  /** Logical NOT */
  not?: UserSettingFilter;
}

type UserSettingsOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'OWNER_ID_ASC'
  | 'OWNER_ID_DESC'
  | 'SEARCH_RADIUS_ASC'
  | 'SEARCH_RADIUS_DESC'
  | 'ZIP_ASC'
  | 'ZIP_DESC'
  | 'LOCATION_ASC'
  | 'LOCATION_DESC'
  | 'CREATED_AT_ASC'
  | 'CREATED_AT_DESC'
  | 'UPDATED_AT_ASC'
  | 'UPDATED_AT_DESC'
  | 'CREATED_BY_ASC'
  | 'CREATED_BY_DESC'
  | 'UPDATED_BY_ASC'
  | 'UPDATED_BY_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface UserSettingsQueryVariables {
  first?: number;
  offset?: number;
  filter?: UserSettingFilter;
  orderBy?: UserSettingsOrderBy[];
}

export interface UserSettingsQueryResult {
  userSettings: {
    totalCount: number;
    nodes: UserSetting[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const userSettingsQueryKey = (variables?: UserSettingsQueryVariables) =>
  ['usersetting', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching UserSetting list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useUserSettingsQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useUserSettingsQuery(
  variables?: UserSettingsQueryVariables,
  options?: Omit<
    UseQueryOptions<UserSettingsQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: userSettingsQueryKey(variables),
    queryFn: () =>
      execute<UserSettingsQueryResult, UserSettingsQueryVariables>(
        userSettingsQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch UserSetting list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchUserSettingsQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: userSettingsQueryKey(variables),
 *   queryFn: () => fetchUserSettingsQuery(variables),
 * });
 * ```
 */
export async function fetchUserSettingsQuery(
  variables?: UserSettingsQueryVariables,
  options?: ExecuteOptions
): Promise<UserSettingsQueryResult> {
  return execute<UserSettingsQueryResult, UserSettingsQueryVariables>(
    userSettingsQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch UserSetting list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchUserSettingsQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchUserSettingsQuery(
  queryClient: QueryClient,
  variables?: UserSettingsQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: userSettingsQueryKey(variables),
    queryFn: () =>
      execute<UserSettingsQueryResult, UserSettingsQueryVariables>(
        userSettingsQueryDocument,
        variables,
        options
      ),
  });
}
