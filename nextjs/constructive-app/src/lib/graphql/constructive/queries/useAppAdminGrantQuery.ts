/**
 * Single item query hook for AppAdminGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type AppAdminGrant } from '../types';

// Re-export entity type for convenience
export type { AppAdminGrant };

// ============================================================================
// GraphQL Document
// ============================================================================
export const appAdminGrantQueryDocument = `
query AppAdminGrantQuery($id: UUID!) {
  appAdminGrant(id: $id) {
    id
    isGrant
    actorId
    grantorId
    createdAt
    updatedAt
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface AppAdminGrantQueryVariables {
  id: string;
}

export interface AppAdminGrantQueryResult {
  appAdminGrant: AppAdminGrant | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const appAdminGrantQueryKey = (id: string) =>
  ['appadmingrant', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single AppAdminGrant by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppAdminGrantQuery('value-here');
 *
 * if (data?.appAdminGrant) {
 *   console.log(data.appAdminGrant.id);
 * }
 * ```
 */
export function useAppAdminGrantQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<AppAdminGrantQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: appAdminGrantQueryKey(id),
    queryFn: () =>
      execute<AppAdminGrantQueryResult, AppAdminGrantQueryVariables>(
        appAdminGrantQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single AppAdminGrant by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchAppAdminGrantQuery('value-here');
 * ```
 */
export async function fetchAppAdminGrantQuery(
  id: string,
  options?: ExecuteOptions
): Promise<AppAdminGrantQueryResult> {
  return execute<AppAdminGrantQueryResult, AppAdminGrantQueryVariables>(
    appAdminGrantQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single AppAdminGrant for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchAppAdminGrantQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchAppAdminGrantQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: appAdminGrantQueryKey(id),
    queryFn: () =>
      execute<AppAdminGrantQueryResult, AppAdminGrantQueryVariables>(
        appAdminGrantQueryDocument,
        { id },
        options
      ),
  });
}
