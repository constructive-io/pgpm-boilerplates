/**
 * List query hook for Index
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type Index,
  type UUIDFilter,
  type StringFilter,
  type UUIDListFilter,
  type JSONFilter,
  type BooleanFilter,
  type IntFilter,
  type StringListFilter,
  type DatetimeFilter,
} from '../types';

// Re-export entity type for convenience
export type { Index };

// ============================================================================
// GraphQL Document
// ============================================================================
export const indicesQueryDocument = `
query IndicesQuery($first: Int, $offset: Int, $filter: IndexFilter, $orderBy: [IndicesOrderBy!]) {
  indices(first: $first, offset: $offset, filter: $filter, orderBy: $orderBy) {
    totalCount
    nodes {
      id
      databaseId
      tableId
      name
      fieldIds
      includeFieldIds
      accessMethod
      indexParams
      whereClause
      isUnique
      smartTags
      category
      module
      scope
      tags
      createdAt
      updatedAt
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface IndexFilter {
  id?: UUIDFilter;
  databaseId?: UUIDFilter;
  tableId?: UUIDFilter;
  name?: StringFilter;
  fieldIds?: UUIDListFilter;
  includeFieldIds?: UUIDListFilter;
  accessMethod?: StringFilter;
  indexParams?: JSONFilter;
  whereClause?: JSONFilter;
  isUnique?: BooleanFilter;
  smartTags?: JSONFilter;
  module?: StringFilter;
  scope?: IntFilter;
  tags?: StringListFilter;
  createdAt?: DatetimeFilter;
  updatedAt?: DatetimeFilter;
  /** Logical AND */
  and?: IndexFilter[];
  /** Logical OR */
  or?: IndexFilter[];
  /** Logical NOT */
  not?: IndexFilter;
}

type IndicesOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'DATABASE_ID_ASC'
  | 'DATABASE_ID_DESC'
  | 'TABLE_ID_ASC'
  | 'TABLE_ID_DESC'
  | 'NAME_ASC'
  | 'NAME_DESC'
  | 'FIELD_IDS_ASC'
  | 'FIELD_IDS_DESC'
  | 'INCLUDE_FIELD_IDS_ASC'
  | 'INCLUDE_FIELD_IDS_DESC'
  | 'ACCESS_METHOD_ASC'
  | 'ACCESS_METHOD_DESC'
  | 'INDEX_PARAMS_ASC'
  | 'INDEX_PARAMS_DESC'
  | 'WHERE_CLAUSE_ASC'
  | 'WHERE_CLAUSE_DESC'
  | 'IS_UNIQUE_ASC'
  | 'IS_UNIQUE_DESC'
  | 'SMART_TAGS_ASC'
  | 'SMART_TAGS_DESC'
  | 'CATEGORY_ASC'
  | 'CATEGORY_DESC'
  | 'MODULE_ASC'
  | 'MODULE_DESC'
  | 'SCOPE_ASC'
  | 'SCOPE_DESC'
  | 'TAGS_ASC'
  | 'TAGS_DESC'
  | 'CREATED_AT_ASC'
  | 'CREATED_AT_DESC'
  | 'UPDATED_AT_ASC'
  | 'UPDATED_AT_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface IndicesQueryVariables {
  first?: number;
  offset?: number;
  filter?: IndexFilter;
  orderBy?: IndicesOrderBy[];
}

export interface IndicesQueryResult {
  indices: {
    totalCount: number;
    nodes: Index[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const indicesQueryKey = (variables?: IndicesQueryVariables) =>
  ['index', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching Index list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useIndicesQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useIndicesQuery(
  variables?: IndicesQueryVariables,
  options?: Omit<
    UseQueryOptions<IndicesQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: indicesQueryKey(variables),
    queryFn: () =>
      execute<IndicesQueryResult, IndicesQueryVariables>(
        indicesQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch Index list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchIndicesQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: indicesQueryKey(variables),
 *   queryFn: () => fetchIndicesQuery(variables),
 * });
 * ```
 */
export async function fetchIndicesQuery(
  variables?: IndicesQueryVariables,
  options?: ExecuteOptions
): Promise<IndicesQueryResult> {
  return execute<IndicesQueryResult, IndicesQueryVariables>(
    indicesQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch Index list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchIndicesQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchIndicesQuery(
  queryClient: QueryClient,
  variables?: IndicesQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: indicesQueryKey(variables),
    queryFn: () =>
      execute<IndicesQueryResult, IndicesQueryVariables>(
        indicesQueryDocument,
        variables,
        options
      ),
  });
}
