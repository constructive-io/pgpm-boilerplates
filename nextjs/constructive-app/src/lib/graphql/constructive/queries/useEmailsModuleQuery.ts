/**
 * Single item query hook for EmailsModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type EmailsModule } from '../types';

// Re-export entity type for convenience
export type { EmailsModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const emailsModuleQueryDocument = `
query EmailsModuleQuery($id: UUID!) {
  emailsModule(id: $id) {
    id
    databaseId
    schemaId
    privateSchemaId
    tableId
    ownerTableId
    tableName
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface EmailsModuleQueryVariables {
  id: string;
}

export interface EmailsModuleQueryResult {
  emailsModule: EmailsModule | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const emailsModuleQueryKey = (id: string) =>
  ['emailsmodule', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single EmailsModule by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useEmailsModuleQuery('value-here');
 *
 * if (data?.emailsModule) {
 *   console.log(data.emailsModule.id);
 * }
 * ```
 */
export function useEmailsModuleQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<EmailsModuleQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: emailsModuleQueryKey(id),
    queryFn: () =>
      execute<EmailsModuleQueryResult, EmailsModuleQueryVariables>(
        emailsModuleQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single EmailsModule by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchEmailsModuleQuery('value-here');
 * ```
 */
export async function fetchEmailsModuleQuery(
  id: string,
  options?: ExecuteOptions
): Promise<EmailsModuleQueryResult> {
  return execute<EmailsModuleQueryResult, EmailsModuleQueryVariables>(
    emailsModuleQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single EmailsModule for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchEmailsModuleQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchEmailsModuleQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: emailsModuleQueryKey(id),
    queryFn: () =>
      execute<EmailsModuleQueryResult, EmailsModuleQueryVariables>(
        emailsModuleQueryDocument,
        { id },
        options
      ),
  });
}
