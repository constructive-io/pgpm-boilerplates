/**
 * List query hook for Domain
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type Domain, type UUIDFilter, type StringFilter } from '../types';

// Re-export entity type for convenience
export type { Domain };

// ============================================================================
// GraphQL Document
// ============================================================================
export const domainsQueryDocument = `
query DomainsQuery($first: Int, $offset: Int, $filter: DomainFilter, $orderBy: [DomainsOrderBy!]) {
  domains(first: $first, offset: $offset, filter: $filter, orderBy: $orderBy) {
    totalCount
    nodes {
      id
      databaseId
      apiId
      siteId
      subdomain
      domain
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface DomainFilter {
  id?: UUIDFilter;
  databaseId?: UUIDFilter;
  apiId?: UUIDFilter;
  siteId?: UUIDFilter;
  subdomain?: StringFilter;
  domain?: StringFilter;
  /** Logical AND */
  and?: DomainFilter[];
  /** Logical OR */
  or?: DomainFilter[];
  /** Logical NOT */
  not?: DomainFilter;
}

type DomainsOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'DATABASE_ID_ASC'
  | 'DATABASE_ID_DESC'
  | 'API_ID_ASC'
  | 'API_ID_DESC'
  | 'SITE_ID_ASC'
  | 'SITE_ID_DESC'
  | 'SUBDOMAIN_ASC'
  | 'SUBDOMAIN_DESC'
  | 'DOMAIN_ASC'
  | 'DOMAIN_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface DomainsQueryVariables {
  first?: number;
  offset?: number;
  filter?: DomainFilter;
  orderBy?: DomainsOrderBy[];
}

export interface DomainsQueryResult {
  domains: {
    totalCount: number;
    nodes: Domain[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const domainsQueryKey = (variables?: DomainsQueryVariables) =>
  ['domain', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching Domain list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useDomainsQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useDomainsQuery(
  variables?: DomainsQueryVariables,
  options?: Omit<
    UseQueryOptions<DomainsQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: domainsQueryKey(variables),
    queryFn: () =>
      execute<DomainsQueryResult, DomainsQueryVariables>(
        domainsQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch Domain list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchDomainsQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: domainsQueryKey(variables),
 *   queryFn: () => fetchDomainsQuery(variables),
 * });
 * ```
 */
export async function fetchDomainsQuery(
  variables?: DomainsQueryVariables,
  options?: ExecuteOptions
): Promise<DomainsQueryResult> {
  return execute<DomainsQueryResult, DomainsQueryVariables>(
    domainsQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch Domain list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchDomainsQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchDomainsQuery(
  queryClient: QueryClient,
  variables?: DomainsQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: domainsQueryKey(variables),
    queryFn: () =>
      execute<DomainsQueryResult, DomainsQueryVariables>(
        domainsQueryDocument,
        variables,
        options
      ),
  });
}
