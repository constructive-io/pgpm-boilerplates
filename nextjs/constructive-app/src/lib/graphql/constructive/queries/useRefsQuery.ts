/**
 * List query hook for Ref
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type Ref, type UUIDFilter, type StringFilter } from '../types';

// Re-export entity type for convenience
export type { Ref };

// ============================================================================
// GraphQL Document
// ============================================================================
export const refsQueryDocument = `
query RefsQuery($first: Int, $offset: Int, $filter: RefFilter, $orderBy: [RefsOrderBy!]) {
  refs(first: $first, offset: $offset, filter: $filter, orderBy: $orderBy) {
    totalCount
    nodes {
      id
      name
      databaseId
      storeId
      commitId
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface RefFilter {
  id?: UUIDFilter;
  name?: StringFilter;
  databaseId?: UUIDFilter;
  storeId?: UUIDFilter;
  commitId?: UUIDFilter;
  /** Logical AND */
  and?: RefFilter[];
  /** Logical OR */
  or?: RefFilter[];
  /** Logical NOT */
  not?: RefFilter;
}

type RefsOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'NAME_ASC'
  | 'NAME_DESC'
  | 'DATABASE_ID_ASC'
  | 'DATABASE_ID_DESC'
  | 'STORE_ID_ASC'
  | 'STORE_ID_DESC'
  | 'COMMIT_ID_ASC'
  | 'COMMIT_ID_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface RefsQueryVariables {
  first?: number;
  offset?: number;
  filter?: RefFilter;
  orderBy?: RefsOrderBy[];
}

export interface RefsQueryResult {
  refs: {
    totalCount: number;
    nodes: Ref[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const refsQueryKey = (variables?: RefsQueryVariables) =>
  ['ref', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching Ref list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useRefsQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useRefsQuery(
  variables?: RefsQueryVariables,
  options?: Omit<
    UseQueryOptions<RefsQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: refsQueryKey(variables),
    queryFn: () =>
      execute<RefsQueryResult, RefsQueryVariables>(
        refsQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch Ref list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchRefsQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: refsQueryKey(variables),
 *   queryFn: () => fetchRefsQuery(variables),
 * });
 * ```
 */
export async function fetchRefsQuery(
  variables?: RefsQueryVariables,
  options?: ExecuteOptions
): Promise<RefsQueryResult> {
  return execute<RefsQueryResult, RefsQueryVariables>(
    refsQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch Ref list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchRefsQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchRefsQuery(
  queryClient: QueryClient,
  variables?: RefsQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: refsQueryKey(variables),
    queryFn: () =>
      execute<RefsQueryResult, RefsQueryVariables>(
        refsQueryDocument,
        variables,
        options
      ),
  });
}
