/**
 * Single item query hook for CryptoAddress
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type CryptoAddress } from '../types';

// Re-export entity type for convenience
export type { CryptoAddress };

// ============================================================================
// GraphQL Document
// ============================================================================
export const cryptoAddressQueryDocument = `
query CryptoAddressQuery($id: UUID!) {
  cryptoAddress(id: $id) {
    id
    ownerId
    address
    isVerified
    isPrimary
    createdAt
    updatedAt
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface CryptoAddressQueryVariables {
  id: string;
}

export interface CryptoAddressQueryResult {
  cryptoAddress: CryptoAddress | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const cryptoAddressQueryKey = (id: string) =>
  ['cryptoaddress', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single CryptoAddress by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useCryptoAddressQuery('value-here');
 *
 * if (data?.cryptoAddress) {
 *   console.log(data.cryptoAddress.id);
 * }
 * ```
 */
export function useCryptoAddressQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<CryptoAddressQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: cryptoAddressQueryKey(id),
    queryFn: () =>
      execute<CryptoAddressQueryResult, CryptoAddressQueryVariables>(
        cryptoAddressQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single CryptoAddress by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchCryptoAddressQuery('value-here');
 * ```
 */
export async function fetchCryptoAddressQuery(
  id: string,
  options?: ExecuteOptions
): Promise<CryptoAddressQueryResult> {
  return execute<CryptoAddressQueryResult, CryptoAddressQueryVariables>(
    cryptoAddressQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single CryptoAddress for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchCryptoAddressQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchCryptoAddressQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: cryptoAddressQueryKey(id),
    queryFn: () =>
      execute<CryptoAddressQueryResult, CryptoAddressQueryVariables>(
        cryptoAddressQueryDocument,
        { id },
        options
      ),
  });
}
