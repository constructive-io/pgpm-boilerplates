/**
 * Single item query hook for OrgInvite
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type OrgInvite } from '../types';

// Re-export entity type for convenience
export type { OrgInvite };

// ============================================================================
// GraphQL Document
// ============================================================================
export const orgInviteQueryDocument = `
query OrgInviteQuery($id: UUID!) {
  orgInvite(id: $id) {
    id
    email
    senderId
    receiverId
    inviteToken
    inviteValid
    inviteLimit
    inviteCount
    multiple
    data
    expiresAt
    createdAt
    updatedAt
    entityId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface OrgInviteQueryVariables {
  id: string;
}

export interface OrgInviteQueryResult {
  orgInvite: OrgInvite | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const orgInviteQueryKey = (id: string) =>
  ['orginvite', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single OrgInvite by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgInviteQuery('value-here');
 *
 * if (data?.orgInvite) {
 *   console.log(data.orgInvite.id);
 * }
 * ```
 */
export function useOrgInviteQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<OrgInviteQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: orgInviteQueryKey(id),
    queryFn: () =>
      execute<OrgInviteQueryResult, OrgInviteQueryVariables>(
        orgInviteQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single OrgInvite by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchOrgInviteQuery('value-here');
 * ```
 */
export async function fetchOrgInviteQuery(
  id: string,
  options?: ExecuteOptions
): Promise<OrgInviteQueryResult> {
  return execute<OrgInviteQueryResult, OrgInviteQueryVariables>(
    orgInviteQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single OrgInvite for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchOrgInviteQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchOrgInviteQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: orgInviteQueryKey(id),
    queryFn: () =>
      execute<OrgInviteQueryResult, OrgInviteQueryVariables>(
        orgInviteQueryDocument,
        { id },
        options
      ),
  });
}
