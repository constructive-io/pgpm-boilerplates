/**
 * Single item query hook for MembershipType
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type MembershipType } from '../types';

// Re-export entity type for convenience
export type { MembershipType };

// ============================================================================
// GraphQL Document
// ============================================================================
export const membershipTypeQueryDocument = `
query MembershipTypeQuery($id: Int!) {
  membershipType(id: $id) {
    id
    name
    description
    prefix
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface MembershipTypeQueryVariables {
  id: number;
}

export interface MembershipTypeQueryResult {
  membershipType: MembershipType | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const membershipTypeQueryKey = (id: number) =>
  ['membershiptype', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single MembershipType by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useMembershipTypeQuery(123);
 *
 * if (data?.membershipType) {
 *   console.log(data.membershipType.id);
 * }
 * ```
 */
export function useMembershipTypeQuery(
  id: number,
  options?: Omit<
    UseQueryOptions<MembershipTypeQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: membershipTypeQueryKey(id),
    queryFn: () =>
      execute<MembershipTypeQueryResult, MembershipTypeQueryVariables>(
        membershipTypeQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single MembershipType by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchMembershipTypeQuery(123);
 * ```
 */
export async function fetchMembershipTypeQuery(
  id: number,
  options?: ExecuteOptions
): Promise<MembershipTypeQueryResult> {
  return execute<MembershipTypeQueryResult, MembershipTypeQueryVariables>(
    membershipTypeQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single MembershipType for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchMembershipTypeQuery(queryClient, 123);
 * ```
 */
export async function prefetchMembershipTypeQuery(
  queryClient: QueryClient,
  id: number,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: membershipTypeQueryKey(id),
    queryFn: () =>
      execute<MembershipTypeQueryResult, MembershipTypeQueryVariables>(
        membershipTypeQueryDocument,
        { id },
        options
      ),
  });
}
