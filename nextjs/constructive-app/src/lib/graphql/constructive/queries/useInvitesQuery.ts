/**
 * List query hook for Invite
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type Invite,
  type UUIDFilter,
  type StringFilter,
  type BooleanFilter,
  type IntFilter,
  type JSONFilter,
  type DatetimeFilter,
} from '../types';

// Re-export entity type for convenience
export type { Invite };

// ============================================================================
// GraphQL Document
// ============================================================================
export const invitesQueryDocument = `
query InvitesQuery($first: Int, $offset: Int, $filter: InviteFilter, $orderBy: [InvitesOrderBy!]) {
  invites(first: $first, offset: $offset, filter: $filter, orderBy: $orderBy) {
    totalCount
    nodes {
      id
      email
      senderId
      inviteToken
      inviteValid
      inviteLimit
      inviteCount
      multiple
      data
      expiresAt
      createdAt
      updatedAt
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface InviteFilter {
  id?: UUIDFilter;
  email?: StringFilter;
  senderId?: UUIDFilter;
  inviteToken?: StringFilter;
  inviteValid?: BooleanFilter;
  inviteLimit?: IntFilter;
  inviteCount?: IntFilter;
  multiple?: BooleanFilter;
  data?: JSONFilter;
  expiresAt?: DatetimeFilter;
  createdAt?: DatetimeFilter;
  updatedAt?: DatetimeFilter;
  /** Logical AND */
  and?: InviteFilter[];
  /** Logical OR */
  or?: InviteFilter[];
  /** Logical NOT */
  not?: InviteFilter;
}

type InvitesOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'EMAIL_ASC'
  | 'EMAIL_DESC'
  | 'SENDER_ID_ASC'
  | 'SENDER_ID_DESC'
  | 'INVITE_TOKEN_ASC'
  | 'INVITE_TOKEN_DESC'
  | 'INVITE_VALID_ASC'
  | 'INVITE_VALID_DESC'
  | 'INVITE_LIMIT_ASC'
  | 'INVITE_LIMIT_DESC'
  | 'INVITE_COUNT_ASC'
  | 'INVITE_COUNT_DESC'
  | 'MULTIPLE_ASC'
  | 'MULTIPLE_DESC'
  | 'DATA_ASC'
  | 'DATA_DESC'
  | 'EXPIRES_AT_ASC'
  | 'EXPIRES_AT_DESC'
  | 'CREATED_AT_ASC'
  | 'CREATED_AT_DESC'
  | 'UPDATED_AT_ASC'
  | 'UPDATED_AT_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface InvitesQueryVariables {
  first?: number;
  offset?: number;
  filter?: InviteFilter;
  orderBy?: InvitesOrderBy[];
}

export interface InvitesQueryResult {
  invites: {
    totalCount: number;
    nodes: Invite[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const invitesQueryKey = (variables?: InvitesQueryVariables) =>
  ['invite', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching Invite list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useInvitesQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useInvitesQuery(
  variables?: InvitesQueryVariables,
  options?: Omit<
    UseQueryOptions<InvitesQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: invitesQueryKey(variables),
    queryFn: () =>
      execute<InvitesQueryResult, InvitesQueryVariables>(
        invitesQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch Invite list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchInvitesQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: invitesQueryKey(variables),
 *   queryFn: () => fetchInvitesQuery(variables),
 * });
 * ```
 */
export async function fetchInvitesQuery(
  variables?: InvitesQueryVariables,
  options?: ExecuteOptions
): Promise<InvitesQueryResult> {
  return execute<InvitesQueryResult, InvitesQueryVariables>(
    invitesQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch Invite list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchInvitesQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchInvitesQuery(
  queryClient: QueryClient,
  variables?: InvitesQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: invitesQueryKey(variables),
    queryFn: () =>
      execute<InvitesQueryResult, InvitesQueryVariables>(
        invitesQueryDocument,
        variables,
        options
      ),
  });
}
