/**
 * Single item query hook for Email
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type Email } from '../types';

// Re-export entity type for convenience
export type { Email };

// ============================================================================
// GraphQL Document
// ============================================================================
export const emailQueryDocument = `
query EmailQuery($id: UUID!) {
  email(id: $id) {
    id
    ownerId
    email
    isVerified
    isPrimary
    createdAt
    updatedAt
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface EmailQueryVariables {
  id: string;
}

export interface EmailQueryResult {
  email: Email | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const emailQueryKey = (id: string) => ['email', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single Email by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useEmailQuery('value-here');
 *
 * if (data?.email) {
 *   console.log(data.email.id);
 * }
 * ```
 */
export function useEmailQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<EmailQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: emailQueryKey(id),
    queryFn: () =>
      execute<EmailQueryResult, EmailQueryVariables>(emailQueryDocument, {
        id,
      }),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single Email by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchEmailQuery('value-here');
 * ```
 */
export async function fetchEmailQuery(
  id: string,
  options?: ExecuteOptions
): Promise<EmailQueryResult> {
  return execute<EmailQueryResult, EmailQueryVariables>(
    emailQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single Email for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchEmailQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchEmailQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: emailQueryKey(id),
    queryFn: () =>
      execute<EmailQueryResult, EmailQueryVariables>(
        emailQueryDocument,
        { id },
        options
      ),
  });
}
