/**
 * Custom query hook for stepsRequired
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type AppLevelRequirementFilter,
  type AppLevelRequirementsConnection,
} from '../schema-types';

/** GraphQL query document */
export const stepsRequiredQueryDocument = `
query StepsRequiredQuery($vlevel: String, $vroleId: UUID, $first: Int, $last: Int, $offset: Int, $before: Cursor, $after: Cursor, $filter: AppLevelRequirementFilter) {
  stepsRequired(
    vlevel: $vlevel
    vroleId: $vroleId
    first: $first
    last: $last
    offset: $offset
    before: $before
    after: $after
    filter: $filter
  ) {
    totalCount
    nodes {
      id
      name
      level
      description
      requiredCount
      priority
      createdAt
      updatedAt
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

export interface StepsRequiredQueryVariables {
  vlevel?: string;
  vroleId?: string;
  /** Only read the first `n` values of the set. */
  first?: number;
  /** Only read the last `n` values of the set. */
  last?: number;
  /**
   * Skip the first `n` values from our `after` cursor, an alternative to cursor
   * based pagination. May not be used with `last`.
   */
  offset?: number;
  /** Read all values in the set before (above) this cursor. */
  before?: string;
  /** Read all values in the set after (below) this cursor. */
  after?: string;
  /** A filter to be used in determining which values should be returned by the collection. */
  filter?: AppLevelRequirementFilter;
}

export interface StepsRequiredQueryResult {
  stepsRequired: AppLevelRequirementsConnection;
}

/** Query key factory for caching */
export const stepsRequiredQueryKey = (
  variables?: StepsRequiredQueryVariables
) => ['stepsRequired', variables] as const;

/**
 * Reads and enables pagination through a set of `AppLevelRequirement`.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useStepsRequiredQuery({ vlevel, vroleId, first, last, offset, before, after, filter });
 *
 * if (data?.stepsRequired) {
 *   console.log(data.stepsRequired);
 * }
 * ```
 */
export function useStepsRequiredQuery(
  variables?: StepsRequiredQueryVariables,
  options?: Omit<
    UseQueryOptions<StepsRequiredQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: stepsRequiredQueryKey(variables),
    queryFn: () =>
      execute<StepsRequiredQueryResult, StepsRequiredQueryVariables>(
        stepsRequiredQueryDocument,
        variables
      ),

    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch stepsRequired without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchStepsRequiredQuery({ vlevel, vroleId, first, last, offset, before, after, filter });
 * ```
 */
export async function fetchStepsRequiredQuery(
  variables?: StepsRequiredQueryVariables,
  options?: ExecuteOptions
): Promise<StepsRequiredQueryResult> {
  return execute<StepsRequiredQueryResult, StepsRequiredQueryVariables>(
    stepsRequiredQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch stepsRequired for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchStepsRequiredQuery(queryClient, { vlevel, vroleId, first, last, offset, before, after, filter });
 * ```
 */
export async function prefetchStepsRequiredQuery(
  queryClient: QueryClient,
  variables?: StepsRequiredQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: stepsRequiredQueryKey(variables),
    queryFn: () =>
      execute<StepsRequiredQueryResult, StepsRequiredQueryVariables>(
        stepsRequiredQueryDocument,
        variables,
        options
      ),
  });
}
