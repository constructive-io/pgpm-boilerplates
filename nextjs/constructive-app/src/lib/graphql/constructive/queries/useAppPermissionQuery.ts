/**
 * Single item query hook for AppPermission
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type AppPermission } from '../types';

// Re-export entity type for convenience
export type { AppPermission };

// ============================================================================
// GraphQL Document
// ============================================================================
export const appPermissionQueryDocument = `
query AppPermissionQuery($id: UUID!) {
  appPermission(id: $id) {
    id
    name
    bitnum
    bitstr
    description
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface AppPermissionQueryVariables {
  id: string;
}

export interface AppPermissionQueryResult {
  appPermission: AppPermission | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const appPermissionQueryKey = (id: string) =>
  ['apppermission', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single AppPermission by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppPermissionQuery('value-here');
 *
 * if (data?.appPermission) {
 *   console.log(data.appPermission.id);
 * }
 * ```
 */
export function useAppPermissionQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<AppPermissionQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: appPermissionQueryKey(id),
    queryFn: () =>
      execute<AppPermissionQueryResult, AppPermissionQueryVariables>(
        appPermissionQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single AppPermission by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchAppPermissionQuery('value-here');
 * ```
 */
export async function fetchAppPermissionQuery(
  id: string,
  options?: ExecuteOptions
): Promise<AppPermissionQueryResult> {
  return execute<AppPermissionQueryResult, AppPermissionQueryVariables>(
    appPermissionQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single AppPermission for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchAppPermissionQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchAppPermissionQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: appPermissionQueryKey(id),
    queryFn: () =>
      execute<AppPermissionQueryResult, AppPermissionQueryVariables>(
        appPermissionQueryDocument,
        { id },
        options
      ),
  });
}
