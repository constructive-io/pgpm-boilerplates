/**
 * List query hook for FullTextSearch
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type FullTextSearch,
  type UUIDFilter,
  type UUIDListFilter,
  type StringListFilter,
  type DatetimeFilter,
} from '../types';

// Re-export entity type for convenience
export type { FullTextSearch };

// ============================================================================
// GraphQL Document
// ============================================================================
export const fullTextSearchesQueryDocument = `
query FullTextSearchesQuery($first: Int, $offset: Int, $filter: FullTextSearchFilter, $orderBy: [FullTextSearchesOrderBy!]) {
  fullTextSearches(
    first: $first
    offset: $offset
    filter: $filter
    orderBy: $orderBy
  ) {
    totalCount
    nodes {
      id
      databaseId
      tableId
      fieldId
      fieldIds
      weights
      langs
      createdAt
      updatedAt
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface FullTextSearchFilter {
  id?: UUIDFilter;
  databaseId?: UUIDFilter;
  tableId?: UUIDFilter;
  fieldId?: UUIDFilter;
  fieldIds?: UUIDListFilter;
  weights?: StringListFilter;
  langs?: StringListFilter;
  createdAt?: DatetimeFilter;
  updatedAt?: DatetimeFilter;
  /** Logical AND */
  and?: FullTextSearchFilter[];
  /** Logical OR */
  or?: FullTextSearchFilter[];
  /** Logical NOT */
  not?: FullTextSearchFilter;
}

type FullTextSearchesOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'DATABASE_ID_ASC'
  | 'DATABASE_ID_DESC'
  | 'TABLE_ID_ASC'
  | 'TABLE_ID_DESC'
  | 'FIELD_ID_ASC'
  | 'FIELD_ID_DESC'
  | 'FIELD_IDS_ASC'
  | 'FIELD_IDS_DESC'
  | 'WEIGHTS_ASC'
  | 'WEIGHTS_DESC'
  | 'LANGS_ASC'
  | 'LANGS_DESC'
  | 'CREATED_AT_ASC'
  | 'CREATED_AT_DESC'
  | 'UPDATED_AT_ASC'
  | 'UPDATED_AT_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface FullTextSearchesQueryVariables {
  first?: number;
  offset?: number;
  filter?: FullTextSearchFilter;
  orderBy?: FullTextSearchesOrderBy[];
}

export interface FullTextSearchesQueryResult {
  fullTextSearches: {
    totalCount: number;
    nodes: FullTextSearch[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const fullTextSearchesQueryKey = (
  variables?: FullTextSearchesQueryVariables
) => ['fulltextsearch', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching FullTextSearch list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useFullTextSearchesQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useFullTextSearchesQuery(
  variables?: FullTextSearchesQueryVariables,
  options?: Omit<
    UseQueryOptions<FullTextSearchesQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: fullTextSearchesQueryKey(variables),
    queryFn: () =>
      execute<FullTextSearchesQueryResult, FullTextSearchesQueryVariables>(
        fullTextSearchesQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch FullTextSearch list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchFullTextSearchesQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: fullTextSearchesQueryKey(variables),
 *   queryFn: () => fetchFullTextSearchesQuery(variables),
 * });
 * ```
 */
export async function fetchFullTextSearchesQuery(
  variables?: FullTextSearchesQueryVariables,
  options?: ExecuteOptions
): Promise<FullTextSearchesQueryResult> {
  return execute<FullTextSearchesQueryResult, FullTextSearchesQueryVariables>(
    fullTextSearchesQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch FullTextSearch list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchFullTextSearchesQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchFullTextSearchesQuery(
  queryClient: QueryClient,
  variables?: FullTextSearchesQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: fullTextSearchesQueryKey(variables),
    queryFn: () =>
      execute<FullTextSearchesQueryResult, FullTextSearchesQueryVariables>(
        fullTextSearchesQueryDocument,
        variables,
        options
      ),
  });
}
