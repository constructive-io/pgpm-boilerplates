/**
 * List query hook for Extension
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type Extension,
  type StringFilter,
  type StringListFilter,
} from '../types';

// Re-export entity type for convenience
export type { Extension };

// ============================================================================
// GraphQL Document
// ============================================================================
export const extensionsQueryDocument = `
query ExtensionsQuery($first: Int, $offset: Int, $filter: ExtensionFilter, $orderBy: [ExtensionsOrderBy!]) {
  extensions(first: $first, offset: $offset, filter: $filter, orderBy: $orderBy) {
    totalCount
    nodes {
      name
      publicSchemas
      privateSchemas
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface ExtensionFilter {
  name?: StringFilter;
  publicSchemas?: StringListFilter;
  privateSchemas?: StringListFilter;
  /** Logical AND */
  and?: ExtensionFilter[];
  /** Logical OR */
  or?: ExtensionFilter[];
  /** Logical NOT */
  not?: ExtensionFilter;
}

type ExtensionsOrderBy =
  | 'NAME_ASC'
  | 'NAME_DESC'
  | 'PUBLIC_SCHEMAS_ASC'
  | 'PUBLIC_SCHEMAS_DESC'
  | 'PRIVATE_SCHEMAS_ASC'
  | 'PRIVATE_SCHEMAS_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface ExtensionsQueryVariables {
  first?: number;
  offset?: number;
  filter?: ExtensionFilter;
  orderBy?: ExtensionsOrderBy[];
}

export interface ExtensionsQueryResult {
  extensions: {
    totalCount: number;
    nodes: Extension[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const extensionsQueryKey = (variables?: ExtensionsQueryVariables) =>
  ['extension', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching Extension list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useExtensionsQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useExtensionsQuery(
  variables?: ExtensionsQueryVariables,
  options?: Omit<
    UseQueryOptions<ExtensionsQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: extensionsQueryKey(variables),
    queryFn: () =>
      execute<ExtensionsQueryResult, ExtensionsQueryVariables>(
        extensionsQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch Extension list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchExtensionsQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: extensionsQueryKey(variables),
 *   queryFn: () => fetchExtensionsQuery(variables),
 * });
 * ```
 */
export async function fetchExtensionsQuery(
  variables?: ExtensionsQueryVariables,
  options?: ExecuteOptions
): Promise<ExtensionsQueryResult> {
  return execute<ExtensionsQueryResult, ExtensionsQueryVariables>(
    extensionsQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch Extension list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchExtensionsQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchExtensionsQuery(
  queryClient: QueryClient,
  variables?: ExtensionsQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: extensionsQueryKey(variables),
    queryFn: () =>
      execute<ExtensionsQueryResult, ExtensionsQueryVariables>(
        extensionsQueryDocument,
        variables,
        options
      ),
  });
}
