/**
 * Custom query hook for orgPermissionsGetByMask
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type OrgPermissionFilter,
  type OrgPermissionsConnection,
} from '../schema-types';

/** GraphQL query document */
export const orgPermissionsGetByMaskQueryDocument = `
query OrgPermissionsGetByMaskQuery($mask: BitString, $first: Int, $last: Int, $offset: Int, $before: Cursor, $after: Cursor, $filter: OrgPermissionFilter) {
  orgPermissionsGetByMask(
    mask: $mask
    first: $first
    last: $last
    offset: $offset
    before: $before
    after: $after
    filter: $filter
  ) {
    totalCount
    nodes {
      id
      name
      bitnum
      bitstr
      description
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

export interface OrgPermissionsGetByMaskQueryVariables {
  mask?: string;
  /** Only read the first `n` values of the set. */
  first?: number;
  /** Only read the last `n` values of the set. */
  last?: number;
  /**
   * Skip the first `n` values from our `after` cursor, an alternative to cursor
   * based pagination. May not be used with `last`.
   */
  offset?: number;
  /** Read all values in the set before (above) this cursor. */
  before?: string;
  /** Read all values in the set after (below) this cursor. */
  after?: string;
  /** A filter to be used in determining which values should be returned by the collection. */
  filter?: OrgPermissionFilter;
}

export interface OrgPermissionsGetByMaskQueryResult {
  orgPermissionsGetByMask: OrgPermissionsConnection;
}

/** Query key factory for caching */
export const orgPermissionsGetByMaskQueryKey = (
  variables?: OrgPermissionsGetByMaskQueryVariables
) => ['orgPermissionsGetByMask', variables] as const;

/**
 * Reads and enables pagination through a set of `OrgPermission`.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgPermissionsGetByMaskQuery({ mask, first, last, offset, before, after, filter });
 *
 * if (data?.orgPermissionsGetByMask) {
 *   console.log(data.orgPermissionsGetByMask);
 * }
 * ```
 */
export function useOrgPermissionsGetByMaskQuery(
  variables?: OrgPermissionsGetByMaskQueryVariables,
  options?: Omit<
    UseQueryOptions<OrgPermissionsGetByMaskQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: orgPermissionsGetByMaskQueryKey(variables),
    queryFn: () =>
      execute<
        OrgPermissionsGetByMaskQueryResult,
        OrgPermissionsGetByMaskQueryVariables
      >(orgPermissionsGetByMaskQueryDocument, variables),

    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch orgPermissionsGetByMask without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchOrgPermissionsGetByMaskQuery({ mask, first, last, offset, before, after, filter });
 * ```
 */
export async function fetchOrgPermissionsGetByMaskQuery(
  variables?: OrgPermissionsGetByMaskQueryVariables,
  options?: ExecuteOptions
): Promise<OrgPermissionsGetByMaskQueryResult> {
  return execute<
    OrgPermissionsGetByMaskQueryResult,
    OrgPermissionsGetByMaskQueryVariables
  >(orgPermissionsGetByMaskQueryDocument, variables, options);
}

/**
 * Prefetch orgPermissionsGetByMask for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchOrgPermissionsGetByMaskQuery(queryClient, { mask, first, last, offset, before, after, filter });
 * ```
 */
export async function prefetchOrgPermissionsGetByMaskQuery(
  queryClient: QueryClient,
  variables?: OrgPermissionsGetByMaskQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: orgPermissionsGetByMaskQueryKey(variables),
    queryFn: () =>
      execute<
        OrgPermissionsGetByMaskQueryResult,
        OrgPermissionsGetByMaskQueryVariables
      >(orgPermissionsGetByMaskQueryDocument, variables, options),
  });
}
