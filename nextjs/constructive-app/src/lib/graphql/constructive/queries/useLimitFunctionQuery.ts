/**
 * Single item query hook for LimitFunction
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type LimitFunction } from '../types';

// Re-export entity type for convenience
export type { LimitFunction };

// ============================================================================
// GraphQL Document
// ============================================================================
export const limitFunctionQueryDocument = `
query LimitFunctionQuery($id: UUID!) {
  limitFunction(id: $id) {
    id
    databaseId
    tableId
    name
    label
    description
    data
    security
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface LimitFunctionQueryVariables {
  id: string;
}

export interface LimitFunctionQueryResult {
  limitFunction: LimitFunction | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const limitFunctionQueryKey = (id: string) =>
  ['limitfunction', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single LimitFunction by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useLimitFunctionQuery('value-here');
 *
 * if (data?.limitFunction) {
 *   console.log(data.limitFunction.id);
 * }
 * ```
 */
export function useLimitFunctionQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<LimitFunctionQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: limitFunctionQueryKey(id),
    queryFn: () =>
      execute<LimitFunctionQueryResult, LimitFunctionQueryVariables>(
        limitFunctionQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single LimitFunction by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchLimitFunctionQuery('value-here');
 * ```
 */
export async function fetchLimitFunctionQuery(
  id: string,
  options?: ExecuteOptions
): Promise<LimitFunctionQueryResult> {
  return execute<LimitFunctionQueryResult, LimitFunctionQueryVariables>(
    limitFunctionQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single LimitFunction for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchLimitFunctionQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchLimitFunctionQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: limitFunctionQueryKey(id),
    queryFn: () =>
      execute<LimitFunctionQueryResult, LimitFunctionQueryVariables>(
        limitFunctionQueryDocument,
        { id },
        options
      ),
  });
}
