/**
 * Single item query hook for FullTextSearch
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type FullTextSearch } from '../types';

// Re-export entity type for convenience
export type { FullTextSearch };

// ============================================================================
// GraphQL Document
// ============================================================================
export const fullTextSearchQueryDocument = `
query FullTextSearchQuery($id: UUID!) {
  fullTextSearch(id: $id) {
    id
    databaseId
    tableId
    fieldId
    fieldIds
    weights
    langs
    createdAt
    updatedAt
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface FullTextSearchQueryVariables {
  id: string;
}

export interface FullTextSearchQueryResult {
  fullTextSearch: FullTextSearch | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const fullTextSearchQueryKey = (id: string) =>
  ['fulltextsearch', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single FullTextSearch by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useFullTextSearchQuery('value-here');
 *
 * if (data?.fullTextSearch) {
 *   console.log(data.fullTextSearch.id);
 * }
 * ```
 */
export function useFullTextSearchQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<FullTextSearchQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: fullTextSearchQueryKey(id),
    queryFn: () =>
      execute<FullTextSearchQueryResult, FullTextSearchQueryVariables>(
        fullTextSearchQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single FullTextSearch by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchFullTextSearchQuery('value-here');
 * ```
 */
export async function fetchFullTextSearchQuery(
  id: string,
  options?: ExecuteOptions
): Promise<FullTextSearchQueryResult> {
  return execute<FullTextSearchQueryResult, FullTextSearchQueryVariables>(
    fullTextSearchQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single FullTextSearch for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchFullTextSearchQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchFullTextSearchQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: fullTextSearchQueryKey(id),
    queryFn: () =>
      execute<FullTextSearchQueryResult, FullTextSearchQueryVariables>(
        fullTextSearchQueryDocument,
        { id },
        options
      ),
  });
}
