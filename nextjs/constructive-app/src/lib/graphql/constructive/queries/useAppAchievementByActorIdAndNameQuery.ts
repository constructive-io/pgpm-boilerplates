/**
 * Custom query hook for appAchievementByActorIdAndName
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type AppAchievement } from '../types';

/** GraphQL query document */
export const appAchievementByActorIdAndNameQueryDocument = `
query AppAchievementByActorIdAndNameQuery($actorId: UUID!, $name: String!) {
  appAchievementByActorIdAndName(actorId: $actorId, name: $name) {
    id
    actorId
    name
    count
    createdAt
    updatedAt
    actor {
      id
      username
      displayName
      profilePicture
      searchTsv
      type
      createdAt
      updatedAt
      searchTsvRank
    }
  }
}
`;

export interface AppAchievementByActorIdAndNameQueryVariables {
  actorId: string;
  name: string;
}

export interface AppAchievementByActorIdAndNameQueryResult {
  appAchievementByActorIdAndName: AppAchievement;
}

/** Query key factory for caching */
export const appAchievementByActorIdAndNameQueryKey = (
  variables?: AppAchievementByActorIdAndNameQueryVariables
) => ['appAchievementByActorIdAndName', variables] as const;

/**
 * Query hook for appAchievementByActorIdAndName
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppAchievementByActorIdAndNameQuery({ actorId, name });
 *
 * if (data?.appAchievementByActorIdAndName) {
 *   console.log(data.appAchievementByActorIdAndName);
 * }
 * ```
 */
export function useAppAchievementByActorIdAndNameQuery(
  variables: AppAchievementByActorIdAndNameQueryVariables,
  options?: Omit<
    UseQueryOptions<AppAchievementByActorIdAndNameQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: appAchievementByActorIdAndNameQueryKey(variables),
    queryFn: () =>
      execute<
        AppAchievementByActorIdAndNameQueryResult,
        AppAchievementByActorIdAndNameQueryVariables
      >(appAchievementByActorIdAndNameQueryDocument, variables),
    enabled: !!variables && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch appAchievementByActorIdAndName without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchAppAchievementByActorIdAndNameQuery({ actorId, name });
 * ```
 */
export async function fetchAppAchievementByActorIdAndNameQuery(
  variables: AppAchievementByActorIdAndNameQueryVariables,
  options?: ExecuteOptions
): Promise<AppAchievementByActorIdAndNameQueryResult> {
  return execute<
    AppAchievementByActorIdAndNameQueryResult,
    AppAchievementByActorIdAndNameQueryVariables
  >(appAchievementByActorIdAndNameQueryDocument, variables, options);
}

/**
 * Prefetch appAchievementByActorIdAndName for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchAppAchievementByActorIdAndNameQuery(queryClient, { actorId, name });
 * ```
 */
export async function prefetchAppAchievementByActorIdAndNameQuery(
  queryClient: QueryClient,
  variables: AppAchievementByActorIdAndNameQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: appAchievementByActorIdAndNameQueryKey(variables),
    queryFn: () =>
      execute<
        AppAchievementByActorIdAndNameQueryResult,
        AppAchievementByActorIdAndNameQueryVariables
      >(appAchievementByActorIdAndNameQueryDocument, variables, options),
  });
}
