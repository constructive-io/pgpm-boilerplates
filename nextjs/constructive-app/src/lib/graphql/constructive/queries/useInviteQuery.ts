/**
 * Single item query hook for Invite
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type Invite } from '../types';

// Re-export entity type for convenience
export type { Invite };

// ============================================================================
// GraphQL Document
// ============================================================================
export const inviteQueryDocument = `
query InviteQuery($id: UUID!) {
  invite(id: $id) {
    id
    email
    senderId
    inviteToken
    inviteValid
    inviteLimit
    inviteCount
    multiple
    data
    expiresAt
    createdAt
    updatedAt
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface InviteQueryVariables {
  id: string;
}

export interface InviteQueryResult {
  invite: Invite | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const inviteQueryKey = (id: string) => ['invite', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single Invite by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useInviteQuery('value-here');
 *
 * if (data?.invite) {
 *   console.log(data.invite.id);
 * }
 * ```
 */
export function useInviteQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<InviteQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: inviteQueryKey(id),
    queryFn: () =>
      execute<InviteQueryResult, InviteQueryVariables>(inviteQueryDocument, {
        id,
      }),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single Invite by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchInviteQuery('value-here');
 * ```
 */
export async function fetchInviteQuery(
  id: string,
  options?: ExecuteOptions
): Promise<InviteQueryResult> {
  return execute<InviteQueryResult, InviteQueryVariables>(
    inviteQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single Invite for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchInviteQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchInviteQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: inviteQueryKey(id),
    queryFn: () =>
      execute<InviteQueryResult, InviteQueryVariables>(
        inviteQueryDocument,
        { id },
        options
      ),
  });
}
