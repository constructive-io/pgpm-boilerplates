/**
 * Single item query hook for Store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type Store } from '../types';

// Re-export entity type for convenience
export type { Store };

// ============================================================================
// GraphQL Document
// ============================================================================
export const storeQueryDocument = `
query StoreQuery($id: UUID!) {
  store(id: $id) {
    id
    name
    databaseId
    hash
    createdAt
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface StoreQueryVariables {
  id: string;
}

export interface StoreQueryResult {
  store: Store | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const storeQueryKey = (id: string) => ['store', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single Store by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useStoreQuery('value-here');
 *
 * if (data?.store) {
 *   console.log(data.store.id);
 * }
 * ```
 */
export function useStoreQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<StoreQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: storeQueryKey(id),
    queryFn: () =>
      execute<StoreQueryResult, StoreQueryVariables>(storeQueryDocument, {
        id,
      }),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single Store by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchStoreQuery('value-here');
 * ```
 */
export async function fetchStoreQuery(
  id: string,
  options?: ExecuteOptions
): Promise<StoreQueryResult> {
  return execute<StoreQueryResult, StoreQueryVariables>(
    storeQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single Store for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchStoreQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchStoreQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: storeQueryKey(id),
    queryFn: () =>
      execute<StoreQueryResult, StoreQueryVariables>(
        storeQueryDocument,
        { id },
        options
      ),
  });
}
