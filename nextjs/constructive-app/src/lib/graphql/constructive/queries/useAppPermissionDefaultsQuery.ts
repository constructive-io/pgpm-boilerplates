/**
 * List query hook for AppPermissionDefault
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type AppPermissionDefault,
  type UUIDFilter,
  type BitStringFilter,
} from '../types';

// Re-export entity type for convenience
export type { AppPermissionDefault };

// ============================================================================
// GraphQL Document
// ============================================================================
export const appPermissionDefaultsQueryDocument = `
query AppPermissionDefaultsQuery($first: Int, $offset: Int, $filter: AppPermissionDefaultFilter, $orderBy: [AppPermissionDefaultsOrderBy!]) {
  appPermissionDefaults(
    first: $first
    offset: $offset
    filter: $filter
    orderBy: $orderBy
  ) {
    totalCount
    nodes {
      id
      permissions
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface AppPermissionDefaultFilter {
  id?: UUIDFilter;
  permissions?: BitStringFilter;
  /** Logical AND */
  and?: AppPermissionDefaultFilter[];
  /** Logical OR */
  or?: AppPermissionDefaultFilter[];
  /** Logical NOT */
  not?: AppPermissionDefaultFilter;
}

type AppPermissionDefaultsOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'PERMISSIONS_ASC'
  | 'PERMISSIONS_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface AppPermissionDefaultsQueryVariables {
  first?: number;
  offset?: number;
  filter?: AppPermissionDefaultFilter;
  orderBy?: AppPermissionDefaultsOrderBy[];
}

export interface AppPermissionDefaultsQueryResult {
  appPermissionDefaults: {
    totalCount: number;
    nodes: AppPermissionDefault[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const appPermissionDefaultsQueryKey = (
  variables?: AppPermissionDefaultsQueryVariables
) => ['apppermissiondefault', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching AppPermissionDefault list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppPermissionDefaultsQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useAppPermissionDefaultsQuery(
  variables?: AppPermissionDefaultsQueryVariables,
  options?: Omit<
    UseQueryOptions<AppPermissionDefaultsQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: appPermissionDefaultsQueryKey(variables),
    queryFn: () =>
      execute<
        AppPermissionDefaultsQueryResult,
        AppPermissionDefaultsQueryVariables
      >(appPermissionDefaultsQueryDocument, variables),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch AppPermissionDefault list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchAppPermissionDefaultsQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: appPermissionDefaultsQueryKey(variables),
 *   queryFn: () => fetchAppPermissionDefaultsQuery(variables),
 * });
 * ```
 */
export async function fetchAppPermissionDefaultsQuery(
  variables?: AppPermissionDefaultsQueryVariables,
  options?: ExecuteOptions
): Promise<AppPermissionDefaultsQueryResult> {
  return execute<
    AppPermissionDefaultsQueryResult,
    AppPermissionDefaultsQueryVariables
  >(appPermissionDefaultsQueryDocument, variables, options);
}

/**
 * Prefetch AppPermissionDefault list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchAppPermissionDefaultsQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchAppPermissionDefaultsQuery(
  queryClient: QueryClient,
  variables?: AppPermissionDefaultsQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: appPermissionDefaultsQueryKey(variables),
    queryFn: () =>
      execute<
        AppPermissionDefaultsQueryResult,
        AppPermissionDefaultsQueryVariables
      >(appPermissionDefaultsQueryDocument, variables, options),
  });
}
