/**
 * List query hook for OrgLimitDefault
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type OrgLimitDefault,
  type UUIDFilter,
  type StringFilter,
  type IntFilter,
} from '../types';

// Re-export entity type for convenience
export type { OrgLimitDefault };

// ============================================================================
// GraphQL Document
// ============================================================================
export const orgLimitDefaultsQueryDocument = `
query OrgLimitDefaultsQuery($first: Int, $offset: Int, $filter: OrgLimitDefaultFilter, $orderBy: [OrgLimitDefaultsOrderBy!]) {
  orgLimitDefaults(
    first: $first
    offset: $offset
    filter: $filter
    orderBy: $orderBy
  ) {
    totalCount
    nodes {
      id
      name
      max
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface OrgLimitDefaultFilter {
  id?: UUIDFilter;
  name?: StringFilter;
  max?: IntFilter;
  /** Logical AND */
  and?: OrgLimitDefaultFilter[];
  /** Logical OR */
  or?: OrgLimitDefaultFilter[];
  /** Logical NOT */
  not?: OrgLimitDefaultFilter;
}

type OrgLimitDefaultsOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'NAME_ASC'
  | 'NAME_DESC'
  | 'MAX_ASC'
  | 'MAX_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface OrgLimitDefaultsQueryVariables {
  first?: number;
  offset?: number;
  filter?: OrgLimitDefaultFilter;
  orderBy?: OrgLimitDefaultsOrderBy[];
}

export interface OrgLimitDefaultsQueryResult {
  orgLimitDefaults: {
    totalCount: number;
    nodes: OrgLimitDefault[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const orgLimitDefaultsQueryKey = (
  variables?: OrgLimitDefaultsQueryVariables
) => ['orglimitdefault', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching OrgLimitDefault list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgLimitDefaultsQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useOrgLimitDefaultsQuery(
  variables?: OrgLimitDefaultsQueryVariables,
  options?: Omit<
    UseQueryOptions<OrgLimitDefaultsQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: orgLimitDefaultsQueryKey(variables),
    queryFn: () =>
      execute<OrgLimitDefaultsQueryResult, OrgLimitDefaultsQueryVariables>(
        orgLimitDefaultsQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch OrgLimitDefault list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchOrgLimitDefaultsQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: orgLimitDefaultsQueryKey(variables),
 *   queryFn: () => fetchOrgLimitDefaultsQuery(variables),
 * });
 * ```
 */
export async function fetchOrgLimitDefaultsQuery(
  variables?: OrgLimitDefaultsQueryVariables,
  options?: ExecuteOptions
): Promise<OrgLimitDefaultsQueryResult> {
  return execute<OrgLimitDefaultsQueryResult, OrgLimitDefaultsQueryVariables>(
    orgLimitDefaultsQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch OrgLimitDefault list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchOrgLimitDefaultsQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchOrgLimitDefaultsQuery(
  queryClient: QueryClient,
  variables?: OrgLimitDefaultsQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: orgLimitDefaultsQueryKey(variables),
    queryFn: () =>
      execute<OrgLimitDefaultsQueryResult, OrgLimitDefaultsQueryVariables>(
        orgLimitDefaultsQueryDocument,
        variables,
        options
      ),
  });
}
