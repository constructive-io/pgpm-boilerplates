/**
 * List query hook for ClaimedInvite
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type ClaimedInvite,
  type UUIDFilter,
  type JSONFilter,
  type DatetimeFilter,
} from '../types';

// Re-export entity type for convenience
export type { ClaimedInvite };

// ============================================================================
// GraphQL Document
// ============================================================================
export const claimedInvitesQueryDocument = `
query ClaimedInvitesQuery($first: Int, $offset: Int, $filter: ClaimedInviteFilter, $orderBy: [ClaimedInvitesOrderBy!]) {
  claimedInvites(
    first: $first
    offset: $offset
    filter: $filter
    orderBy: $orderBy
  ) {
    totalCount
    nodes {
      id
      data
      senderId
      receiverId
      createdAt
      updatedAt
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface ClaimedInviteFilter {
  id?: UUIDFilter;
  data?: JSONFilter;
  senderId?: UUIDFilter;
  receiverId?: UUIDFilter;
  createdAt?: DatetimeFilter;
  updatedAt?: DatetimeFilter;
  /** Logical AND */
  and?: ClaimedInviteFilter[];
  /** Logical OR */
  or?: ClaimedInviteFilter[];
  /** Logical NOT */
  not?: ClaimedInviteFilter;
}

type ClaimedInvitesOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'DATA_ASC'
  | 'DATA_DESC'
  | 'SENDER_ID_ASC'
  | 'SENDER_ID_DESC'
  | 'RECEIVER_ID_ASC'
  | 'RECEIVER_ID_DESC'
  | 'CREATED_AT_ASC'
  | 'CREATED_AT_DESC'
  | 'UPDATED_AT_ASC'
  | 'UPDATED_AT_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface ClaimedInvitesQueryVariables {
  first?: number;
  offset?: number;
  filter?: ClaimedInviteFilter;
  orderBy?: ClaimedInvitesOrderBy[];
}

export interface ClaimedInvitesQueryResult {
  claimedInvites: {
    totalCount: number;
    nodes: ClaimedInvite[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const claimedInvitesQueryKey = (
  variables?: ClaimedInvitesQueryVariables
) => ['claimedinvite', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching ClaimedInvite list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useClaimedInvitesQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useClaimedInvitesQuery(
  variables?: ClaimedInvitesQueryVariables,
  options?: Omit<
    UseQueryOptions<ClaimedInvitesQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: claimedInvitesQueryKey(variables),
    queryFn: () =>
      execute<ClaimedInvitesQueryResult, ClaimedInvitesQueryVariables>(
        claimedInvitesQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch ClaimedInvite list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchClaimedInvitesQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: claimedInvitesQueryKey(variables),
 *   queryFn: () => fetchClaimedInvitesQuery(variables),
 * });
 * ```
 */
export async function fetchClaimedInvitesQuery(
  variables?: ClaimedInvitesQueryVariables,
  options?: ExecuteOptions
): Promise<ClaimedInvitesQueryResult> {
  return execute<ClaimedInvitesQueryResult, ClaimedInvitesQueryVariables>(
    claimedInvitesQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch ClaimedInvite list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchClaimedInvitesQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchClaimedInvitesQuery(
  queryClient: QueryClient,
  variables?: ClaimedInvitesQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: claimedInvitesQueryKey(variables),
    queryFn: () =>
      execute<ClaimedInvitesQueryResult, ClaimedInvitesQueryVariables>(
        claimedInvitesQueryDocument,
        variables,
        options
      ),
  });
}
