/**
 * Single item query hook for OrganizationSetting
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type OrganizationSetting } from '../types';

// Re-export entity type for convenience
export type { OrganizationSetting };

// ============================================================================
// GraphQL Document
// ============================================================================
export const organizationSettingQueryDocument = `
query OrganizationSettingQuery($id: UUID!) {
  organizationSetting(id: $id) {
    id
    ownerId
    legalName
    addressLineOne
    addressLineTwo
    state
    city
    createdAt
    updatedAt
    createdBy
    updatedBy
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface OrganizationSettingQueryVariables {
  id: string;
}

export interface OrganizationSettingQueryResult {
  organizationSetting: OrganizationSetting | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const organizationSettingQueryKey = (id: string) =>
  ['organizationsetting', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single OrganizationSetting by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrganizationSettingQuery('value-here');
 *
 * if (data?.organizationSetting) {
 *   console.log(data.organizationSetting.id);
 * }
 * ```
 */
export function useOrganizationSettingQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<OrganizationSettingQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: organizationSettingQueryKey(id),
    queryFn: () =>
      execute<
        OrganizationSettingQueryResult,
        OrganizationSettingQueryVariables
      >(organizationSettingQueryDocument, { id }),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single OrganizationSetting by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchOrganizationSettingQuery('value-here');
 * ```
 */
export async function fetchOrganizationSettingQuery(
  id: string,
  options?: ExecuteOptions
): Promise<OrganizationSettingQueryResult> {
  return execute<
    OrganizationSettingQueryResult,
    OrganizationSettingQueryVariables
  >(organizationSettingQueryDocument, { id }, options);
}

/**
 * Prefetch a single OrganizationSetting for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchOrganizationSettingQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchOrganizationSettingQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: organizationSettingQueryKey(id),
    queryFn: () =>
      execute<
        OrganizationSettingQueryResult,
        OrganizationSettingQueryVariables
      >(organizationSettingQueryDocument, { id }, options),
  });
}
