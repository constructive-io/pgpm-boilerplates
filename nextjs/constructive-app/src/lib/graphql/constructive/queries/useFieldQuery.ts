/**
 * Single item query hook for Field
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type Field } from '../types';

// Re-export entity type for convenience
export type { Field };

// ============================================================================
// GraphQL Document
// ============================================================================
export const fieldQueryDocument = `
query FieldQuery($id: UUID!) {
  field(id: $id) {
    id
    databaseId
    tableId
    name
    label
    description
    smartTags
    isRequired
    defaultValue
    defaultValueAst
    isHidden
    type
    fieldOrder
    regexp
    chk
    chkExpr
    min
    max
    tags
    category
    module
    scope
    createdAt
    updatedAt
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface FieldQueryVariables {
  id: string;
}

export interface FieldQueryResult {
  field: Field | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const fieldQueryKey = (id: string) => ['field', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single Field by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useFieldQuery('value-here');
 *
 * if (data?.field) {
 *   console.log(data.field.id);
 * }
 * ```
 */
export function useFieldQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<FieldQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: fieldQueryKey(id),
    queryFn: () =>
      execute<FieldQueryResult, FieldQueryVariables>(fieldQueryDocument, {
        id,
      }),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single Field by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchFieldQuery('value-here');
 * ```
 */
export async function fetchFieldQuery(
  id: string,
  options?: ExecuteOptions
): Promise<FieldQueryResult> {
  return execute<FieldQueryResult, FieldQueryVariables>(
    fieldQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single Field for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchFieldQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchFieldQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: fieldQueryKey(id),
    queryFn: () =>
      execute<FieldQueryResult, FieldQueryVariables>(
        fieldQueryDocument,
        { id },
        options
      ),
  });
}
