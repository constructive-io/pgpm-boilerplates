/**
 * Single item query hook for App
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type App } from '../types';

// Re-export entity type for convenience
export type { App };

// ============================================================================
// GraphQL Document
// ============================================================================
export const appQueryDocument = `
query AppQuery($id: UUID!) {
  app(id: $id) {
    id
    databaseId
    siteId
    name
    appImage
    appStoreLink
    appStoreId
    appIdPrefix
    playStoreLink
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface AppQueryVariables {
  id: string;
}

export interface AppQueryResult {
  app: App | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const appQueryKey = (id: string) => ['app', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single App by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppQuery('value-here');
 *
 * if (data?.app) {
 *   console.log(data.app.id);
 * }
 * ```
 */
export function useAppQuery(
  id: string,
  options?: Omit<UseQueryOptions<AppQueryResult, Error>, 'queryKey' | 'queryFn'>
) {
  return useQuery({
    queryKey: appQueryKey(id),
    queryFn: () =>
      execute<AppQueryResult, AppQueryVariables>(appQueryDocument, { id }),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single App by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchAppQuery('value-here');
 * ```
 */
export async function fetchAppQuery(
  id: string,
  options?: ExecuteOptions
): Promise<AppQueryResult> {
  return execute<AppQueryResult, AppQueryVariables>(
    appQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single App for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchAppQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchAppQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: appQueryKey(id),
    queryFn: () =>
      execute<AppQueryResult, AppQueryVariables>(
        appQueryDocument,
        { id },
        options
      ),
  });
}
