/**
 * Single item query hook for TableGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type TableGrant } from '../types';

// Re-export entity type for convenience
export type { TableGrant };

// ============================================================================
// GraphQL Document
// ============================================================================
export const tableGrantQueryDocument = `
query TableGrantQuery($id: UUID!) {
  tableGrant(id: $id) {
    id
    databaseId
    tableId
    privilege
    roleName
    fieldIds
    createdAt
    updatedAt
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface TableGrantQueryVariables {
  id: string;
}

export interface TableGrantQueryResult {
  tableGrant: TableGrant | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const tableGrantQueryKey = (id: string) =>
  ['tablegrant', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single TableGrant by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useTableGrantQuery('value-here');
 *
 * if (data?.tableGrant) {
 *   console.log(data.tableGrant.id);
 * }
 * ```
 */
export function useTableGrantQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<TableGrantQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: tableGrantQueryKey(id),
    queryFn: () =>
      execute<TableGrantQueryResult, TableGrantQueryVariables>(
        tableGrantQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single TableGrant by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchTableGrantQuery('value-here');
 * ```
 */
export async function fetchTableGrantQuery(
  id: string,
  options?: ExecuteOptions
): Promise<TableGrantQueryResult> {
  return execute<TableGrantQueryResult, TableGrantQueryVariables>(
    tableGrantQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single TableGrant for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchTableGrantQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchTableGrantQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: tableGrantQueryKey(id),
    queryFn: () =>
      execute<TableGrantQueryResult, TableGrantQueryVariables>(
        tableGrantQueryDocument,
        { id },
        options
      ),
  });
}
