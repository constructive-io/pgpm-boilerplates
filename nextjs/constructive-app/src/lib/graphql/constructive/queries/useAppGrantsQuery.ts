/**
 * List query hook for AppGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type AppGrant,
  type UUIDFilter,
  type BitStringFilter,
  type BooleanFilter,
  type DatetimeFilter,
} from '../types';

// Re-export entity type for convenience
export type { AppGrant };

// ============================================================================
// GraphQL Document
// ============================================================================
export const appGrantsQueryDocument = `
query AppGrantsQuery($first: Int, $offset: Int, $filter: AppGrantFilter, $orderBy: [AppGrantsOrderBy!]) {
  appGrants(first: $first, offset: $offset, filter: $filter, orderBy: $orderBy) {
    totalCount
    nodes {
      id
      permissions
      isGrant
      actorId
      grantorId
      createdAt
      updatedAt
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface AppGrantFilter {
  id?: UUIDFilter;
  permissions?: BitStringFilter;
  isGrant?: BooleanFilter;
  actorId?: UUIDFilter;
  grantorId?: UUIDFilter;
  createdAt?: DatetimeFilter;
  updatedAt?: DatetimeFilter;
  /** Logical AND */
  and?: AppGrantFilter[];
  /** Logical OR */
  or?: AppGrantFilter[];
  /** Logical NOT */
  not?: AppGrantFilter;
}

type AppGrantsOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'PERMISSIONS_ASC'
  | 'PERMISSIONS_DESC'
  | 'IS_GRANT_ASC'
  | 'IS_GRANT_DESC'
  | 'ACTOR_ID_ASC'
  | 'ACTOR_ID_DESC'
  | 'GRANTOR_ID_ASC'
  | 'GRANTOR_ID_DESC'
  | 'CREATED_AT_ASC'
  | 'CREATED_AT_DESC'
  | 'UPDATED_AT_ASC'
  | 'UPDATED_AT_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface AppGrantsQueryVariables {
  first?: number;
  offset?: number;
  filter?: AppGrantFilter;
  orderBy?: AppGrantsOrderBy[];
}

export interface AppGrantsQueryResult {
  appGrants: {
    totalCount: number;
    nodes: AppGrant[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const appGrantsQueryKey = (variables?: AppGrantsQueryVariables) =>
  ['appgrant', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching AppGrant list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppGrantsQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useAppGrantsQuery(
  variables?: AppGrantsQueryVariables,
  options?: Omit<
    UseQueryOptions<AppGrantsQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: appGrantsQueryKey(variables),
    queryFn: () =>
      execute<AppGrantsQueryResult, AppGrantsQueryVariables>(
        appGrantsQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch AppGrant list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchAppGrantsQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: appGrantsQueryKey(variables),
 *   queryFn: () => fetchAppGrantsQuery(variables),
 * });
 * ```
 */
export async function fetchAppGrantsQuery(
  variables?: AppGrantsQueryVariables,
  options?: ExecuteOptions
): Promise<AppGrantsQueryResult> {
  return execute<AppGrantsQueryResult, AppGrantsQueryVariables>(
    appGrantsQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch AppGrant list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchAppGrantsQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchAppGrantsQuery(
  queryClient: QueryClient,
  variables?: AppGrantsQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: appGrantsQueryKey(variables),
    queryFn: () =>
      execute<AppGrantsQueryResult, AppGrantsQueryVariables>(
        appGrantsQueryDocument,
        variables,
        options
      ),
  });
}
