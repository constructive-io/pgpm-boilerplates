/**
 * Single item query hook for AppStep
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type AppStep } from '../types';

// Re-export entity type for convenience
export type { AppStep };

// ============================================================================
// GraphQL Document
// ============================================================================
export const appStepQueryDocument = `
query AppStepQuery($id: UUID!) {
  appStep(id: $id) {
    id
    actorId
    name
    count
    createdAt
    updatedAt
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface AppStepQueryVariables {
  id: string;
}

export interface AppStepQueryResult {
  appStep: AppStep | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const appStepQueryKey = (id: string) =>
  ['appstep', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single AppStep by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppStepQuery('value-here');
 *
 * if (data?.appStep) {
 *   console.log(data.appStep.id);
 * }
 * ```
 */
export function useAppStepQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<AppStepQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: appStepQueryKey(id),
    queryFn: () =>
      execute<AppStepQueryResult, AppStepQueryVariables>(appStepQueryDocument, {
        id,
      }),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single AppStep by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchAppStepQuery('value-here');
 * ```
 */
export async function fetchAppStepQuery(
  id: string,
  options?: ExecuteOptions
): Promise<AppStepQueryResult> {
  return execute<AppStepQueryResult, AppStepQueryVariables>(
    appStepQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single AppStep for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchAppStepQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchAppStepQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: appStepQueryKey(id),
    queryFn: () =>
      execute<AppStepQueryResult, AppStepQueryVariables>(
        appStepQueryDocument,
        { id },
        options
      ),
  });
}
