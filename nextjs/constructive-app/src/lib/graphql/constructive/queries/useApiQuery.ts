/**
 * Single item query hook for Api
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type Api } from '../types';

// Re-export entity type for convenience
export type { Api };

// ============================================================================
// GraphQL Document
// ============================================================================
export const apiQueryDocument = `
query ApiQuery($id: UUID!) {
  api(id: $id) {
    id
    databaseId
    name
    dbname
    roleName
    anonRole
    isPublic
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface ApiQueryVariables {
  id: string;
}

export interface ApiQueryResult {
  api: Api | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const apiQueryKey = (id: string) => ['api', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single Api by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useApiQuery('value-here');
 *
 * if (data?.api) {
 *   console.log(data.api.id);
 * }
 * ```
 */
export function useApiQuery(
  id: string,
  options?: Omit<UseQueryOptions<ApiQueryResult, Error>, 'queryKey' | 'queryFn'>
) {
  return useQuery({
    queryKey: apiQueryKey(id),
    queryFn: () =>
      execute<ApiQueryResult, ApiQueryVariables>(apiQueryDocument, { id }),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single Api by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchApiQuery('value-here');
 * ```
 */
export async function fetchApiQuery(
  id: string,
  options?: ExecuteOptions
): Promise<ApiQueryResult> {
  return execute<ApiQueryResult, ApiQueryVariables>(
    apiQueryDocument,
    { id },
    options
  );
}

/**
 * Prefetch a single Api for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchApiQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchApiQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: apiQueryKey(id),
    queryFn: () =>
      execute<ApiQueryResult, ApiQueryVariables>(
        apiQueryDocument,
        { id },
        options
      ),
  });
}
