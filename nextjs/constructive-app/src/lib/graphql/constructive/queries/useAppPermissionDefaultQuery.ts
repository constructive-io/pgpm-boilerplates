/**
 * Single item query hook for AppPermissionDefault
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type AppPermissionDefault } from '../types';

// Re-export entity type for convenience
export type { AppPermissionDefault };

// ============================================================================
// GraphQL Document
// ============================================================================
export const appPermissionDefaultQueryDocument = `
query AppPermissionDefaultQuery($id: UUID!) {
  appPermissionDefault(id: $id) {
    id
    permissions
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface AppPermissionDefaultQueryVariables {
  id: string;
}

export interface AppPermissionDefaultQueryResult {
  appPermissionDefault: AppPermissionDefault | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const appPermissionDefaultQueryKey = (id: string) =>
  ['apppermissiondefault', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single AppPermissionDefault by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppPermissionDefaultQuery('value-here');
 *
 * if (data?.appPermissionDefault) {
 *   console.log(data.appPermissionDefault.id);
 * }
 * ```
 */
export function useAppPermissionDefaultQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<AppPermissionDefaultQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: appPermissionDefaultQueryKey(id),
    queryFn: () =>
      execute<
        AppPermissionDefaultQueryResult,
        AppPermissionDefaultQueryVariables
      >(appPermissionDefaultQueryDocument, { id }),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single AppPermissionDefault by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchAppPermissionDefaultQuery('value-here');
 * ```
 */
export async function fetchAppPermissionDefaultQuery(
  id: string,
  options?: ExecuteOptions
): Promise<AppPermissionDefaultQueryResult> {
  return execute<
    AppPermissionDefaultQueryResult,
    AppPermissionDefaultQueryVariables
  >(appPermissionDefaultQueryDocument, { id }, options);
}

/**
 * Prefetch a single AppPermissionDefault for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchAppPermissionDefaultQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchAppPermissionDefaultQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: appPermissionDefaultQueryKey(id),
    queryFn: () =>
      execute<
        AppPermissionDefaultQueryResult,
        AppPermissionDefaultQueryVariables
      >(appPermissionDefaultQueryDocument, { id }, options),
  });
}
