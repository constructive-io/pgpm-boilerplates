/**
 * Single item query hook for AppLevelRequirement
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type AppLevelRequirement } from '../types';

// Re-export entity type for convenience
export type { AppLevelRequirement };

// ============================================================================
// GraphQL Document
// ============================================================================
export const appLevelRequirementQueryDocument = `
query AppLevelRequirementQuery($id: UUID!) {
  appLevelRequirement(id: $id) {
    id
    name
    level
    description
    requiredCount
    priority
    createdAt
    updatedAt
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface AppLevelRequirementQueryVariables {
  id: string;
}

export interface AppLevelRequirementQueryResult {
  appLevelRequirement: AppLevelRequirement | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const appLevelRequirementQueryKey = (id: string) =>
  ['applevelrequirement', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single AppLevelRequirement by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppLevelRequirementQuery('value-here');
 *
 * if (data?.appLevelRequirement) {
 *   console.log(data.appLevelRequirement.id);
 * }
 * ```
 */
export function useAppLevelRequirementQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<AppLevelRequirementQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: appLevelRequirementQueryKey(id),
    queryFn: () =>
      execute<
        AppLevelRequirementQueryResult,
        AppLevelRequirementQueryVariables
      >(appLevelRequirementQueryDocument, { id }),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single AppLevelRequirement by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchAppLevelRequirementQuery('value-here');
 * ```
 */
export async function fetchAppLevelRequirementQuery(
  id: string,
  options?: ExecuteOptions
): Promise<AppLevelRequirementQueryResult> {
  return execute<
    AppLevelRequirementQueryResult,
    AppLevelRequirementQueryVariables
  >(appLevelRequirementQueryDocument, { id }, options);
}

/**
 * Prefetch a single AppLevelRequirement for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchAppLevelRequirementQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchAppLevelRequirementQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: appLevelRequirementQueryKey(id),
    queryFn: () =>
      execute<
        AppLevelRequirementQueryResult,
        AppLevelRequirementQueryVariables
      >(appLevelRequirementQueryDocument, { id }, options),
  });
}
