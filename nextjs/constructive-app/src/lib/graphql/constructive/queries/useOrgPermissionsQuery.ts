/**
 * List query hook for OrgPermission
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type OrgPermission,
  type UUIDFilter,
  type StringFilter,
  type IntFilter,
  type BitStringFilter,
} from '../types';

// Re-export entity type for convenience
export type { OrgPermission };

// ============================================================================
// GraphQL Document
// ============================================================================
export const orgPermissionsQueryDocument = `
query OrgPermissionsQuery($first: Int, $offset: Int, $filter: OrgPermissionFilter, $orderBy: [OrgPermissionsOrderBy!]) {
  orgPermissions(
    first: $first
    offset: $offset
    filter: $filter
    orderBy: $orderBy
  ) {
    totalCount
    nodes {
      id
      name
      bitnum
      bitstr
      description
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface OrgPermissionFilter {
  id?: UUIDFilter;
  name?: StringFilter;
  bitnum?: IntFilter;
  bitstr?: BitStringFilter;
  description?: StringFilter;
  /** Logical AND */
  and?: OrgPermissionFilter[];
  /** Logical OR */
  or?: OrgPermissionFilter[];
  /** Logical NOT */
  not?: OrgPermissionFilter;
}

type OrgPermissionsOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'NAME_ASC'
  | 'NAME_DESC'
  | 'BITNUM_ASC'
  | 'BITNUM_DESC'
  | 'BITSTR_ASC'
  | 'BITSTR_DESC'
  | 'DESCRIPTION_ASC'
  | 'DESCRIPTION_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface OrgPermissionsQueryVariables {
  first?: number;
  offset?: number;
  filter?: OrgPermissionFilter;
  orderBy?: OrgPermissionsOrderBy[];
}

export interface OrgPermissionsQueryResult {
  orgPermissions: {
    totalCount: number;
    nodes: OrgPermission[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const orgPermissionsQueryKey = (
  variables?: OrgPermissionsQueryVariables
) => ['orgpermission', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching OrgPermission list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgPermissionsQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useOrgPermissionsQuery(
  variables?: OrgPermissionsQueryVariables,
  options?: Omit<
    UseQueryOptions<OrgPermissionsQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: orgPermissionsQueryKey(variables),
    queryFn: () =>
      execute<OrgPermissionsQueryResult, OrgPermissionsQueryVariables>(
        orgPermissionsQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch OrgPermission list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchOrgPermissionsQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: orgPermissionsQueryKey(variables),
 *   queryFn: () => fetchOrgPermissionsQuery(variables),
 * });
 * ```
 */
export async function fetchOrgPermissionsQuery(
  variables?: OrgPermissionsQueryVariables,
  options?: ExecuteOptions
): Promise<OrgPermissionsQueryResult> {
  return execute<OrgPermissionsQueryResult, OrgPermissionsQueryVariables>(
    orgPermissionsQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch OrgPermission list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchOrgPermissionsQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchOrgPermissionsQuery(
  queryClient: QueryClient,
  variables?: OrgPermissionsQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: orgPermissionsQueryKey(variables),
    queryFn: () =>
      execute<OrgPermissionsQueryResult, OrgPermissionsQueryVariables>(
        orgPermissionsQueryDocument,
        variables,
        options
      ),
  });
}
