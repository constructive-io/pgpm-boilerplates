/**
 * List query hook for OrgClaimedInvite
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import {
  type OrgClaimedInvite,
  type UUIDFilter,
  type JSONFilter,
  type DatetimeFilter,
} from '../types';

// Re-export entity type for convenience
export type { OrgClaimedInvite };

// ============================================================================
// GraphQL Document
// ============================================================================
export const orgClaimedInvitesQueryDocument = `
query OrgClaimedInvitesQuery($first: Int, $offset: Int, $filter: OrgClaimedInviteFilter, $orderBy: [OrgClaimedInvitesOrderBy!]) {
  orgClaimedInvites(
    first: $first
    offset: $offset
    filter: $filter
    orderBy: $orderBy
  ) {
    totalCount
    nodes {
      id
      data
      senderId
      receiverId
      createdAt
      updatedAt
      entityId
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface OrgClaimedInviteFilter {
  id?: UUIDFilter;
  data?: JSONFilter;
  senderId?: UUIDFilter;
  receiverId?: UUIDFilter;
  createdAt?: DatetimeFilter;
  updatedAt?: DatetimeFilter;
  entityId?: UUIDFilter;
  /** Logical AND */
  and?: OrgClaimedInviteFilter[];
  /** Logical OR */
  or?: OrgClaimedInviteFilter[];
  /** Logical NOT */
  not?: OrgClaimedInviteFilter;
}

type OrgClaimedInvitesOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'DATA_ASC'
  | 'DATA_DESC'
  | 'SENDER_ID_ASC'
  | 'SENDER_ID_DESC'
  | 'RECEIVER_ID_ASC'
  | 'RECEIVER_ID_DESC'
  | 'CREATED_AT_ASC'
  | 'CREATED_AT_DESC'
  | 'UPDATED_AT_ASC'
  | 'UPDATED_AT_DESC'
  | 'ENTITY_ID_ASC'
  | 'ENTITY_ID_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface OrgClaimedInvitesQueryVariables {
  first?: number;
  offset?: number;
  filter?: OrgClaimedInviteFilter;
  orderBy?: OrgClaimedInvitesOrderBy[];
}

export interface OrgClaimedInvitesQueryResult {
  orgClaimedInvites: {
    totalCount: number;
    nodes: OrgClaimedInvite[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const orgClaimedInvitesQueryKey = (
  variables?: OrgClaimedInvitesQueryVariables
) => ['orgclaimedinvite', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching OrgClaimedInvite list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgClaimedInvitesQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useOrgClaimedInvitesQuery(
  variables?: OrgClaimedInvitesQueryVariables,
  options?: Omit<
    UseQueryOptions<OrgClaimedInvitesQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: orgClaimedInvitesQueryKey(variables),
    queryFn: () =>
      execute<OrgClaimedInvitesQueryResult, OrgClaimedInvitesQueryVariables>(
        orgClaimedInvitesQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch OrgClaimedInvite list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchOrgClaimedInvitesQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: orgClaimedInvitesQueryKey(variables),
 *   queryFn: () => fetchOrgClaimedInvitesQuery(variables),
 * });
 * ```
 */
export async function fetchOrgClaimedInvitesQuery(
  variables?: OrgClaimedInvitesQueryVariables,
  options?: ExecuteOptions
): Promise<OrgClaimedInvitesQueryResult> {
  return execute<OrgClaimedInvitesQueryResult, OrgClaimedInvitesQueryVariables>(
    orgClaimedInvitesQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch OrgClaimedInvite list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchOrgClaimedInvitesQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchOrgClaimedInvitesQuery(
  queryClient: QueryClient,
  variables?: OrgClaimedInvitesQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: orgClaimedInvitesQueryKey(variables),
    queryFn: () =>
      execute<OrgClaimedInvitesQueryResult, OrgClaimedInvitesQueryVariables>(
        orgClaimedInvitesQueryDocument,
        variables,
        options
      ),
  });
}
