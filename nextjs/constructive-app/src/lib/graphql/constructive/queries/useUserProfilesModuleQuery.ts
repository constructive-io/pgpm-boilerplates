/**
 * Single item query hook for UserProfilesModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type UserProfilesModule } from '../types';

// Re-export entity type for convenience
export type { UserProfilesModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const userProfilesModuleQueryDocument = `
query UserProfilesModuleQuery($id: UUID!) {
  userProfilesModule(id: $id) {
    id
    databaseId
    schemaId
    privateSchemaId
    tableId
    ownerTableId
    tableName
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface UserProfilesModuleQueryVariables {
  id: string;
}

export interface UserProfilesModuleQueryResult {
  userProfilesModule: UserProfilesModule | null;
}

// ============================================================================
// Query Key
// ============================================================================
export const userProfilesModuleQueryKey = (id: string) =>
  ['userprofilesmodule', 'detail', id] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching a single UserProfilesModule by primary key
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useUserProfilesModuleQuery('value-here');
 *
 * if (data?.userProfilesModule) {
 *   console.log(data.userProfilesModule.id);
 * }
 * ```
 */
export function useUserProfilesModuleQuery(
  id: string,
  options?: Omit<
    UseQueryOptions<UserProfilesModuleQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: userProfilesModuleQueryKey(id),
    queryFn: () =>
      execute<UserProfilesModuleQueryResult, UserProfilesModuleQueryVariables>(
        userProfilesModuleQueryDocument,
        { id }
      ),
    enabled: !!id && options?.enabled !== false,
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch a single UserProfilesModule by primary key without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchUserProfilesModuleQuery('value-here');
 * ```
 */
export async function fetchUserProfilesModuleQuery(
  id: string,
  options?: ExecuteOptions
): Promise<UserProfilesModuleQueryResult> {
  return execute<
    UserProfilesModuleQueryResult,
    UserProfilesModuleQueryVariables
  >(userProfilesModuleQueryDocument, { id }, options);
}

/**
 * Prefetch a single UserProfilesModule for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchUserProfilesModuleQuery(queryClient, 'value-here');
 * ```
 */
export async function prefetchUserProfilesModuleQuery(
  queryClient: QueryClient,
  id: string,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: userProfilesModuleQueryKey(id),
    queryFn: () =>
      execute<UserProfilesModuleQueryResult, UserProfilesModuleQueryVariables>(
        userProfilesModuleQueryDocument,
        { id },
        options
      ),
  });
}
