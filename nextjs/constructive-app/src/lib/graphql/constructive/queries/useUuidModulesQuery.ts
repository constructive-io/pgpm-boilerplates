/**
 * List query hook for UuidModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useQuery,
  type UseQueryOptions,
  type QueryClient,
} from '@tanstack/react-query';
import { execute, type ExecuteOptions } from '../client';
import { type UuidModule, type UUIDFilter, type StringFilter } from '../types';

// Re-export entity type for convenience
export type { UuidModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const uuidModulesQueryDocument = `
query UuidModulesQuery($first: Int, $offset: Int, $filter: UuidModuleFilter, $orderBy: [UuidModulesOrderBy!]) {
  uuidModules(first: $first, offset: $offset, filter: $filter, orderBy: $orderBy) {
    totalCount
    nodes {
      id
      databaseId
      schemaId
      uuidFunction
      uuidSeed
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
interface UuidModuleFilter {
  id?: UUIDFilter;
  databaseId?: UUIDFilter;
  schemaId?: UUIDFilter;
  uuidFunction?: StringFilter;
  uuidSeed?: StringFilter;
  /** Logical AND */
  and?: UuidModuleFilter[];
  /** Logical OR */
  or?: UuidModuleFilter[];
  /** Logical NOT */
  not?: UuidModuleFilter;
}

type UuidModulesOrderBy =
  | 'ID_ASC'
  | 'ID_DESC'
  | 'DATABASE_ID_ASC'
  | 'DATABASE_ID_DESC'
  | 'SCHEMA_ID_ASC'
  | 'SCHEMA_ID_DESC'
  | 'UUID_FUNCTION_ASC'
  | 'UUID_FUNCTION_DESC'
  | 'UUID_SEED_ASC'
  | 'UUID_SEED_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC';

export interface UuidModulesQueryVariables {
  first?: number;
  offset?: number;
  filter?: UuidModuleFilter;
  orderBy?: UuidModulesOrderBy[];
}

export interface UuidModulesQueryResult {
  uuidModules: {
    totalCount: number;
    nodes: UuidModule[];
    pageInfo: {
      hasNextPage: boolean;
      hasPreviousPage: boolean;
      startCursor: string | null;
      endCursor: string | null;
    };
  };
}

// ============================================================================
// Query Key
// ============================================================================
export const uuidModulesQueryKey = (variables?: UuidModulesQueryVariables) =>
  ['uuidmodule', 'list', variables] as const;

// ============================================================================
// Hook
// ============================================================================
/**
 * Query hook for fetching UuidModule list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useUuidModulesQuery({
 *   first: 10,
 *   filter: { name: { equalTo: "example" } },
 *   orderBy: ['CREATED_AT_DESC'],
 * });
 * ```
 */
export function useUuidModulesQuery(
  variables?: UuidModulesQueryVariables,
  options?: Omit<
    UseQueryOptions<UuidModulesQueryResult, Error>,
    'queryKey' | 'queryFn'
  >
) {
  return useQuery({
    queryKey: uuidModulesQueryKey(variables),
    queryFn: () =>
      execute<UuidModulesQueryResult, UuidModulesQueryVariables>(
        uuidModulesQueryDocument,
        variables
      ),
    ...options,
  });
}

// ============================================================================
// Standalone Functions (non-React)
// ============================================================================
/**
 * Fetch UuidModule list without React hooks
 *
 * @example
 * ```ts
 * // Direct fetch
 * const data = await fetchUuidModulesQuery({ first: 10 });
 *
 * // With QueryClient
 * const data = await queryClient.fetchQuery({
 *   queryKey: uuidModulesQueryKey(variables),
 *   queryFn: () => fetchUuidModulesQuery(variables),
 * });
 * ```
 */
export async function fetchUuidModulesQuery(
  variables?: UuidModulesQueryVariables,
  options?: ExecuteOptions
): Promise<UuidModulesQueryResult> {
  return execute<UuidModulesQueryResult, UuidModulesQueryVariables>(
    uuidModulesQueryDocument,
    variables,
    options
  );
}

/**
 * Prefetch UuidModule list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchUuidModulesQuery(queryClient, { first: 10 });
 * ```
 */
export async function prefetchUuidModulesQuery(
  queryClient: QueryClient,
  variables?: UuidModulesQueryVariables,
  options?: ExecuteOptions
): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: uuidModulesQueryKey(variables),
    queryFn: () =>
      execute<UuidModulesQueryResult, UuidModulesQueryVariables>(
        uuidModulesQueryDocument,
        variables,
        options
      ),
  });
}
