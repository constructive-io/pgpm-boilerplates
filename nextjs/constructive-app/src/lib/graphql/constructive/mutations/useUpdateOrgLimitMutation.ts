/**
 * Update mutation hook for OrgLimit
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type OrgLimit } from '../types';

// Re-export entity type for convenience
export type { OrgLimit };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateOrgLimitMutationDocument = `
mutation UpdateOrgLimitMutation($input: UpdateOrgLimitInput!) {
  updateOrgLimit(input: $input) {
    orgLimit {
      id
      name
      actorId
      num
      max
      entityId
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a OrgLimit - all fields optional */
interface OrgLimitPatch {
  name?: string | null;
  actorId?: string | null;
  num?: number | null;
  max?: number | null;
  entityId?: string | null;
}

export interface UpdateOrgLimitMutationVariables {
  input: {
    id: string;
    patch: OrgLimitPatch;
  };
}

export interface UpdateOrgLimitMutationResult {
  updateOrgLimit: {
    orgLimit: OrgLimit;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a OrgLimit
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateOrgLimitMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateOrgLimitMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateOrgLimitMutationResult,
      Error,
      UpdateOrgLimitMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateOrgLimitMutationVariables) =>
      execute<UpdateOrgLimitMutationResult, UpdateOrgLimitMutationVariables>(
        updateOrgLimitMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['orglimit', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['orglimit', 'list'] });
    },
    ...options,
  });
}
