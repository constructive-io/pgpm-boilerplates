/**
 * Create mutation hook for LimitFunction
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type LimitFunction } from '../types';

// Re-export entity type for convenience
export type { LimitFunction };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createLimitFunctionMutationDocument = `
mutation CreateLimitFunctionMutation($input: CreateLimitFunctionInput!) {
  createLimitFunction(input: $input) {
    limitFunction {
      id
      databaseId
      tableId
      name
      label
      description
      data
      security
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a LimitFunction */
interface LimitFunctionCreateInput {
  databaseId?: string | null;
  tableId?: string | null;
  name?: string | null;
  label?: string | null;
  description?: string | null;
  data?: unknown | null;
  security?: number | null;
}

export interface CreateLimitFunctionMutationVariables {
  input: {
    limitFunction: LimitFunctionCreateInput;
  };
}

export interface CreateLimitFunctionMutationResult {
  createLimitFunction: {
    limitFunction: LimitFunction;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a LimitFunction
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateLimitFunctionMutation();
 *
 * mutate({
 *   input: {
 *     limitFunction: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateLimitFunctionMutation(
  options?: Omit<
    UseMutationOptions<
      CreateLimitFunctionMutationResult,
      Error,
      CreateLimitFunctionMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateLimitFunctionMutationVariables) =>
      execute<
        CreateLimitFunctionMutationResult,
        CreateLimitFunctionMutationVariables
      >(createLimitFunctionMutationDocument, variables),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['limitfunction', 'list'] });
    },
    ...options,
  });
}
