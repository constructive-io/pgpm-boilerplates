/**
 * Create mutation hook for SiteModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type SiteModule } from '../types';

// Re-export entity type for convenience
export type { SiteModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createSiteModuleMutationDocument = `
mutation CreateSiteModuleMutation($input: CreateSiteModuleInput!) {
  createSiteModule(input: $input) {
    siteModule {
      id
      databaseId
      siteId
      name
      data
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a SiteModule */
interface SiteModuleCreateInput {
  databaseId?: string | null;
  siteId?: string | null;
  name?: string | null;
  data?: unknown | null;
}

export interface CreateSiteModuleMutationVariables {
  input: {
    siteModule: SiteModuleCreateInput;
  };
}

export interface CreateSiteModuleMutationResult {
  createSiteModule: {
    siteModule: SiteModule;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a SiteModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateSiteModuleMutation();
 *
 * mutate({
 *   input: {
 *     siteModule: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateSiteModuleMutation(
  options?: Omit<
    UseMutationOptions<
      CreateSiteModuleMutationResult,
      Error,
      CreateSiteModuleMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateSiteModuleMutationVariables) =>
      execute<
        CreateSiteModuleMutationResult,
        CreateSiteModuleMutationVariables
      >(createSiteModuleMutationDocument, variables),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['sitemodule', 'list'] });
    },
    ...options,
  });
}
