/**
 * Create mutation hook for OrgOwnerGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type OrgOwnerGrant } from '../types';

// Re-export entity type for convenience
export type { OrgOwnerGrant };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createOrgOwnerGrantMutationDocument = `
mutation CreateOrgOwnerGrantMutation($input: CreateOrgOwnerGrantInput!) {
  createOrgOwnerGrant(input: $input) {
    orgOwnerGrant {
      id
      isGrant
      actorId
      entityId
      grantorId
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a OrgOwnerGrant */
interface OrgOwnerGrantCreateInput {
  isGrant?: boolean | null;
  actorId?: string | null;
  entityId?: string | null;
  grantorId?: string | null;
}

export interface CreateOrgOwnerGrantMutationVariables {
  input: {
    orgOwnerGrant: OrgOwnerGrantCreateInput;
  };
}

export interface CreateOrgOwnerGrantMutationResult {
  createOrgOwnerGrant: {
    orgOwnerGrant: OrgOwnerGrant;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a OrgOwnerGrant
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgOwnerGrantMutation();
 *
 * mutate({
 *   input: {
 *     orgOwnerGrant: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateOrgOwnerGrantMutation(
  options?: Omit<
    UseMutationOptions<
      CreateOrgOwnerGrantMutationResult,
      Error,
      CreateOrgOwnerGrantMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateOrgOwnerGrantMutationVariables) =>
      execute<
        CreateOrgOwnerGrantMutationResult,
        CreateOrgOwnerGrantMutationVariables
      >(createOrgOwnerGrantMutationDocument, variables),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['orgownergrant', 'list'] });
    },
    ...options,
  });
}
