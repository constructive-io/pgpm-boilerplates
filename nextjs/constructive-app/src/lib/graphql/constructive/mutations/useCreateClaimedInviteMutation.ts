/**
 * Create mutation hook for ClaimedInvite
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type ClaimedInvite } from '../types';

// Re-export entity type for convenience
export type { ClaimedInvite };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createClaimedInviteMutationDocument = `
mutation CreateClaimedInviteMutation($input: CreateClaimedInviteInput!) {
  createClaimedInvite(input: $input) {
    claimedInvite {
      id
      data
      senderId
      receiverId
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a ClaimedInvite */
interface ClaimedInviteCreateInput {
  data?: unknown | null;
  senderId?: string | null;
  receiverId?: string | null;
}

export interface CreateClaimedInviteMutationVariables {
  input: {
    claimedInvite: ClaimedInviteCreateInput;
  };
}

export interface CreateClaimedInviteMutationResult {
  createClaimedInvite: {
    claimedInvite: ClaimedInvite;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a ClaimedInvite
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateClaimedInviteMutation();
 *
 * mutate({
 *   input: {
 *     claimedInvite: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateClaimedInviteMutation(
  options?: Omit<
    UseMutationOptions<
      CreateClaimedInviteMutationResult,
      Error,
      CreateClaimedInviteMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateClaimedInviteMutationVariables) =>
      execute<
        CreateClaimedInviteMutationResult,
        CreateClaimedInviteMutationVariables
      >(createClaimedInviteMutationDocument, variables),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['claimedinvite', 'list'] });
    },
    ...options,
  });
}
