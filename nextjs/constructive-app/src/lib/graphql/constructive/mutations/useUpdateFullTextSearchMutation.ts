/**
 * Update mutation hook for FullTextSearch
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type FullTextSearch } from '../types';

// Re-export entity type for convenience
export type { FullTextSearch };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateFullTextSearchMutationDocument = `
mutation UpdateFullTextSearchMutation($input: UpdateFullTextSearchInput!) {
  updateFullTextSearch(input: $input) {
    fullTextSearch {
      id
      databaseId
      tableId
      fieldId
      fieldIds
      weights
      langs
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a FullTextSearch - all fields optional */
interface FullTextSearchPatch {
  databaseId?: string | null;
  tableId?: string | null;
  fieldId?: string | null;
  fieldIds?: string[] | null;
  weights?: string[] | null;
  langs?: string[] | null;
  createdAt?: string | null;
  updatedAt?: string | null;
}

export interface UpdateFullTextSearchMutationVariables {
  input: {
    id: string;
    patch: FullTextSearchPatch;
  };
}

export interface UpdateFullTextSearchMutationResult {
  updateFullTextSearch: {
    fullTextSearch: FullTextSearch;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a FullTextSearch
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateFullTextSearchMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateFullTextSearchMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateFullTextSearchMutationResult,
      Error,
      UpdateFullTextSearchMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateFullTextSearchMutationVariables) =>
      execute<
        UpdateFullTextSearchMutationResult,
        UpdateFullTextSearchMutationVariables
      >(updateFullTextSearchMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['fulltextsearch', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['fulltextsearch', 'list'] });
    },
    ...options,
  });
}
