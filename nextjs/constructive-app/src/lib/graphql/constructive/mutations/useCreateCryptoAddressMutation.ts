/**
 * Create mutation hook for CryptoAddress
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type CryptoAddress } from '../types';

// Re-export entity type for convenience
export type { CryptoAddress };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createCryptoAddressMutationDocument = `
mutation CreateCryptoAddressMutation($input: CreateCryptoAddressInput!) {
  createCryptoAddress(input: $input) {
    cryptoAddress {
      id
      ownerId
      address
      isVerified
      isPrimary
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a CryptoAddress */
interface CryptoAddressCreateInput {
  ownerId?: string | null;
  address?: string | null;
  isVerified?: boolean | null;
  isPrimary?: boolean | null;
}

export interface CreateCryptoAddressMutationVariables {
  input: {
    cryptoAddress: CryptoAddressCreateInput;
  };
}

export interface CreateCryptoAddressMutationResult {
  createCryptoAddress: {
    cryptoAddress: CryptoAddress;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a CryptoAddress
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateCryptoAddressMutation();
 *
 * mutate({
 *   input: {
 *     cryptoAddress: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateCryptoAddressMutation(
  options?: Omit<
    UseMutationOptions<
      CreateCryptoAddressMutationResult,
      Error,
      CreateCryptoAddressMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateCryptoAddressMutationVariables) =>
      execute<
        CreateCryptoAddressMutationResult,
        CreateCryptoAddressMutationVariables
      >(createCryptoAddressMutationDocument, variables),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['cryptoaddress', 'list'] });
    },
    ...options,
  });
}
