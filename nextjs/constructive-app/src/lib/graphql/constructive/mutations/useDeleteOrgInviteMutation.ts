/**
 * Delete mutation hook for OrgInvite
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deleteOrgInviteMutationDocument = `
mutation DeleteOrgInviteMutation($input: DeleteOrgInviteInput!) {
  deleteOrgInvite(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeleteOrgInviteMutationVariables {
  input: {
    id: string;
  };
}

export interface DeleteOrgInviteMutationResult {
  deleteOrgInvite: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a OrgInvite
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteOrgInviteMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeleteOrgInviteMutation(
  options?: Omit<
    UseMutationOptions<
      DeleteOrgInviteMutationResult,
      Error,
      DeleteOrgInviteMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeleteOrgInviteMutationVariables) =>
      execute<DeleteOrgInviteMutationResult, DeleteOrgInviteMutationVariables>(
        deleteOrgInviteMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['orginvite', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['orginvite', 'list'] });
    },
    ...options,
  });
}
