/**
 * Update mutation hook for AppPermission
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type AppPermission } from '../types';

// Re-export entity type for convenience
export type { AppPermission };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateAppPermissionMutationDocument = `
mutation UpdateAppPermissionMutation($input: UpdateAppPermissionInput!) {
  updateAppPermission(input: $input) {
    appPermission {
      id
      name
      bitnum
      bitstr
      description
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a AppPermission - all fields optional */
interface AppPermissionPatch {
  name?: string | null;
  bitnum?: number | null;
  bitstr?: string | null;
  description?: string | null;
}

export interface UpdateAppPermissionMutationVariables {
  input: {
    id: string;
    patch: AppPermissionPatch;
  };
}

export interface UpdateAppPermissionMutationResult {
  updateAppPermission: {
    appPermission: AppPermission;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a AppPermission
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppPermissionMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateAppPermissionMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateAppPermissionMutationResult,
      Error,
      UpdateAppPermissionMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateAppPermissionMutationVariables) =>
      execute<
        UpdateAppPermissionMutationResult,
        UpdateAppPermissionMutationVariables
      >(updateAppPermissionMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['apppermission', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['apppermission', 'list'] });
    },
    ...options,
  });
}
