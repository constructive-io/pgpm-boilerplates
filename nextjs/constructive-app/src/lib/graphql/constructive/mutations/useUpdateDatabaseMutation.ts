/**
 * Update mutation hook for Database
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Database } from '../types';

// Re-export entity type for convenience
export type { Database };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateDatabaseMutationDocument = `
mutation UpdateDatabaseMutation($input: UpdateDatabaseInput!) {
  updateDatabase(input: $input) {
    database {
      id
      ownerId
      schemaName
      privateSchemaName
      name
      label
      hash
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a Database - all fields optional */
interface DatabasePatch {
  ownerId?: string | null;
  schemaName?: string | null;
  privateSchemaName?: string | null;
  name?: string | null;
  label?: string | null;
  hash?: string | null;
  createdAt?: string | null;
  updatedAt?: string | null;
}

export interface UpdateDatabaseMutationVariables {
  input: {
    id: string;
    patch: DatabasePatch;
  };
}

export interface UpdateDatabaseMutationResult {
  updateDatabase: {
    database: Database;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a Database
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateDatabaseMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateDatabaseMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateDatabaseMutationResult,
      Error,
      UpdateDatabaseMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateDatabaseMutationVariables) =>
      execute<UpdateDatabaseMutationResult, UpdateDatabaseMutationVariables>(
        updateDatabaseMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['database', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['database', 'list'] });
    },
    ...options,
  });
}
