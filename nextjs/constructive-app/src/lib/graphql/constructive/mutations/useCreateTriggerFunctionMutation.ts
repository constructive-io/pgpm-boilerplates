/**
 * Create mutation hook for TriggerFunction
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type TriggerFunction } from '../types';

// Re-export entity type for convenience
export type { TriggerFunction };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createTriggerFunctionMutationDocument = `
mutation CreateTriggerFunctionMutation($input: CreateTriggerFunctionInput!) {
  createTriggerFunction(input: $input) {
    triggerFunction {
      id
      databaseId
      name
      code
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a TriggerFunction */
interface TriggerFunctionCreateInput {
  databaseId?: string | null;
  name?: string | null;
  code?: string | null;
}

export interface CreateTriggerFunctionMutationVariables {
  input: {
    triggerFunction: TriggerFunctionCreateInput;
  };
}

export interface CreateTriggerFunctionMutationResult {
  createTriggerFunction: {
    triggerFunction: TriggerFunction;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a TriggerFunction
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateTriggerFunctionMutation();
 *
 * mutate({
 *   input: {
 *     triggerFunction: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateTriggerFunctionMutation(
  options?: Omit<
    UseMutationOptions<
      CreateTriggerFunctionMutationResult,
      Error,
      CreateTriggerFunctionMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateTriggerFunctionMutationVariables) =>
      execute<
        CreateTriggerFunctionMutationResult,
        CreateTriggerFunctionMutationVariables
      >(createTriggerFunctionMutationDocument, variables),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['triggerfunction', 'list'] });
    },
    ...options,
  });
}
