/**
 * Delete mutation hook for OrgLimitDefault
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deleteOrgLimitDefaultMutationDocument = `
mutation DeleteOrgLimitDefaultMutation($input: DeleteOrgLimitDefaultInput!) {
  deleteOrgLimitDefault(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeleteOrgLimitDefaultMutationVariables {
  input: {
    id: string;
  };
}

export interface DeleteOrgLimitDefaultMutationResult {
  deleteOrgLimitDefault: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a OrgLimitDefault
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteOrgLimitDefaultMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeleteOrgLimitDefaultMutation(
  options?: Omit<
    UseMutationOptions<
      DeleteOrgLimitDefaultMutationResult,
      Error,
      DeleteOrgLimitDefaultMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeleteOrgLimitDefaultMutationVariables) =>
      execute<
        DeleteOrgLimitDefaultMutationResult,
        DeleteOrgLimitDefaultMutationVariables
      >(deleteOrgLimitDefaultMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['orglimitdefault', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['orglimitdefault', 'list'] });
    },
    ...options,
  });
}
