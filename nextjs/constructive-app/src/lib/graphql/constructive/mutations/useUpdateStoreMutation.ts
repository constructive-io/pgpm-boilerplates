/**
 * Update mutation hook for Store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Store } from '../types';

// Re-export entity type for convenience
export type { Store };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateStoreMutationDocument = `
mutation UpdateStoreMutation($input: UpdateStoreInput!) {
  updateStore(input: $input) {
    store {
      id
      name
      databaseId
      hash
      createdAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a Store - all fields optional */
interface StorePatch {
  name?: string | null;
  databaseId?: string | null;
  hash?: string | null;
  createdAt?: string | null;
}

export interface UpdateStoreMutationVariables {
  input: {
    id: string;
    patch: StorePatch;
  };
}

export interface UpdateStoreMutationResult {
  updateStore: {
    store: Store;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a Store
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateStoreMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateStoreMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateStoreMutationResult,
      Error,
      UpdateStoreMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateStoreMutationVariables) =>
      execute<UpdateStoreMutationResult, UpdateStoreMutationVariables>(
        updateStoreMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['store', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['store', 'list'] });
    },
    ...options,
  });
}
