/**
 * Create mutation hook for AppGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type AppGrant } from '../types';

// Re-export entity type for convenience
export type { AppGrant };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createAppGrantMutationDocument = `
mutation CreateAppGrantMutation($input: CreateAppGrantInput!) {
  createAppGrant(input: $input) {
    appGrant {
      id
      permissions
      isGrant
      actorId
      grantorId
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a AppGrant */
interface AppGrantCreateInput {
  permissions?: string | null;
  isGrant?: boolean | null;
  actorId?: string | null;
  grantorId?: string | null;
}

export interface CreateAppGrantMutationVariables {
  input: {
    appGrant: AppGrantCreateInput;
  };
}

export interface CreateAppGrantMutationResult {
  createAppGrant: {
    appGrant: AppGrant;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a AppGrant
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateAppGrantMutation();
 *
 * mutate({
 *   input: {
 *     appGrant: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateAppGrantMutation(
  options?: Omit<
    UseMutationOptions<
      CreateAppGrantMutationResult,
      Error,
      CreateAppGrantMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateAppGrantMutationVariables) =>
      execute<CreateAppGrantMutationResult, CreateAppGrantMutationVariables>(
        createAppGrantMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['appgrant', 'list'] });
    },
    ...options,
  });
}
