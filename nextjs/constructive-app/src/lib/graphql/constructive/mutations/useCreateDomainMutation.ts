/**
 * Create mutation hook for Domain
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Domain } from '../types';

// Re-export entity type for convenience
export type { Domain };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createDomainMutationDocument = `
mutation CreateDomainMutation($input: CreateDomainInput!) {
  createDomain(input: $input) {
    domain {
      id
      databaseId
      apiId
      siteId
      subdomain
      domain
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a Domain */
interface DomainCreateInput {
  databaseId?: string | null;
  apiId?: string | null;
  siteId?: string | null;
  subdomain?: string | null;
  domain?: string | null;
}

export interface CreateDomainMutationVariables {
  input: {
    domain: DomainCreateInput;
  };
}

export interface CreateDomainMutationResult {
  createDomain: {
    domain: Domain;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a Domain
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateDomainMutation();
 *
 * mutate({
 *   input: {
 *     domain: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateDomainMutation(
  options?: Omit<
    UseMutationOptions<
      CreateDomainMutationResult,
      Error,
      CreateDomainMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateDomainMutationVariables) =>
      execute<CreateDomainMutationResult, CreateDomainMutationVariables>(
        createDomainMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['domain', 'list'] });
    },
    ...options,
  });
}
