/**
 * Update mutation hook for SiteMetadatum
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type SiteMetadatum } from '../types';

// Re-export entity type for convenience
export type { SiteMetadatum };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateSiteMetadatumMutationDocument = `
mutation UpdateSiteMetadatumMutation($input: UpdateSiteMetadatumInput!) {
  updateSiteMetadatum(input: $input) {
    siteMetadatum {
      id
      databaseId
      siteId
      title
      description
      ogImage
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a SiteMetadatum - all fields optional */
interface SiteMetadatumPatch {
  databaseId?: string | null;
  siteId?: string | null;
  title?: string | null;
  description?: string | null;
  ogImage?: unknown | null;
}

export interface UpdateSiteMetadatumMutationVariables {
  input: {
    id: string;
    patch: SiteMetadatumPatch;
  };
}

export interface UpdateSiteMetadatumMutationResult {
  updateSiteMetadatum: {
    siteMetadatum: SiteMetadatum;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a SiteMetadatum
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateSiteMetadatumMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateSiteMetadatumMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateSiteMetadatumMutationResult,
      Error,
      UpdateSiteMetadatumMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateSiteMetadatumMutationVariables) =>
      execute<
        UpdateSiteMetadatumMutationResult,
        UpdateSiteMetadatumMutationVariables
      >(updateSiteMetadatumMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['sitemetadatum', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['sitemetadatum', 'list'] });
    },
    ...options,
  });
}
