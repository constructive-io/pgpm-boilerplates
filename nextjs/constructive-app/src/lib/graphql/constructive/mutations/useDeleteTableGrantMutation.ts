/**
 * Delete mutation hook for TableGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deleteTableGrantMutationDocument = `
mutation DeleteTableGrantMutation($input: DeleteTableGrantInput!) {
  deleteTableGrant(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeleteTableGrantMutationVariables {
  input: {
    id: string;
  };
}

export interface DeleteTableGrantMutationResult {
  deleteTableGrant: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a TableGrant
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteTableGrantMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeleteTableGrantMutation(
  options?: Omit<
    UseMutationOptions<
      DeleteTableGrantMutationResult,
      Error,
      DeleteTableGrantMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeleteTableGrantMutationVariables) =>
      execute<
        DeleteTableGrantMutationResult,
        DeleteTableGrantMutationVariables
      >(deleteTableGrantMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['tablegrant', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['tablegrant', 'list'] });
    },
    ...options,
  });
}
