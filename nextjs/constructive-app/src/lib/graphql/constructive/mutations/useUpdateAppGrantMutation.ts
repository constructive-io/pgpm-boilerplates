/**
 * Update mutation hook for AppGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type AppGrant } from '../types';

// Re-export entity type for convenience
export type { AppGrant };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateAppGrantMutationDocument = `
mutation UpdateAppGrantMutation($input: UpdateAppGrantInput!) {
  updateAppGrant(input: $input) {
    appGrant {
      id
      permissions
      isGrant
      actorId
      grantorId
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a AppGrant - all fields optional */
interface AppGrantPatch {
  permissions?: string | null;
  isGrant?: boolean | null;
  actorId?: string | null;
  grantorId?: string | null;
  createdAt?: string | null;
  updatedAt?: string | null;
}

export interface UpdateAppGrantMutationVariables {
  input: {
    id: string;
    patch: AppGrantPatch;
  };
}

export interface UpdateAppGrantMutationResult {
  updateAppGrant: {
    appGrant: AppGrant;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a AppGrant
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppGrantMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateAppGrantMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateAppGrantMutationResult,
      Error,
      UpdateAppGrantMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateAppGrantMutationVariables) =>
      execute<UpdateAppGrantMutationResult, UpdateAppGrantMutationVariables>(
        updateAppGrantMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['appgrant', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['appgrant', 'list'] });
    },
    ...options,
  });
}
