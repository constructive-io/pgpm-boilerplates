/**
 * Update mutation hook for Trigger
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Trigger } from '../types';
import { type ObjectCategory } from '../schema-types';

// Re-export entity type for convenience
export type { Trigger };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateTriggerMutationDocument = `
mutation UpdateTriggerMutation($input: UpdateTriggerInput!) {
  updateTrigger(input: $input) {
    trigger {
      id
      databaseId
      tableId
      name
      event
      functionName
      smartTags
      category
      module
      scope
      tags
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a Trigger - all fields optional */
interface TriggerPatch {
  databaseId?: string | null;
  tableId?: string | null;
  name?: string | null;
  event?: string | null;
  functionName?: string | null;
  smartTags?: unknown | null;
  category?: ObjectCategory | null;
  module?: string | null;
  scope?: number | null;
  tags?: string[] | null;
  createdAt?: string | null;
  updatedAt?: string | null;
}

export interface UpdateTriggerMutationVariables {
  input: {
    id: string;
    patch: TriggerPatch;
  };
}

export interface UpdateTriggerMutationResult {
  updateTrigger: {
    trigger: Trigger;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a Trigger
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateTriggerMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateTriggerMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateTriggerMutationResult,
      Error,
      UpdateTriggerMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateTriggerMutationVariables) =>
      execute<UpdateTriggerMutationResult, UpdateTriggerMutationVariables>(
        updateTriggerMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['trigger', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['trigger', 'list'] });
    },
    ...options,
  });
}
