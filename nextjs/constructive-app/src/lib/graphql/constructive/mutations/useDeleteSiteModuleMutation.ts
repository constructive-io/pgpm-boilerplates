/**
 * Delete mutation hook for SiteModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deleteSiteModuleMutationDocument = `
mutation DeleteSiteModuleMutation($input: DeleteSiteModuleInput!) {
  deleteSiteModule(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeleteSiteModuleMutationVariables {
  input: {
    id: string;
  };
}

export interface DeleteSiteModuleMutationResult {
  deleteSiteModule: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a SiteModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteSiteModuleMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeleteSiteModuleMutation(
  options?: Omit<
    UseMutationOptions<
      DeleteSiteModuleMutationResult,
      Error,
      DeleteSiteModuleMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeleteSiteModuleMutationVariables) =>
      execute<
        DeleteSiteModuleMutationResult,
        DeleteSiteModuleMutationVariables
      >(deleteSiteModuleMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['sitemodule', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['sitemodule', 'list'] });
    },
    ...options,
  });
}
