/**
 * Create mutation hook for Database
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Database } from '../types';

// Re-export entity type for convenience
export type { Database };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createDatabaseMutationDocument = `
mutation CreateDatabaseMutation($input: CreateDatabaseInput!) {
  createDatabase(input: $input) {
    database {
      id
      ownerId
      schemaName
      privateSchemaName
      name
      label
      hash
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a Database */
interface DatabaseCreateInput {
  ownerId?: string | null;
  schemaName?: string | null;
  privateSchemaName?: string | null;
  name?: string | null;
  label?: string | null;
  hash?: string | null;
}

export interface CreateDatabaseMutationVariables {
  input: {
    database: DatabaseCreateInput;
  };
}

export interface CreateDatabaseMutationResult {
  createDatabase: {
    database: Database;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a Database
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateDatabaseMutation();
 *
 * mutate({
 *   input: {
 *     database: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateDatabaseMutation(
  options?: Omit<
    UseMutationOptions<
      CreateDatabaseMutationResult,
      Error,
      CreateDatabaseMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateDatabaseMutationVariables) =>
      execute<CreateDatabaseMutationResult, CreateDatabaseMutationVariables>(
        createDatabaseMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['database', 'list'] });
    },
    ...options,
  });
}
