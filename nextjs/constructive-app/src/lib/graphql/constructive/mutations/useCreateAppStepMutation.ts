/**
 * Create mutation hook for AppStep
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type AppStep } from '../types';

// Re-export entity type for convenience
export type { AppStep };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createAppStepMutationDocument = `
mutation CreateAppStepMutation($input: CreateAppStepInput!) {
  createAppStep(input: $input) {
    appStep {
      id
      actorId
      name
      count
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a AppStep */
interface AppStepCreateInput {
  actorId?: string | null;
  name?: string | null;
  count?: number | null;
}

export interface CreateAppStepMutationVariables {
  input: {
    appStep: AppStepCreateInput;
  };
}

export interface CreateAppStepMutationResult {
  createAppStep: {
    appStep: AppStep;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a AppStep
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateAppStepMutation();
 *
 * mutate({
 *   input: {
 *     appStep: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateAppStepMutation(
  options?: Omit<
    UseMutationOptions<
      CreateAppStepMutationResult,
      Error,
      CreateAppStepMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateAppStepMutationVariables) =>
      execute<CreateAppStepMutationResult, CreateAppStepMutationVariables>(
        createAppStepMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['appstep', 'list'] });
    },
    ...options,
  });
}
