/**
 * Create mutation hook for PhoneNumber
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type PhoneNumber } from '../types';

// Re-export entity type for convenience
export type { PhoneNumber };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createPhoneNumberMutationDocument = `
mutation CreatePhoneNumberMutation($input: CreatePhoneNumberInput!) {
  createPhoneNumber(input: $input) {
    phoneNumber {
      id
      ownerId
      cc
      number
      isVerified
      isPrimary
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a PhoneNumber */
interface PhoneNumberCreateInput {
  ownerId?: string | null;
  cc?: string | null;
  number?: string | null;
  isVerified?: boolean | null;
  isPrimary?: boolean | null;
}

export interface CreatePhoneNumberMutationVariables {
  input: {
    phoneNumber: PhoneNumberCreateInput;
  };
}

export interface CreatePhoneNumberMutationResult {
  createPhoneNumber: {
    phoneNumber: PhoneNumber;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a PhoneNumber
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreatePhoneNumberMutation();
 *
 * mutate({
 *   input: {
 *     phoneNumber: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreatePhoneNumberMutation(
  options?: Omit<
    UseMutationOptions<
      CreatePhoneNumberMutationResult,
      Error,
      CreatePhoneNumberMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreatePhoneNumberMutationVariables) =>
      execute<
        CreatePhoneNumberMutationResult,
        CreatePhoneNumberMutationVariables
      >(createPhoneNumberMutationDocument, variables),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['phonenumber', 'list'] });
    },
    ...options,
  });
}
