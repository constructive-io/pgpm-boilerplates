/**
 * Update mutation hook for AppLimitDefault
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type AppLimitDefault } from '../types';

// Re-export entity type for convenience
export type { AppLimitDefault };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateAppLimitDefaultMutationDocument = `
mutation UpdateAppLimitDefaultMutation($input: UpdateAppLimitDefaultInput!) {
  updateAppLimitDefault(input: $input) {
    appLimitDefault {
      id
      name
      max
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a AppLimitDefault - all fields optional */
interface AppLimitDefaultPatch {
  name?: string | null;
  max?: number | null;
}

export interface UpdateAppLimitDefaultMutationVariables {
  input: {
    id: string;
    patch: AppLimitDefaultPatch;
  };
}

export interface UpdateAppLimitDefaultMutationResult {
  updateAppLimitDefault: {
    appLimitDefault: AppLimitDefault;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a AppLimitDefault
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppLimitDefaultMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateAppLimitDefaultMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateAppLimitDefaultMutationResult,
      Error,
      UpdateAppLimitDefaultMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateAppLimitDefaultMutationVariables) =>
      execute<
        UpdateAppLimitDefaultMutationResult,
        UpdateAppLimitDefaultMutationVariables
      >(updateAppLimitDefaultMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['applimitdefault', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['applimitdefault', 'list'] });
    },
    ...options,
  });
}
