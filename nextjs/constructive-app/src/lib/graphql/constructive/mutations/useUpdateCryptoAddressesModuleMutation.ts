/**
 * Update mutation hook for CryptoAddressesModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type CryptoAddressesModule } from '../types';

// Re-export entity type for convenience
export type { CryptoAddressesModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateCryptoAddressesModuleMutationDocument = `
mutation UpdateCryptoAddressesModuleMutation($input: UpdateCryptoAddressesModuleInput!) {
  updateCryptoAddressesModule(input: $input) {
    cryptoAddressesModule {
      id
      databaseId
      schemaId
      privateSchemaId
      tableId
      ownerTableId
      tableName
      cryptoNetwork
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a CryptoAddressesModule - all fields optional */
interface CryptoAddressesModulePatch {
  databaseId?: string | null;
  schemaId?: string | null;
  privateSchemaId?: string | null;
  tableId?: string | null;
  ownerTableId?: string | null;
  tableName?: string | null;
  cryptoNetwork?: string | null;
}

export interface UpdateCryptoAddressesModuleMutationVariables {
  input: {
    id: string;
    patch: CryptoAddressesModulePatch;
  };
}

export interface UpdateCryptoAddressesModuleMutationResult {
  updateCryptoAddressesModule: {
    cryptoAddressesModule: CryptoAddressesModule;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a CryptoAddressesModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateCryptoAddressesModuleMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateCryptoAddressesModuleMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateCryptoAddressesModuleMutationResult,
      Error,
      UpdateCryptoAddressesModuleMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateCryptoAddressesModuleMutationVariables) =>
      execute<
        UpdateCryptoAddressesModuleMutationResult,
        UpdateCryptoAddressesModuleMutationVariables
      >(updateCryptoAddressesModuleMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['cryptoaddressesmodule', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({
        queryKey: ['cryptoaddressesmodule', 'list'],
      });
    },
    ...options,
  });
}
