/**
 * Update mutation hook for EmailsModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type EmailsModule } from '../types';

// Re-export entity type for convenience
export type { EmailsModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateEmailsModuleMutationDocument = `
mutation UpdateEmailsModuleMutation($input: UpdateEmailsModuleInput!) {
  updateEmailsModule(input: $input) {
    emailsModule {
      id
      databaseId
      schemaId
      privateSchemaId
      tableId
      ownerTableId
      tableName
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a EmailsModule - all fields optional */
interface EmailsModulePatch {
  databaseId?: string | null;
  schemaId?: string | null;
  privateSchemaId?: string | null;
  tableId?: string | null;
  ownerTableId?: string | null;
  tableName?: string | null;
}

export interface UpdateEmailsModuleMutationVariables {
  input: {
    id: string;
    patch: EmailsModulePatch;
  };
}

export interface UpdateEmailsModuleMutationResult {
  updateEmailsModule: {
    emailsModule: EmailsModule;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a EmailsModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateEmailsModuleMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateEmailsModuleMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateEmailsModuleMutationResult,
      Error,
      UpdateEmailsModuleMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateEmailsModuleMutationVariables) =>
      execute<
        UpdateEmailsModuleMutationResult,
        UpdateEmailsModuleMutationVariables
      >(updateEmailsModuleMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['emailsmodule', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['emailsmodule', 'list'] });
    },
    ...options,
  });
}
