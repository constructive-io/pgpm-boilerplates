/**
 * Update mutation hook for Commit
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Commit } from '../types';

// Re-export entity type for convenience
export type { Commit };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateCommitMutationDocument = `
mutation UpdateCommitMutation($input: UpdateCommitInput!) {
  updateCommit(input: $input) {
    commit {
      id
      message
      databaseId
      storeId
      parentIds
      authorId
      committerId
      treeId
      date
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a Commit - all fields optional */
interface CommitPatch {
  message?: string | null;
  databaseId?: string | null;
  storeId?: string | null;
  parentIds?: string[] | null;
  authorId?: string | null;
  committerId?: string | null;
  treeId?: string | null;
  date?: string | null;
}

export interface UpdateCommitMutationVariables {
  input: {
    id: string;
    patch: CommitPatch;
  };
}

export interface UpdateCommitMutationResult {
  updateCommit: {
    commit: Commit;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a Commit
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateCommitMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateCommitMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateCommitMutationResult,
      Error,
      UpdateCommitMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateCommitMutationVariables) =>
      execute<UpdateCommitMutationResult, UpdateCommitMutationVariables>(
        updateCommitMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['commit', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['commit', 'list'] });
    },
    ...options,
  });
}
