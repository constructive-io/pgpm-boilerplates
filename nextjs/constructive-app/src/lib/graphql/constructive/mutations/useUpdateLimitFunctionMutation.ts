/**
 * Update mutation hook for LimitFunction
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type LimitFunction } from '../types';

// Re-export entity type for convenience
export type { LimitFunction };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateLimitFunctionMutationDocument = `
mutation UpdateLimitFunctionMutation($input: UpdateLimitFunctionInput!) {
  updateLimitFunction(input: $input) {
    limitFunction {
      id
      databaseId
      tableId
      name
      label
      description
      data
      security
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a LimitFunction - all fields optional */
interface LimitFunctionPatch {
  databaseId?: string | null;
  tableId?: string | null;
  name?: string | null;
  label?: string | null;
  description?: string | null;
  data?: unknown | null;
  security?: number | null;
}

export interface UpdateLimitFunctionMutationVariables {
  input: {
    id: string;
    patch: LimitFunctionPatch;
  };
}

export interface UpdateLimitFunctionMutationResult {
  updateLimitFunction: {
    limitFunction: LimitFunction;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a LimitFunction
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateLimitFunctionMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateLimitFunctionMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateLimitFunctionMutationResult,
      Error,
      UpdateLimitFunctionMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateLimitFunctionMutationVariables) =>
      execute<
        UpdateLimitFunctionMutationResult,
        UpdateLimitFunctionMutationVariables
      >(updateLimitFunctionMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['limitfunction', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['limitfunction', 'list'] });
    },
    ...options,
  });
}
