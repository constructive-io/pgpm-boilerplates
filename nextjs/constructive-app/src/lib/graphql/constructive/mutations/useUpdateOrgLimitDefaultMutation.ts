/**
 * Update mutation hook for OrgLimitDefault
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type OrgLimitDefault } from '../types';

// Re-export entity type for convenience
export type { OrgLimitDefault };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateOrgLimitDefaultMutationDocument = `
mutation UpdateOrgLimitDefaultMutation($input: UpdateOrgLimitDefaultInput!) {
  updateOrgLimitDefault(input: $input) {
    orgLimitDefault {
      id
      name
      max
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a OrgLimitDefault - all fields optional */
interface OrgLimitDefaultPatch {
  name?: string | null;
  max?: number | null;
}

export interface UpdateOrgLimitDefaultMutationVariables {
  input: {
    id: string;
    patch: OrgLimitDefaultPatch;
  };
}

export interface UpdateOrgLimitDefaultMutationResult {
  updateOrgLimitDefault: {
    orgLimitDefault: OrgLimitDefault;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a OrgLimitDefault
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateOrgLimitDefaultMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateOrgLimitDefaultMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateOrgLimitDefaultMutationResult,
      Error,
      UpdateOrgLimitDefaultMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateOrgLimitDefaultMutationVariables) =>
      execute<
        UpdateOrgLimitDefaultMutationResult,
        UpdateOrgLimitDefaultMutationVariables
      >(updateOrgLimitDefaultMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['orglimitdefault', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['orglimitdefault', 'list'] });
    },
    ...options,
  });
}
