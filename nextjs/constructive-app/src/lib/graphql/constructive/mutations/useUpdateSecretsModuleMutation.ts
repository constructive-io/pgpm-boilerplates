/**
 * Update mutation hook for SecretsModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type SecretsModule } from '../types';

// Re-export entity type for convenience
export type { SecretsModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateSecretsModuleMutationDocument = `
mutation UpdateSecretsModuleMutation($input: UpdateSecretsModuleInput!) {
  updateSecretsModule(input: $input) {
    secretsModule {
      id
      databaseId
      schemaId
      tableId
      tableName
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a SecretsModule - all fields optional */
interface SecretsModulePatch {
  databaseId?: string | null;
  schemaId?: string | null;
  tableId?: string | null;
  tableName?: string | null;
}

export interface UpdateSecretsModuleMutationVariables {
  input: {
    id: string;
    patch: SecretsModulePatch;
  };
}

export interface UpdateSecretsModuleMutationResult {
  updateSecretsModule: {
    secretsModule: SecretsModule;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a SecretsModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateSecretsModuleMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateSecretsModuleMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateSecretsModuleMutationResult,
      Error,
      UpdateSecretsModuleMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateSecretsModuleMutationVariables) =>
      execute<
        UpdateSecretsModuleMutationResult,
        UpdateSecretsModuleMutationVariables
      >(updateSecretsModuleMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['secretsmodule', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['secretsmodule', 'list'] });
    },
    ...options,
  });
}
