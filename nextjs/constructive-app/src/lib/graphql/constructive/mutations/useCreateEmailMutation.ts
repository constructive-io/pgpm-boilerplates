/**
 * Create mutation hook for Email
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Email } from '../types';

// Re-export entity type for convenience
export type { Email };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createEmailMutationDocument = `
mutation CreateEmailMutation($input: CreateEmailInput!) {
  createEmail(input: $input) {
    email {
      id
      ownerId
      email
      isVerified
      isPrimary
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a Email */
interface EmailCreateInput {
  ownerId?: string | null;
  email?: string | null;
  isVerified?: boolean | null;
  isPrimary?: boolean | null;
}

export interface CreateEmailMutationVariables {
  input: {
    email: EmailCreateInput;
  };
}

export interface CreateEmailMutationResult {
  createEmail: {
    email: Email;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a Email
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateEmailMutation();
 *
 * mutate({
 *   input: {
 *     email: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateEmailMutation(
  options?: Omit<
    UseMutationOptions<
      CreateEmailMutationResult,
      Error,
      CreateEmailMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateEmailMutationVariables) =>
      execute<CreateEmailMutationResult, CreateEmailMutationVariables>(
        createEmailMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['email', 'list'] });
    },
    ...options,
  });
}
