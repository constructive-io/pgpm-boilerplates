/**
 * Update mutation hook for SiteTheme
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type SiteTheme } from '../types';

// Re-export entity type for convenience
export type { SiteTheme };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateSiteThemeMutationDocument = `
mutation UpdateSiteThemeMutation($input: UpdateSiteThemeInput!) {
  updateSiteTheme(input: $input) {
    siteTheme {
      id
      databaseId
      siteId
      theme
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a SiteTheme - all fields optional */
interface SiteThemePatch {
  databaseId?: string | null;
  siteId?: string | null;
  theme?: unknown | null;
}

export interface UpdateSiteThemeMutationVariables {
  input: {
    id: string;
    patch: SiteThemePatch;
  };
}

export interface UpdateSiteThemeMutationResult {
  updateSiteTheme: {
    siteTheme: SiteTheme;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a SiteTheme
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateSiteThemeMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateSiteThemeMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateSiteThemeMutationResult,
      Error,
      UpdateSiteThemeMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateSiteThemeMutationVariables) =>
      execute<UpdateSiteThemeMutationResult, UpdateSiteThemeMutationVariables>(
        updateSiteThemeMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['sitetheme', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['sitetheme', 'list'] });
    },
    ...options,
  });
}
