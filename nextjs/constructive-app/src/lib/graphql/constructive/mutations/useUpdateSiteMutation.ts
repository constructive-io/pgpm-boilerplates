/**
 * Update mutation hook for Site
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Site } from '../types';

// Re-export entity type for convenience
export type { Site };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateSiteMutationDocument = `
mutation UpdateSiteMutation($input: UpdateSiteInput!) {
  updateSite(input: $input) {
    site {
      id
      databaseId
      title
      description
      ogImage
      favicon
      appleTouchIcon
      logo
      dbname
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a Site - all fields optional */
interface SitePatch {
  databaseId?: string | null;
  title?: string | null;
  description?: string | null;
  ogImage?: unknown | null;
  favicon?: string | null;
  appleTouchIcon?: unknown | null;
  logo?: unknown | null;
  dbname?: string | null;
}

export interface UpdateSiteMutationVariables {
  input: {
    id: string;
    patch: SitePatch;
  };
}

export interface UpdateSiteMutationResult {
  updateSite: {
    site: Site;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a Site
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateSiteMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateSiteMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateSiteMutationResult,
      Error,
      UpdateSiteMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateSiteMutationVariables) =>
      execute<UpdateSiteMutationResult, UpdateSiteMutationVariables>(
        updateSiteMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['site', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['site', 'list'] });
    },
    ...options,
  });
}
