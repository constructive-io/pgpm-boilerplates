/**
 * Update mutation hook for ClaimedInvite
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type ClaimedInvite } from '../types';

// Re-export entity type for convenience
export type { ClaimedInvite };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateClaimedInviteMutationDocument = `
mutation UpdateClaimedInviteMutation($input: UpdateClaimedInviteInput!) {
  updateClaimedInvite(input: $input) {
    claimedInvite {
      id
      data
      senderId
      receiverId
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a ClaimedInvite - all fields optional */
interface ClaimedInvitePatch {
  data?: unknown | null;
  senderId?: string | null;
  receiverId?: string | null;
  createdAt?: string | null;
  updatedAt?: string | null;
}

export interface UpdateClaimedInviteMutationVariables {
  input: {
    id: string;
    patch: ClaimedInvitePatch;
  };
}

export interface UpdateClaimedInviteMutationResult {
  updateClaimedInvite: {
    claimedInvite: ClaimedInvite;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a ClaimedInvite
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateClaimedInviteMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateClaimedInviteMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateClaimedInviteMutationResult,
      Error,
      UpdateClaimedInviteMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateClaimedInviteMutationVariables) =>
      execute<
        UpdateClaimedInviteMutationResult,
        UpdateClaimedInviteMutationVariables
      >(updateClaimedInviteMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['claimedinvite', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['claimedinvite', 'list'] });
    },
    ...options,
  });
}
