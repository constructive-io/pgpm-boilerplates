/**
 * Update mutation hook for AppOwnerGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type AppOwnerGrant } from '../types';

// Re-export entity type for convenience
export type { AppOwnerGrant };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateAppOwnerGrantMutationDocument = `
mutation UpdateAppOwnerGrantMutation($input: UpdateAppOwnerGrantInput!) {
  updateAppOwnerGrant(input: $input) {
    appOwnerGrant {
      id
      isGrant
      actorId
      grantorId
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a AppOwnerGrant - all fields optional */
interface AppOwnerGrantPatch {
  isGrant?: boolean | null;
  actorId?: string | null;
  grantorId?: string | null;
  createdAt?: string | null;
  updatedAt?: string | null;
}

export interface UpdateAppOwnerGrantMutationVariables {
  input: {
    id: string;
    patch: AppOwnerGrantPatch;
  };
}

export interface UpdateAppOwnerGrantMutationResult {
  updateAppOwnerGrant: {
    appOwnerGrant: AppOwnerGrant;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a AppOwnerGrant
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppOwnerGrantMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateAppOwnerGrantMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateAppOwnerGrantMutationResult,
      Error,
      UpdateAppOwnerGrantMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateAppOwnerGrantMutationVariables) =>
      execute<
        UpdateAppOwnerGrantMutationResult,
        UpdateAppOwnerGrantMutationVariables
      >(updateAppOwnerGrantMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['appownergrant', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['appownergrant', 'list'] });
    },
    ...options,
  });
}
