/**
 * Delete mutation hook for AppAchievement
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deleteAppAchievementMutationDocument = `
mutation DeleteAppAchievementMutation($input: DeleteAppAchievementInput!) {
  deleteAppAchievement(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeleteAppAchievementMutationVariables {
  input: {
    id: string;
  };
}

export interface DeleteAppAchievementMutationResult {
  deleteAppAchievement: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a AppAchievement
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppAchievementMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeleteAppAchievementMutation(
  options?: Omit<
    UseMutationOptions<
      DeleteAppAchievementMutationResult,
      Error,
      DeleteAppAchievementMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeleteAppAchievementMutationVariables) =>
      execute<
        DeleteAppAchievementMutationResult,
        DeleteAppAchievementMutationVariables
      >(deleteAppAchievementMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['appachievement', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['appachievement', 'list'] });
    },
    ...options,
  });
}
