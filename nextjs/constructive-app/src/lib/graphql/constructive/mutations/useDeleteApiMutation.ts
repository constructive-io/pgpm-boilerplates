/**
 * Delete mutation hook for Api
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deleteApiMutationDocument = `
mutation DeleteApiMutation($input: DeleteApiInput!) {
  deleteApi(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeleteApiMutationVariables {
  input: {
    id: string;
  };
}

export interface DeleteApiMutationResult {
  deleteApi: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a Api
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteApiMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeleteApiMutation(
  options?: Omit<
    UseMutationOptions<
      DeleteApiMutationResult,
      Error,
      DeleteApiMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeleteApiMutationVariables) =>
      execute<DeleteApiMutationResult, DeleteApiMutationVariables>(
        deleteApiMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['api', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['api', 'list'] });
    },
    ...options,
  });
}
