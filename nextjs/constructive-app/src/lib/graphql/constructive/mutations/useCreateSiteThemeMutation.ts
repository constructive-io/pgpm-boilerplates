/**
 * Create mutation hook for SiteTheme
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type SiteTheme } from '../types';

// Re-export entity type for convenience
export type { SiteTheme };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createSiteThemeMutationDocument = `
mutation CreateSiteThemeMutation($input: CreateSiteThemeInput!) {
  createSiteTheme(input: $input) {
    siteTheme {
      id
      databaseId
      siteId
      theme
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a SiteTheme */
interface SiteThemeCreateInput {
  databaseId?: string | null;
  siteId?: string | null;
  theme?: unknown | null;
}

export interface CreateSiteThemeMutationVariables {
  input: {
    siteTheme: SiteThemeCreateInput;
  };
}

export interface CreateSiteThemeMutationResult {
  createSiteTheme: {
    siteTheme: SiteTheme;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a SiteTheme
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateSiteThemeMutation();
 *
 * mutate({
 *   input: {
 *     siteTheme: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateSiteThemeMutation(
  options?: Omit<
    UseMutationOptions<
      CreateSiteThemeMutationResult,
      Error,
      CreateSiteThemeMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateSiteThemeMutationVariables) =>
      execute<CreateSiteThemeMutationResult, CreateSiteThemeMutationVariables>(
        createSiteThemeMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['sitetheme', 'list'] });
    },
    ...options,
  });
}
