/**
 * Update mutation hook for Email
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Email } from '../types';

// Re-export entity type for convenience
export type { Email };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateEmailMutationDocument = `
mutation UpdateEmailMutation($input: UpdateEmailInput!) {
  updateEmail(input: $input) {
    email {
      id
      ownerId
      email
      isVerified
      isPrimary
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a Email - all fields optional */
interface EmailPatch {
  ownerId?: string | null;
  email?: string | null;
  isVerified?: boolean | null;
  isPrimary?: boolean | null;
  createdAt?: string | null;
  updatedAt?: string | null;
}

export interface UpdateEmailMutationVariables {
  input: {
    id: string;
    patch: EmailPatch;
  };
}

export interface UpdateEmailMutationResult {
  updateEmail: {
    email: Email;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a Email
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateEmailMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateEmailMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateEmailMutationResult,
      Error,
      UpdateEmailMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateEmailMutationVariables) =>
      execute<UpdateEmailMutationResult, UpdateEmailMutationVariables>(
        updateEmailMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['email', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['email', 'list'] });
    },
    ...options,
  });
}
