/**
 * Update mutation hook for Policy
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Policy } from '../types';
import { type ObjectCategory } from '../schema-types';

// Re-export entity type for convenience
export type { Policy };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updatePolicyMutationDocument = `
mutation UpdatePolicyMutation($input: UpdatePolicyInput!) {
  updatePolicy(input: $input) {
    policy {
      id
      databaseId
      tableId
      name
      roleName
      privilege
      permissive
      disabled
      template
      data
      smartTags
      category
      module
      scope
      tags
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a Policy - all fields optional */
interface PolicyPatch {
  databaseId?: string | null;
  tableId?: string | null;
  name?: string | null;
  roleName?: string | null;
  privilege?: string | null;
  permissive?: boolean | null;
  disabled?: boolean | null;
  template?: string | null;
  data?: unknown | null;
  smartTags?: unknown | null;
  category?: ObjectCategory | null;
  module?: string | null;
  scope?: number | null;
  tags?: string[] | null;
  createdAt?: string | null;
  updatedAt?: string | null;
}

export interface UpdatePolicyMutationVariables {
  input: {
    id: string;
    patch: PolicyPatch;
  };
}

export interface UpdatePolicyMutationResult {
  updatePolicy: {
    policy: Policy;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a Policy
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdatePolicyMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdatePolicyMutation(
  options?: Omit<
    UseMutationOptions<
      UpdatePolicyMutationResult,
      Error,
      UpdatePolicyMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdatePolicyMutationVariables) =>
      execute<UpdatePolicyMutationResult, UpdatePolicyMutationVariables>(
        updatePolicyMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['policy', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['policy', 'list'] });
    },
    ...options,
  });
}
