/**
 * Update mutation hook for OrgClaimedInvite
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type OrgClaimedInvite } from '../types';

// Re-export entity type for convenience
export type { OrgClaimedInvite };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateOrgClaimedInviteMutationDocument = `
mutation UpdateOrgClaimedInviteMutation($input: UpdateOrgClaimedInviteInput!) {
  updateOrgClaimedInvite(input: $input) {
    orgClaimedInvite {
      id
      data
      senderId
      receiverId
      createdAt
      updatedAt
      entityId
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a OrgClaimedInvite - all fields optional */
interface OrgClaimedInvitePatch {
  data?: unknown | null;
  senderId?: string | null;
  receiverId?: string | null;
  createdAt?: string | null;
  updatedAt?: string | null;
  entityId?: string | null;
}

export interface UpdateOrgClaimedInviteMutationVariables {
  input: {
    id: string;
    patch: OrgClaimedInvitePatch;
  };
}

export interface UpdateOrgClaimedInviteMutationResult {
  updateOrgClaimedInvite: {
    orgClaimedInvite: OrgClaimedInvite;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a OrgClaimedInvite
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateOrgClaimedInviteMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateOrgClaimedInviteMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateOrgClaimedInviteMutationResult,
      Error,
      UpdateOrgClaimedInviteMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateOrgClaimedInviteMutationVariables) =>
      execute<
        UpdateOrgClaimedInviteMutationResult,
        UpdateOrgClaimedInviteMutationVariables
      >(updateOrgClaimedInviteMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['orgclaimedinvite', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['orgclaimedinvite', 'list'] });
    },
    ...options,
  });
}
