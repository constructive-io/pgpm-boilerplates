/**
 * Create mutation hook for ApiModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type ApiModule } from '../types';

// Re-export entity type for convenience
export type { ApiModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createApiModuleMutationDocument = `
mutation CreateApiModuleMutation($input: CreateApiModuleInput!) {
  createApiModule(input: $input) {
    apiModule {
      id
      databaseId
      apiId
      name
      data
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a ApiModule */
interface ApiModuleCreateInput {
  databaseId?: string | null;
  apiId?: string | null;
  name?: string | null;
  data?: unknown | null;
}

export interface CreateApiModuleMutationVariables {
  input: {
    apiModule: ApiModuleCreateInput;
  };
}

export interface CreateApiModuleMutationResult {
  createApiModule: {
    apiModule: ApiModule;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a ApiModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateApiModuleMutation();
 *
 * mutate({
 *   input: {
 *     apiModule: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateApiModuleMutation(
  options?: Omit<
    UseMutationOptions<
      CreateApiModuleMutationResult,
      Error,
      CreateApiModuleMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateApiModuleMutationVariables) =>
      execute<CreateApiModuleMutationResult, CreateApiModuleMutationVariables>(
        createApiModuleMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['apimodule', 'list'] });
    },
    ...options,
  });
}
