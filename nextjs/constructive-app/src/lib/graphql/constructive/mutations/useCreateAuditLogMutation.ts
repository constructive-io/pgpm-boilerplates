/**
 * Create mutation hook for AuditLog
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type AuditLog } from '../types';

// Re-export entity type for convenience
export type { AuditLog };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createAuditLogMutationDocument = `
mutation CreateAuditLogMutation($input: CreateAuditLogInput!) {
  createAuditLog(input: $input) {
    auditLog {
      id
      event
      actorId
      origin
      userAgent
      ipAddress
      success
      createdAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a AuditLog */
interface AuditLogCreateInput {
  event?: string | null;
  actorId?: string | null;
  origin?: string | null;
  userAgent?: string | null;
  ipAddress?: string | null;
  success?: boolean | null;
}

export interface CreateAuditLogMutationVariables {
  input: {
    auditLog: AuditLogCreateInput;
  };
}

export interface CreateAuditLogMutationResult {
  createAuditLog: {
    auditLog: AuditLog;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a AuditLog
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateAuditLogMutation();
 *
 * mutate({
 *   input: {
 *     auditLog: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateAuditLogMutation(
  options?: Omit<
    UseMutationOptions<
      CreateAuditLogMutationResult,
      Error,
      CreateAuditLogMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateAuditLogMutationVariables) =>
      execute<CreateAuditLogMutationResult, CreateAuditLogMutationVariables>(
        createAuditLogMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['auditlog', 'list'] });
    },
    ...options,
  });
}
