/**
 * Create mutation hook for UuidModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type UuidModule } from '../types';

// Re-export entity type for convenience
export type { UuidModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createUuidModuleMutationDocument = `
mutation CreateUuidModuleMutation($input: CreateUuidModuleInput!) {
  createUuidModule(input: $input) {
    uuidModule {
      id
      databaseId
      schemaId
      uuidFunction
      uuidSeed
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a UuidModule */
interface UuidModuleCreateInput {
  databaseId?: string | null;
  schemaId?: string | null;
  uuidFunction?: string | null;
  uuidSeed?: string | null;
}

export interface CreateUuidModuleMutationVariables {
  input: {
    uuidModule: UuidModuleCreateInput;
  };
}

export interface CreateUuidModuleMutationResult {
  createUuidModule: {
    uuidModule: UuidModule;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a UuidModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateUuidModuleMutation();
 *
 * mutate({
 *   input: {
 *     uuidModule: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateUuidModuleMutation(
  options?: Omit<
    UseMutationOptions<
      CreateUuidModuleMutationResult,
      Error,
      CreateUuidModuleMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateUuidModuleMutationVariables) =>
      execute<
        CreateUuidModuleMutationResult,
        CreateUuidModuleMutationVariables
      >(createUuidModuleMutationDocument, variables),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['uuidmodule', 'list'] });
    },
    ...options,
  });
}
