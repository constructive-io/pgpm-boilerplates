/**
 * Update mutation hook for OrgPermissionDefault
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type OrgPermissionDefault } from '../types';

// Re-export entity type for convenience
export type { OrgPermissionDefault };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateOrgPermissionDefaultMutationDocument = `
mutation UpdateOrgPermissionDefaultMutation($input: UpdateOrgPermissionDefaultInput!) {
  updateOrgPermissionDefault(input: $input) {
    orgPermissionDefault {
      id
      permissions
      entityId
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a OrgPermissionDefault - all fields optional */
interface OrgPermissionDefaultPatch {
  permissions?: string | null;
  entityId?: string | null;
}

export interface UpdateOrgPermissionDefaultMutationVariables {
  input: {
    id: string;
    patch: OrgPermissionDefaultPatch;
  };
}

export interface UpdateOrgPermissionDefaultMutationResult {
  updateOrgPermissionDefault: {
    orgPermissionDefault: OrgPermissionDefault;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a OrgPermissionDefault
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateOrgPermissionDefaultMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateOrgPermissionDefaultMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateOrgPermissionDefaultMutationResult,
      Error,
      UpdateOrgPermissionDefaultMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateOrgPermissionDefaultMutationVariables) =>
      execute<
        UpdateOrgPermissionDefaultMutationResult,
        UpdateOrgPermissionDefaultMutationVariables
      >(updateOrgPermissionDefaultMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['orgpermissiondefault', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({
        queryKey: ['orgpermissiondefault', 'list'],
      });
    },
    ...options,
  });
}
