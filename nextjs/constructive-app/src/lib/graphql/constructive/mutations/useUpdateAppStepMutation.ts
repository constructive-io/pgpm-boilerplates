/**
 * Update mutation hook for AppStep
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type AppStep } from '../types';

// Re-export entity type for convenience
export type { AppStep };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateAppStepMutationDocument = `
mutation UpdateAppStepMutation($input: UpdateAppStepInput!) {
  updateAppStep(input: $input) {
    appStep {
      id
      actorId
      name
      count
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a AppStep - all fields optional */
interface AppStepPatch {
  actorId?: string | null;
  name?: string | null;
  count?: number | null;
  createdAt?: string | null;
  updatedAt?: string | null;
}

export interface UpdateAppStepMutationVariables {
  input: {
    id: string;
    patch: AppStepPatch;
  };
}

export interface UpdateAppStepMutationResult {
  updateAppStep: {
    appStep: AppStep;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a AppStep
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppStepMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateAppStepMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateAppStepMutationResult,
      Error,
      UpdateAppStepMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateAppStepMutationVariables) =>
      execute<UpdateAppStepMutationResult, UpdateAppStepMutationVariables>(
        updateAppStepMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['appstep', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['appstep', 'list'] });
    },
    ...options,
  });
}
