/**
 * Create mutation hook for Ref
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Ref } from '../types';

// Re-export entity type for convenience
export type { Ref };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createRefMutationDocument = `
mutation CreateRefMutation($input: CreateRefInput!) {
  createRef(input: $input) {
    ref {
      id
      name
      databaseId
      storeId
      commitId
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a Ref */
interface RefCreateInput {
  name?: string | null;
  databaseId?: string | null;
  storeId?: string | null;
  commitId?: string | null;
}

export interface CreateRefMutationVariables {
  input: {
    ref: RefCreateInput;
  };
}

export interface CreateRefMutationResult {
  createRef: {
    ref: Ref;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a Ref
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateRefMutation();
 *
 * mutate({
 *   input: {
 *     ref: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateRefMutation(
  options?: Omit<
    UseMutationOptions<
      CreateRefMutationResult,
      Error,
      CreateRefMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateRefMutationVariables) =>
      execute<CreateRefMutationResult, CreateRefMutationVariables>(
        createRefMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['ref', 'list'] });
    },
    ...options,
  });
}
