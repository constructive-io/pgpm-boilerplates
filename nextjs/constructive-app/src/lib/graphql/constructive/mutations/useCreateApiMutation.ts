/**
 * Create mutation hook for Api
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Api } from '../types';

// Re-export entity type for convenience
export type { Api };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createApiMutationDocument = `
mutation CreateApiMutation($input: CreateApiInput!) {
  createApi(input: $input) {
    api {
      id
      databaseId
      name
      dbname
      roleName
      anonRole
      isPublic
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a Api */
interface ApiCreateInput {
  databaseId?: string | null;
  name?: string | null;
  dbname?: string | null;
  roleName?: string | null;
  anonRole?: string | null;
  isPublic?: boolean | null;
}

export interface CreateApiMutationVariables {
  input: {
    api: ApiCreateInput;
  };
}

export interface CreateApiMutationResult {
  createApi: {
    api: Api;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a Api
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateApiMutation();
 *
 * mutate({
 *   input: {
 *     api: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateApiMutation(
  options?: Omit<
    UseMutationOptions<
      CreateApiMutationResult,
      Error,
      CreateApiMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateApiMutationVariables) =>
      execute<CreateApiMutationResult, CreateApiMutationVariables>(
        createApiMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['api', 'list'] });
    },
    ...options,
  });
}
