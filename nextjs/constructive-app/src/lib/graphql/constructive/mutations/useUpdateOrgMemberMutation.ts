/**
 * Update mutation hook for OrgMember
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type OrgMember } from '../types';

// Re-export entity type for convenience
export type { OrgMember };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateOrgMemberMutationDocument = `
mutation UpdateOrgMemberMutation($input: UpdateOrgMemberInput!) {
  updateOrgMember(input: $input) {
    orgMember {
      id
      isAdmin
      actorId
      entityId
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a OrgMember - all fields optional */
interface OrgMemberPatch {
  isAdmin?: boolean | null;
  actorId?: string | null;
  entityId?: string | null;
}

export interface UpdateOrgMemberMutationVariables {
  input: {
    id: string;
    patch: OrgMemberPatch;
  };
}

export interface UpdateOrgMemberMutationResult {
  updateOrgMember: {
    orgMember: OrgMember;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a OrgMember
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateOrgMemberMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateOrgMemberMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateOrgMemberMutationResult,
      Error,
      UpdateOrgMemberMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateOrgMemberMutationVariables) =>
      execute<UpdateOrgMemberMutationResult, UpdateOrgMemberMutationVariables>(
        updateOrgMemberMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['orgmember', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['orgmember', 'list'] });
    },
    ...options,
  });
}
