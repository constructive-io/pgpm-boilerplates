/**
 * Create mutation hook for Table
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Table } from '../types';
import { type ObjectCategory } from '../schema-types';

// Re-export entity type for convenience
export type { Table };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createTableMutationDocument = `
mutation CreateTableMutation($input: CreateTableInput!) {
  createTable(input: $input) {
    table {
      id
      databaseId
      schemaId
      name
      label
      description
      smartTags
      category
      module
      scope
      useRls
      timestamps
      peoplestamps
      pluralName
      singularName
      tags
      inheritsId
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a Table */
interface TableCreateInput {
  databaseId?: string | null;
  schemaId?: string | null;
  name?: string | null;
  label?: string | null;
  description?: string | null;
  smartTags?: unknown | null;
  category?: ObjectCategory | null;
  module?: string | null;
  scope?: number | null;
  useRls?: boolean | null;
  timestamps?: boolean | null;
  peoplestamps?: boolean | null;
  pluralName?: string | null;
  singularName?: string | null;
  tags?: string[] | null;
  inheritsId?: string | null;
}

export interface CreateTableMutationVariables {
  input: {
    table: TableCreateInput;
  };
}

export interface CreateTableMutationResult {
  createTable: {
    table: Table;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a Table
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateTableMutation();
 *
 * mutate({
 *   input: {
 *     table: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateTableMutation(
  options?: Omit<
    UseMutationOptions<
      CreateTableMutationResult,
      Error,
      CreateTableMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateTableMutationVariables) =>
      execute<CreateTableMutationResult, CreateTableMutationVariables>(
        createTableMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['table', 'list'] });
    },
    ...options,
  });
}
