/**
 * Update mutation hook for TriggerFunction
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type TriggerFunction } from '../types';

// Re-export entity type for convenience
export type { TriggerFunction };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateTriggerFunctionMutationDocument = `
mutation UpdateTriggerFunctionMutation($input: UpdateTriggerFunctionInput!) {
  updateTriggerFunction(input: $input) {
    triggerFunction {
      id
      databaseId
      name
      code
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a TriggerFunction - all fields optional */
interface TriggerFunctionPatch {
  databaseId?: string | null;
  name?: string | null;
  code?: string | null;
  createdAt?: string | null;
  updatedAt?: string | null;
}

export interface UpdateTriggerFunctionMutationVariables {
  input: {
    id: string;
    patch: TriggerFunctionPatch;
  };
}

export interface UpdateTriggerFunctionMutationResult {
  updateTriggerFunction: {
    triggerFunction: TriggerFunction;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a TriggerFunction
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateTriggerFunctionMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateTriggerFunctionMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateTriggerFunctionMutationResult,
      Error,
      UpdateTriggerFunctionMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateTriggerFunctionMutationVariables) =>
      execute<
        UpdateTriggerFunctionMutationResult,
        UpdateTriggerFunctionMutationVariables
      >(updateTriggerFunctionMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['triggerfunction', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['triggerfunction', 'list'] });
    },
    ...options,
  });
}
