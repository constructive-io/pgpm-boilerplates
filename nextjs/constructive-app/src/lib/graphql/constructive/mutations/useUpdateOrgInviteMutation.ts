/**
 * Update mutation hook for OrgInvite
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type OrgInvite } from '../types';

// Re-export entity type for convenience
export type { OrgInvite };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateOrgInviteMutationDocument = `
mutation UpdateOrgInviteMutation($input: UpdateOrgInviteInput!) {
  updateOrgInvite(input: $input) {
    orgInvite {
      id
      email
      senderId
      receiverId
      inviteToken
      inviteValid
      inviteLimit
      inviteCount
      multiple
      data
      expiresAt
      createdAt
      updatedAt
      entityId
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a OrgInvite - all fields optional */
interface OrgInvitePatch {
  email?: string | null;
  senderId?: string | null;
  receiverId?: string | null;
  inviteToken?: string | null;
  inviteValid?: boolean | null;
  inviteLimit?: number | null;
  inviteCount?: number | null;
  multiple?: boolean | null;
  data?: unknown | null;
  expiresAt?: string | null;
  createdAt?: string | null;
  updatedAt?: string | null;
  entityId?: string | null;
}

export interface UpdateOrgInviteMutationVariables {
  input: {
    id: string;
    patch: OrgInvitePatch;
  };
}

export interface UpdateOrgInviteMutationResult {
  updateOrgInvite: {
    orgInvite: OrgInvite;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a OrgInvite
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateOrgInviteMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateOrgInviteMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateOrgInviteMutationResult,
      Error,
      UpdateOrgInviteMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateOrgInviteMutationVariables) =>
      execute<UpdateOrgInviteMutationResult, UpdateOrgInviteMutationVariables>(
        updateOrgInviteMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['orginvite', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['orginvite', 'list'] });
    },
    ...options,
  });
}
