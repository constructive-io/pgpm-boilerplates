/**
 * Create mutation hook for OrgLimit
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type OrgLimit } from '../types';

// Re-export entity type for convenience
export type { OrgLimit };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createOrgLimitMutationDocument = `
mutation CreateOrgLimitMutation($input: CreateOrgLimitInput!) {
  createOrgLimit(input: $input) {
    orgLimit {
      id
      name
      actorId
      num
      max
      entityId
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a OrgLimit */
interface OrgLimitCreateInput {
  name?: string | null;
  actorId?: string | null;
  num?: number | null;
  max?: number | null;
  entityId?: string | null;
}

export interface CreateOrgLimitMutationVariables {
  input: {
    orgLimit: OrgLimitCreateInput;
  };
}

export interface CreateOrgLimitMutationResult {
  createOrgLimit: {
    orgLimit: OrgLimit;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a OrgLimit
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgLimitMutation();
 *
 * mutate({
 *   input: {
 *     orgLimit: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateOrgLimitMutation(
  options?: Omit<
    UseMutationOptions<
      CreateOrgLimitMutationResult,
      Error,
      CreateOrgLimitMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateOrgLimitMutationVariables) =>
      execute<CreateOrgLimitMutationResult, CreateOrgLimitMutationVariables>(
        createOrgLimitMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['orglimit', 'list'] });
    },
    ...options,
  });
}
