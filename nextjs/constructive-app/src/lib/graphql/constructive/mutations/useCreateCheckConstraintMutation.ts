/**
 * Create mutation hook for CheckConstraint
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type CheckConstraint } from '../types';
import { type ObjectCategory } from '../schema-types';

// Re-export entity type for convenience
export type { CheckConstraint };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createCheckConstraintMutationDocument = `
mutation CreateCheckConstraintMutation($input: CreateCheckConstraintInput!) {
  createCheckConstraint(input: $input) {
    checkConstraint {
      id
      databaseId
      tableId
      name
      type
      fieldIds
      expr
      smartTags
      category
      module
      scope
      tags
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a CheckConstraint */
interface CheckConstraintCreateInput {
  databaseId?: string | null;
  tableId?: string | null;
  name?: string | null;
  type?: string | null;
  fieldIds?: string[] | null;
  expr?: unknown | null;
  smartTags?: unknown | null;
  category?: ObjectCategory | null;
  module?: string | null;
  scope?: number | null;
  tags?: string[] | null;
}

export interface CreateCheckConstraintMutationVariables {
  input: {
    checkConstraint: CheckConstraintCreateInput;
  };
}

export interface CreateCheckConstraintMutationResult {
  createCheckConstraint: {
    checkConstraint: CheckConstraint;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a CheckConstraint
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateCheckConstraintMutation();
 *
 * mutate({
 *   input: {
 *     checkConstraint: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateCheckConstraintMutation(
  options?: Omit<
    UseMutationOptions<
      CreateCheckConstraintMutationResult,
      Error,
      CreateCheckConstraintMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateCheckConstraintMutationVariables) =>
      execute<
        CreateCheckConstraintMutationResult,
        CreateCheckConstraintMutationVariables
      >(createCheckConstraintMutationDocument, variables),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['checkconstraint', 'list'] });
    },
    ...options,
  });
}
