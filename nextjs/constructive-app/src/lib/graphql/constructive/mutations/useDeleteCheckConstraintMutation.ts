/**
 * Delete mutation hook for CheckConstraint
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deleteCheckConstraintMutationDocument = `
mutation DeleteCheckConstraintMutation($input: DeleteCheckConstraintInput!) {
  deleteCheckConstraint(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeleteCheckConstraintMutationVariables {
  input: {
    id: string;
  };
}

export interface DeleteCheckConstraintMutationResult {
  deleteCheckConstraint: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a CheckConstraint
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteCheckConstraintMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeleteCheckConstraintMutation(
  options?: Omit<
    UseMutationOptions<
      DeleteCheckConstraintMutationResult,
      Error,
      DeleteCheckConstraintMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeleteCheckConstraintMutationVariables) =>
      execute<
        DeleteCheckConstraintMutationResult,
        DeleteCheckConstraintMutationVariables
      >(deleteCheckConstraintMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['checkconstraint', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['checkconstraint', 'list'] });
    },
    ...options,
  });
}
