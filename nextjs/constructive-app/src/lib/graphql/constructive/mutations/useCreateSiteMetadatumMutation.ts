/**
 * Create mutation hook for SiteMetadatum
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type SiteMetadatum } from '../types';

// Re-export entity type for convenience
export type { SiteMetadatum };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createSiteMetadatumMutationDocument = `
mutation CreateSiteMetadatumMutation($input: CreateSiteMetadatumInput!) {
  createSiteMetadatum(input: $input) {
    siteMetadatum {
      id
      databaseId
      siteId
      title
      description
      ogImage
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a SiteMetadatum */
interface SiteMetadatumCreateInput {
  databaseId?: string | null;
  siteId?: string | null;
  title?: string | null;
  description?: string | null;
  ogImage?: unknown | null;
}

export interface CreateSiteMetadatumMutationVariables {
  input: {
    siteMetadatum: SiteMetadatumCreateInput;
  };
}

export interface CreateSiteMetadatumMutationResult {
  createSiteMetadatum: {
    siteMetadatum: SiteMetadatum;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a SiteMetadatum
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateSiteMetadatumMutation();
 *
 * mutate({
 *   input: {
 *     siteMetadatum: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateSiteMetadatumMutation(
  options?: Omit<
    UseMutationOptions<
      CreateSiteMetadatumMutationResult,
      Error,
      CreateSiteMetadatumMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateSiteMetadatumMutationVariables) =>
      execute<
        CreateSiteMetadatumMutationResult,
        CreateSiteMetadatumMutationVariables
      >(createSiteMetadatumMutationDocument, variables),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['sitemetadatum', 'list'] });
    },
    ...options,
  });
}
