/**
 * Update mutation hook for CryptoAddress
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type CryptoAddress } from '../types';

// Re-export entity type for convenience
export type { CryptoAddress };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateCryptoAddressMutationDocument = `
mutation UpdateCryptoAddressMutation($input: UpdateCryptoAddressInput!) {
  updateCryptoAddress(input: $input) {
    cryptoAddress {
      id
      ownerId
      address
      isVerified
      isPrimary
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a CryptoAddress - all fields optional */
interface CryptoAddressPatch {
  ownerId?: string | null;
  address?: string | null;
  isVerified?: boolean | null;
  isPrimary?: boolean | null;
  createdAt?: string | null;
  updatedAt?: string | null;
}

export interface UpdateCryptoAddressMutationVariables {
  input: {
    id: string;
    patch: CryptoAddressPatch;
  };
}

export interface UpdateCryptoAddressMutationResult {
  updateCryptoAddress: {
    cryptoAddress: CryptoAddress;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a CryptoAddress
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateCryptoAddressMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateCryptoAddressMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateCryptoAddressMutationResult,
      Error,
      UpdateCryptoAddressMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateCryptoAddressMutationVariables) =>
      execute<
        UpdateCryptoAddressMutationResult,
        UpdateCryptoAddressMutationVariables
      >(updateCryptoAddressMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['cryptoaddress', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['cryptoaddress', 'list'] });
    },
    ...options,
  });
}
