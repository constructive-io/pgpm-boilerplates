/**
 * Update mutation hook for ApiModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type ApiModule } from '../types';

// Re-export entity type for convenience
export type { ApiModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateApiModuleMutationDocument = `
mutation UpdateApiModuleMutation($input: UpdateApiModuleInput!) {
  updateApiModule(input: $input) {
    apiModule {
      id
      databaseId
      apiId
      name
      data
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a ApiModule - all fields optional */
interface ApiModulePatch {
  databaseId?: string | null;
  apiId?: string | null;
  name?: string | null;
  data?: unknown | null;
}

export interface UpdateApiModuleMutationVariables {
  input: {
    id: string;
    patch: ApiModulePatch;
  };
}

export interface UpdateApiModuleMutationResult {
  updateApiModule: {
    apiModule: ApiModule;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a ApiModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateApiModuleMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateApiModuleMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateApiModuleMutationResult,
      Error,
      UpdateApiModuleMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateApiModuleMutationVariables) =>
      execute<UpdateApiModuleMutationResult, UpdateApiModuleMutationVariables>(
        updateApiModuleMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['apimodule', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['apimodule', 'list'] });
    },
    ...options,
  });
}
