/**
 * Delete mutation hook for FullTextSearch
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deleteFullTextSearchMutationDocument = `
mutation DeleteFullTextSearchMutation($input: DeleteFullTextSearchInput!) {
  deleteFullTextSearch(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeleteFullTextSearchMutationVariables {
  input: {
    id: string;
  };
}

export interface DeleteFullTextSearchMutationResult {
  deleteFullTextSearch: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a FullTextSearch
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteFullTextSearchMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeleteFullTextSearchMutation(
  options?: Omit<
    UseMutationOptions<
      DeleteFullTextSearchMutationResult,
      Error,
      DeleteFullTextSearchMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeleteFullTextSearchMutationVariables) =>
      execute<
        DeleteFullTextSearchMutationResult,
        DeleteFullTextSearchMutationVariables
      >(deleteFullTextSearchMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['fulltextsearch', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['fulltextsearch', 'list'] });
    },
    ...options,
  });
}
