/**
 * Update mutation hook for Index
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Index } from '../types';
import { type ObjectCategory } from '../schema-types';

// Re-export entity type for convenience
export type { Index };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateIndexMutationDocument = `
mutation UpdateIndexMutation($input: UpdateIndexInput!) {
  updateIndex(input: $input) {
    index {
      id
      databaseId
      tableId
      name
      fieldIds
      includeFieldIds
      accessMethod
      indexParams
      whereClause
      isUnique
      smartTags
      category
      module
      scope
      tags
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a Index - all fields optional */
interface IndexPatch {
  databaseId?: string | null;
  tableId?: string | null;
  name?: string | null;
  fieldIds?: string[] | null;
  includeFieldIds?: string[] | null;
  accessMethod?: string | null;
  indexParams?: unknown | null;
  whereClause?: unknown | null;
  isUnique?: boolean | null;
  smartTags?: unknown | null;
  category?: ObjectCategory | null;
  module?: string | null;
  scope?: number | null;
  tags?: string[] | null;
  createdAt?: string | null;
  updatedAt?: string | null;
}

export interface UpdateIndexMutationVariables {
  input: {
    id: string;
    patch: IndexPatch;
  };
}

export interface UpdateIndexMutationResult {
  updateIndex: {
    index: Index;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a Index
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateIndexMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateIndexMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateIndexMutationResult,
      Error,
      UpdateIndexMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateIndexMutationVariables) =>
      execute<UpdateIndexMutationResult, UpdateIndexMutationVariables>(
        updateIndexMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['index', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['index', 'list'] });
    },
    ...options,
  });
}
