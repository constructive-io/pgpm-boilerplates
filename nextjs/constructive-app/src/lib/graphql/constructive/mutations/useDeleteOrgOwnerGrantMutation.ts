/**
 * Delete mutation hook for OrgOwnerGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deleteOrgOwnerGrantMutationDocument = `
mutation DeleteOrgOwnerGrantMutation($input: DeleteOrgOwnerGrantInput!) {
  deleteOrgOwnerGrant(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeleteOrgOwnerGrantMutationVariables {
  input: {
    id: string;
  };
}

export interface DeleteOrgOwnerGrantMutationResult {
  deleteOrgOwnerGrant: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a OrgOwnerGrant
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteOrgOwnerGrantMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeleteOrgOwnerGrantMutation(
  options?: Omit<
    UseMutationOptions<
      DeleteOrgOwnerGrantMutationResult,
      Error,
      DeleteOrgOwnerGrantMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeleteOrgOwnerGrantMutationVariables) =>
      execute<
        DeleteOrgOwnerGrantMutationResult,
        DeleteOrgOwnerGrantMutationVariables
      >(deleteOrgOwnerGrantMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['orgownergrant', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['orgownergrant', 'list'] });
    },
    ...options,
  });
}
