/**
 * Create mutation hook for OrgClaimedInvite
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type OrgClaimedInvite } from '../types';

// Re-export entity type for convenience
export type { OrgClaimedInvite };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createOrgClaimedInviteMutationDocument = `
mutation CreateOrgClaimedInviteMutation($input: CreateOrgClaimedInviteInput!) {
  createOrgClaimedInvite(input: $input) {
    orgClaimedInvite {
      id
      data
      senderId
      receiverId
      createdAt
      updatedAt
      entityId
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a OrgClaimedInvite */
interface OrgClaimedInviteCreateInput {
  data?: unknown | null;
  senderId?: string | null;
  receiverId?: string | null;
  entityId?: string | null;
}

export interface CreateOrgClaimedInviteMutationVariables {
  input: {
    orgClaimedInvite: OrgClaimedInviteCreateInput;
  };
}

export interface CreateOrgClaimedInviteMutationResult {
  createOrgClaimedInvite: {
    orgClaimedInvite: OrgClaimedInvite;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a OrgClaimedInvite
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgClaimedInviteMutation();
 *
 * mutate({
 *   input: {
 *     orgClaimedInvite: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateOrgClaimedInviteMutation(
  options?: Omit<
    UseMutationOptions<
      CreateOrgClaimedInviteMutationResult,
      Error,
      CreateOrgClaimedInviteMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateOrgClaimedInviteMutationVariables) =>
      execute<
        CreateOrgClaimedInviteMutationResult,
        CreateOrgClaimedInviteMutationVariables
      >(createOrgClaimedInviteMutationDocument, variables),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['orgclaimedinvite', 'list'] });
    },
    ...options,
  });
}
