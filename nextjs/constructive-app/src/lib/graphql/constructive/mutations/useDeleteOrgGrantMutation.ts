/**
 * Delete mutation hook for OrgGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deleteOrgGrantMutationDocument = `
mutation DeleteOrgGrantMutation($input: DeleteOrgGrantInput!) {
  deleteOrgGrant(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeleteOrgGrantMutationVariables {
  input: {
    id: string;
  };
}

export interface DeleteOrgGrantMutationResult {
  deleteOrgGrant: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a OrgGrant
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteOrgGrantMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeleteOrgGrantMutation(
  options?: Omit<
    UseMutationOptions<
      DeleteOrgGrantMutationResult,
      Error,
      DeleteOrgGrantMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeleteOrgGrantMutationVariables) =>
      execute<DeleteOrgGrantMutationResult, DeleteOrgGrantMutationVariables>(
        deleteOrgGrantMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['orggrant', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['orggrant', 'list'] });
    },
    ...options,
  });
}
