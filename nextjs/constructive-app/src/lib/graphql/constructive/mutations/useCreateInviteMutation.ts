/**
 * Create mutation hook for Invite
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Invite } from '../types';

// Re-export entity type for convenience
export type { Invite };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createInviteMutationDocument = `
mutation CreateInviteMutation($input: CreateInviteInput!) {
  createInvite(input: $input) {
    invite {
      id
      email
      senderId
      inviteToken
      inviteValid
      inviteLimit
      inviteCount
      multiple
      data
      expiresAt
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a Invite */
interface InviteCreateInput {
  email?: string | null;
  senderId?: string | null;
  inviteToken?: string | null;
  inviteValid?: boolean | null;
  inviteLimit?: number | null;
  inviteCount?: number | null;
  multiple?: boolean | null;
  data?: unknown | null;
  expiresAt?: string | null;
}

export interface CreateInviteMutationVariables {
  input: {
    invite: InviteCreateInput;
  };
}

export interface CreateInviteMutationResult {
  createInvite: {
    invite: Invite;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a Invite
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateInviteMutation();
 *
 * mutate({
 *   input: {
 *     invite: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateInviteMutation(
  options?: Omit<
    UseMutationOptions<
      CreateInviteMutationResult,
      Error,
      CreateInviteMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateInviteMutationVariables) =>
      execute<CreateInviteMutationResult, CreateInviteMutationVariables>(
        createInviteMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['invite', 'list'] });
    },
    ...options,
  });
}
