/**
 * Delete mutation hook for AppLevel
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deleteAppLevelMutationDocument = `
mutation DeleteAppLevelMutation($input: DeleteAppLevelInput!) {
  deleteAppLevel(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeleteAppLevelMutationVariables {
  input: {
    id: string;
  };
}

export interface DeleteAppLevelMutationResult {
  deleteAppLevel: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a AppLevel
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppLevelMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeleteAppLevelMutation(
  options?: Omit<
    UseMutationOptions<
      DeleteAppLevelMutationResult,
      Error,
      DeleteAppLevelMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeleteAppLevelMutationVariables) =>
      execute<DeleteAppLevelMutationResult, DeleteAppLevelMutationVariables>(
        deleteAppLevelMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['applevel', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['applevel', 'list'] });
    },
    ...options,
  });
}
