/**
 * Delete mutation hook for Site
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deleteSiteMutationDocument = `
mutation DeleteSiteMutation($input: DeleteSiteInput!) {
  deleteSite(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeleteSiteMutationVariables {
  input: {
    id: string;
  };
}

export interface DeleteSiteMutationResult {
  deleteSite: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a Site
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteSiteMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeleteSiteMutation(
  options?: Omit<
    UseMutationOptions<
      DeleteSiteMutationResult,
      Error,
      DeleteSiteMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeleteSiteMutationVariables) =>
      execute<DeleteSiteMutationResult, DeleteSiteMutationVariables>(
        deleteSiteMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['site', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['site', 'list'] });
    },
    ...options,
  });
}
