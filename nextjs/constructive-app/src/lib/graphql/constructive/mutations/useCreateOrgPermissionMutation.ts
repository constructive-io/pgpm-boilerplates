/**
 * Create mutation hook for OrgPermission
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type OrgPermission } from '../types';

// Re-export entity type for convenience
export type { OrgPermission };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createOrgPermissionMutationDocument = `
mutation CreateOrgPermissionMutation($input: CreateOrgPermissionInput!) {
  createOrgPermission(input: $input) {
    orgPermission {
      id
      name
      bitnum
      bitstr
      description
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a OrgPermission */
interface OrgPermissionCreateInput {
  name?: string | null;
  bitnum?: number | null;
  bitstr?: string | null;
  description?: string | null;
}

export interface CreateOrgPermissionMutationVariables {
  input: {
    orgPermission: OrgPermissionCreateInput;
  };
}

export interface CreateOrgPermissionMutationResult {
  createOrgPermission: {
    orgPermission: OrgPermission;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a OrgPermission
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgPermissionMutation();
 *
 * mutate({
 *   input: {
 *     orgPermission: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateOrgPermissionMutation(
  options?: Omit<
    UseMutationOptions<
      CreateOrgPermissionMutationResult,
      Error,
      CreateOrgPermissionMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateOrgPermissionMutationVariables) =>
      execute<
        CreateOrgPermissionMutationResult,
        CreateOrgPermissionMutationVariables
      >(createOrgPermissionMutationDocument, variables),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['orgpermission', 'list'] });
    },
    ...options,
  });
}
