/**
 * Create mutation hook for Trigger
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Trigger } from '../types';
import { type ObjectCategory } from '../schema-types';

// Re-export entity type for convenience
export type { Trigger };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createTriggerMutationDocument = `
mutation CreateTriggerMutation($input: CreateTriggerInput!) {
  createTrigger(input: $input) {
    trigger {
      id
      databaseId
      tableId
      name
      event
      functionName
      smartTags
      category
      module
      scope
      tags
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a Trigger */
interface TriggerCreateInput {
  databaseId?: string | null;
  tableId?: string | null;
  name?: string | null;
  event?: string | null;
  functionName?: string | null;
  smartTags?: unknown | null;
  category?: ObjectCategory | null;
  module?: string | null;
  scope?: number | null;
  tags?: string[] | null;
}

export interface CreateTriggerMutationVariables {
  input: {
    trigger: TriggerCreateInput;
  };
}

export interface CreateTriggerMutationResult {
  createTrigger: {
    trigger: Trigger;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a Trigger
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateTriggerMutation();
 *
 * mutate({
 *   input: {
 *     trigger: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateTriggerMutation(
  options?: Omit<
    UseMutationOptions<
      CreateTriggerMutationResult,
      Error,
      CreateTriggerMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateTriggerMutationVariables) =>
      execute<CreateTriggerMutationResult, CreateTriggerMutationVariables>(
        createTriggerMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['trigger', 'list'] });
    },
    ...options,
  });
}
