/**
 * Delete mutation hook for Invite
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deleteInviteMutationDocument = `
mutation DeleteInviteMutation($input: DeleteInviteInput!) {
  deleteInvite(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeleteInviteMutationVariables {
  input: {
    id: string;
  };
}

export interface DeleteInviteMutationResult {
  deleteInvite: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a Invite
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteInviteMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeleteInviteMutation(
  options?: Omit<
    UseMutationOptions<
      DeleteInviteMutationResult,
      Error,
      DeleteInviteMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeleteInviteMutationVariables) =>
      execute<DeleteInviteMutationResult, DeleteInviteMutationVariables>(
        deleteInviteMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['invite', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['invite', 'list'] });
    },
    ...options,
  });
}
