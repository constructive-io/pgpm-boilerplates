/**
 * Delete mutation hook for OrgClaimedInvite
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deleteOrgClaimedInviteMutationDocument = `
mutation DeleteOrgClaimedInviteMutation($input: DeleteOrgClaimedInviteInput!) {
  deleteOrgClaimedInvite(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeleteOrgClaimedInviteMutationVariables {
  input: {
    id: string;
  };
}

export interface DeleteOrgClaimedInviteMutationResult {
  deleteOrgClaimedInvite: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a OrgClaimedInvite
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteOrgClaimedInviteMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeleteOrgClaimedInviteMutation(
  options?: Omit<
    UseMutationOptions<
      DeleteOrgClaimedInviteMutationResult,
      Error,
      DeleteOrgClaimedInviteMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeleteOrgClaimedInviteMutationVariables) =>
      execute<
        DeleteOrgClaimedInviteMutationResult,
        DeleteOrgClaimedInviteMutationVariables
      >(deleteOrgClaimedInviteMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['orgclaimedinvite', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['orgclaimedinvite', 'list'] });
    },
    ...options,
  });
}
