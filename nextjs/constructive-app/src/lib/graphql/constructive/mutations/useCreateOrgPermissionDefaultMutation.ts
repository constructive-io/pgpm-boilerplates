/**
 * Create mutation hook for OrgPermissionDefault
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type OrgPermissionDefault } from '../types';

// Re-export entity type for convenience
export type { OrgPermissionDefault };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createOrgPermissionDefaultMutationDocument = `
mutation CreateOrgPermissionDefaultMutation($input: CreateOrgPermissionDefaultInput!) {
  createOrgPermissionDefault(input: $input) {
    orgPermissionDefault {
      id
      permissions
      entityId
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a OrgPermissionDefault */
interface OrgPermissionDefaultCreateInput {
  permissions?: string | null;
  entityId?: string | null;
}

export interface CreateOrgPermissionDefaultMutationVariables {
  input: {
    orgPermissionDefault: OrgPermissionDefaultCreateInput;
  };
}

export interface CreateOrgPermissionDefaultMutationResult {
  createOrgPermissionDefault: {
    orgPermissionDefault: OrgPermissionDefault;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a OrgPermissionDefault
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgPermissionDefaultMutation();
 *
 * mutate({
 *   input: {
 *     orgPermissionDefault: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateOrgPermissionDefaultMutation(
  options?: Omit<
    UseMutationOptions<
      CreateOrgPermissionDefaultMutationResult,
      Error,
      CreateOrgPermissionDefaultMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateOrgPermissionDefaultMutationVariables) =>
      execute<
        CreateOrgPermissionDefaultMutationResult,
        CreateOrgPermissionDefaultMutationVariables
      >(createOrgPermissionDefaultMutationDocument, variables),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({
        queryKey: ['orgpermissiondefault', 'list'],
      });
    },
    ...options,
  });
}
