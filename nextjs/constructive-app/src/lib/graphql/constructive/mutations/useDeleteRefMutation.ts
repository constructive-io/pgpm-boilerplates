/**
 * Delete mutation hook for Ref
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deleteRefMutationDocument = `
mutation DeleteRefMutation($input: DeleteRefInput!) {
  deleteRef(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeleteRefMutationVariables {
  input: {
    id: string;
  };
}

export interface DeleteRefMutationResult {
  deleteRef: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a Ref
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteRefMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeleteRefMutation(
  options?: Omit<
    UseMutationOptions<
      DeleteRefMutationResult,
      Error,
      DeleteRefMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeleteRefMutationVariables) =>
      execute<DeleteRefMutationResult, DeleteRefMutationVariables>(
        deleteRefMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['ref', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['ref', 'list'] });
    },
    ...options,
  });
}
