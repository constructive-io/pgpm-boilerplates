/**
 * Create mutation hook for AppLevel
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type AppLevel } from '../types';

// Re-export entity type for convenience
export type { AppLevel };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createAppLevelMutationDocument = `
mutation CreateAppLevelMutation($input: CreateAppLevelInput!) {
  createAppLevel(input: $input) {
    appLevel {
      id
      name
      description
      image
      ownerId
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a AppLevel */
interface AppLevelCreateInput {
  name?: string | null;
  description?: string | null;
  image?: unknown | null;
  ownerId?: string | null;
}

export interface CreateAppLevelMutationVariables {
  input: {
    appLevel: AppLevelCreateInput;
  };
}

export interface CreateAppLevelMutationResult {
  createAppLevel: {
    appLevel: AppLevel;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a AppLevel
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateAppLevelMutation();
 *
 * mutate({
 *   input: {
 *     appLevel: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateAppLevelMutation(
  options?: Omit<
    UseMutationOptions<
      CreateAppLevelMutationResult,
      Error,
      CreateAppLevelMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateAppLevelMutationVariables) =>
      execute<CreateAppLevelMutationResult, CreateAppLevelMutationVariables>(
        createAppLevelMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['applevel', 'list'] });
    },
    ...options,
  });
}
