/**
 * Delete mutation hook for Commit
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deleteCommitMutationDocument = `
mutation DeleteCommitMutation($input: DeleteCommitInput!) {
  deleteCommit(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeleteCommitMutationVariables {
  input: {
    id: string;
  };
}

export interface DeleteCommitMutationResult {
  deleteCommit: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a Commit
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteCommitMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeleteCommitMutation(
  options?: Omit<
    UseMutationOptions<
      DeleteCommitMutationResult,
      Error,
      DeleteCommitMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeleteCommitMutationVariables) =>
      execute<DeleteCommitMutationResult, DeleteCommitMutationVariables>(
        deleteCommitMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['commit', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['commit', 'list'] });
    },
    ...options,
  });
}
