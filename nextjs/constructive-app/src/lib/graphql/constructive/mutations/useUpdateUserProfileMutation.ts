/**
 * Update mutation hook for UserProfile
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type UserProfile } from '../types';

// Re-export entity type for convenience
export type { UserProfile };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateUserProfileMutationDocument = `
mutation UpdateUserProfileMutation($input: UpdateUserProfileInput!) {
  updateUserProfile(input: $input) {
    userProfile {
      id
      ownerId
      profilePicture
      bio
      firstName
      lastName
      tags
      desired
      createdAt
      updatedAt
      createdBy
      updatedBy
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a UserProfile - all fields optional */
interface UserProfilePatch {
  ownerId?: string | null;
  profilePicture?: unknown | null;
  bio?: string | null;
  firstName?: string | null;
  lastName?: string | null;
  tags?: string[] | null;
  desired?: string[] | null;
  createdAt?: string | null;
  updatedAt?: string | null;
  createdBy?: string | null;
  updatedBy?: string | null;
}

export interface UpdateUserProfileMutationVariables {
  input: {
    id: string;
    patch: UserProfilePatch;
  };
}

export interface UpdateUserProfileMutationResult {
  updateUserProfile: {
    userProfile: UserProfile;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a UserProfile
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateUserProfileMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateUserProfileMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateUserProfileMutationResult,
      Error,
      UpdateUserProfileMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateUserProfileMutationVariables) =>
      execute<
        UpdateUserProfileMutationResult,
        UpdateUserProfileMutationVariables
      >(updateUserProfileMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['userprofile', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['userprofile', 'list'] });
    },
    ...options,
  });
}
