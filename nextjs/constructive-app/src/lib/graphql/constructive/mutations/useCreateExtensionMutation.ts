/**
 * Create mutation hook for Extension
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Extension } from '../types';

// Re-export entity type for convenience
export type { Extension };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createExtensionMutationDocument = `
mutation CreateExtensionMutation($input: CreateExtensionInput!) {
  createExtension(input: $input) {
    extension {
      name
      publicSchemas
      privateSchemas
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a Extension */
interface ExtensionCreateInput {
  name?: string | null;
  publicSchemas?: string[] | null;
  privateSchemas?: string[] | null;
}

export interface CreateExtensionMutationVariables {
  input: {
    extension: ExtensionCreateInput;
  };
}

export interface CreateExtensionMutationResult {
  createExtension: {
    extension: Extension;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a Extension
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateExtensionMutation();
 *
 * mutate({
 *   input: {
 *     extension: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateExtensionMutation(
  options?: Omit<
    UseMutationOptions<
      CreateExtensionMutationResult,
      Error,
      CreateExtensionMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateExtensionMutationVariables) =>
      execute<CreateExtensionMutationResult, CreateExtensionMutationVariables>(
        createExtensionMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['extension', 'list'] });
    },
    ...options,
  });
}
