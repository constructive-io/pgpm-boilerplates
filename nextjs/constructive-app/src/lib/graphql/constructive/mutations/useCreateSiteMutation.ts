/**
 * Create mutation hook for Site
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Site } from '../types';

// Re-export entity type for convenience
export type { Site };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createSiteMutationDocument = `
mutation CreateSiteMutation($input: CreateSiteInput!) {
  createSite(input: $input) {
    site {
      id
      databaseId
      title
      description
      ogImage
      favicon
      appleTouchIcon
      logo
      dbname
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a Site */
interface SiteCreateInput {
  databaseId?: string | null;
  title?: string | null;
  description?: string | null;
  ogImage?: unknown | null;
  favicon?: string | null;
  appleTouchIcon?: unknown | null;
  logo?: unknown | null;
  dbname?: string | null;
}

export interface CreateSiteMutationVariables {
  input: {
    site: SiteCreateInput;
  };
}

export interface CreateSiteMutationResult {
  createSite: {
    site: Site;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a Site
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateSiteMutation();
 *
 * mutate({
 *   input: {
 *     site: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateSiteMutation(
  options?: Omit<
    UseMutationOptions<
      CreateSiteMutationResult,
      Error,
      CreateSiteMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateSiteMutationVariables) =>
      execute<CreateSiteMutationResult, CreateSiteMutationVariables>(
        createSiteMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['site', 'list'] });
    },
    ...options,
  });
}
