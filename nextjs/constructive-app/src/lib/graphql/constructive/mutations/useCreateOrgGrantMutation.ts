/**
 * Create mutation hook for OrgGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type OrgGrant } from '../types';

// Re-export entity type for convenience
export type { OrgGrant };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createOrgGrantMutationDocument = `
mutation CreateOrgGrantMutation($input: CreateOrgGrantInput!) {
  createOrgGrant(input: $input) {
    orgGrant {
      id
      permissions
      isGrant
      actorId
      entityId
      grantorId
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a OrgGrant */
interface OrgGrantCreateInput {
  permissions?: string | null;
  isGrant?: boolean | null;
  actorId?: string | null;
  entityId?: string | null;
  grantorId?: string | null;
}

export interface CreateOrgGrantMutationVariables {
  input: {
    orgGrant: OrgGrantCreateInput;
  };
}

export interface CreateOrgGrantMutationResult {
  createOrgGrant: {
    orgGrant: OrgGrant;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a OrgGrant
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgGrantMutation();
 *
 * mutate({
 *   input: {
 *     orgGrant: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateOrgGrantMutation(
  options?: Omit<
    UseMutationOptions<
      CreateOrgGrantMutationResult,
      Error,
      CreateOrgGrantMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateOrgGrantMutationVariables) =>
      execute<CreateOrgGrantMutationResult, CreateOrgGrantMutationVariables>(
        createOrgGrantMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['orggrant', 'list'] });
    },
    ...options,
  });
}
