/**
 * Update mutation hook for SiteModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type SiteModule } from '../types';

// Re-export entity type for convenience
export type { SiteModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateSiteModuleMutationDocument = `
mutation UpdateSiteModuleMutation($input: UpdateSiteModuleInput!) {
  updateSiteModule(input: $input) {
    siteModule {
      id
      databaseId
      siteId
      name
      data
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a SiteModule - all fields optional */
interface SiteModulePatch {
  databaseId?: string | null;
  siteId?: string | null;
  name?: string | null;
  data?: unknown | null;
}

export interface UpdateSiteModuleMutationVariables {
  input: {
    id: string;
    patch: SiteModulePatch;
  };
}

export interface UpdateSiteModuleMutationResult {
  updateSiteModule: {
    siteModule: SiteModule;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a SiteModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateSiteModuleMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateSiteModuleMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateSiteModuleMutationResult,
      Error,
      UpdateSiteModuleMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateSiteModuleMutationVariables) =>
      execute<
        UpdateSiteModuleMutationResult,
        UpdateSiteModuleMutationVariables
      >(updateSiteModuleMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['sitemodule', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['sitemodule', 'list'] });
    },
    ...options,
  });
}
