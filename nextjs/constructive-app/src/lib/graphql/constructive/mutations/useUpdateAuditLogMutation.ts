/**
 * Update mutation hook for AuditLog
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type AuditLog } from '../types';

// Re-export entity type for convenience
export type { AuditLog };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateAuditLogMutationDocument = `
mutation UpdateAuditLogMutation($input: UpdateAuditLogInput!) {
  updateAuditLog(input: $input) {
    auditLog {
      id
      event
      actorId
      origin
      userAgent
      ipAddress
      success
      createdAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a AuditLog - all fields optional */
interface AuditLogPatch {
  event?: string | null;
  actorId?: string | null;
  origin?: string | null;
  userAgent?: string | null;
  ipAddress?: string | null;
  success?: boolean | null;
  createdAt?: string | null;
}

export interface UpdateAuditLogMutationVariables {
  input: {
    id: string;
    patch: AuditLogPatch;
  };
}

export interface UpdateAuditLogMutationResult {
  updateAuditLog: {
    auditLog: AuditLog;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a AuditLog
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAuditLogMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateAuditLogMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateAuditLogMutationResult,
      Error,
      UpdateAuditLogMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateAuditLogMutationVariables) =>
      execute<UpdateAuditLogMutationResult, UpdateAuditLogMutationVariables>(
        updateAuditLogMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['auditlog', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['auditlog', 'list'] });
    },
    ...options,
  });
}
