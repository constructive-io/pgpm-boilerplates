/**
 * Create mutation hook for OrgMember
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type OrgMember } from '../types';

// Re-export entity type for convenience
export type { OrgMember };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createOrgMemberMutationDocument = `
mutation CreateOrgMemberMutation($input: CreateOrgMemberInput!) {
  createOrgMember(input: $input) {
    orgMember {
      id
      isAdmin
      actorId
      entityId
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a OrgMember */
interface OrgMemberCreateInput {
  isAdmin?: boolean | null;
  actorId?: string | null;
  entityId?: string | null;
}

export interface CreateOrgMemberMutationVariables {
  input: {
    orgMember: OrgMemberCreateInput;
  };
}

export interface CreateOrgMemberMutationResult {
  createOrgMember: {
    orgMember: OrgMember;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a OrgMember
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgMemberMutation();
 *
 * mutate({
 *   input: {
 *     orgMember: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateOrgMemberMutation(
  options?: Omit<
    UseMutationOptions<
      CreateOrgMemberMutationResult,
      Error,
      CreateOrgMemberMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateOrgMemberMutationVariables) =>
      execute<CreateOrgMemberMutationResult, CreateOrgMemberMutationVariables>(
        createOrgMemberMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['orgmember', 'list'] });
    },
    ...options,
  });
}
