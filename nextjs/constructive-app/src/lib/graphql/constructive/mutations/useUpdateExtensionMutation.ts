/**
 * Update mutation hook for Extension
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Extension } from '../types';

// Re-export entity type for convenience
export type { Extension };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateExtensionMutationDocument = `
mutation UpdateExtensionMutation($input: UpdateExtensionInput!) {
  updateExtension(input: $input) {
    extension {
      name
      publicSchemas
      privateSchemas
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a Extension - all fields optional */
interface ExtensionPatch {
  name?: string | null;
  publicSchemas?: string[] | null;
  privateSchemas?: string[] | null;
}

export interface UpdateExtensionMutationVariables {
  input: {
    id: string;
    patch: ExtensionPatch;
  };
}

export interface UpdateExtensionMutationResult {
  updateExtension: {
    extension: Extension;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a Extension
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateExtensionMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateExtensionMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateExtensionMutationResult,
      Error,
      UpdateExtensionMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateExtensionMutationVariables) =>
      execute<UpdateExtensionMutationResult, UpdateExtensionMutationVariables>(
        updateExtensionMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['extension', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['extension', 'list'] });
    },
    ...options,
  });
}
