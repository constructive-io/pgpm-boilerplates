/**
 * Create mutation hook for FieldModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type FieldModule } from '../types';

// Re-export entity type for convenience
export type { FieldModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createFieldModuleMutationDocument = `
mutation CreateFieldModuleMutation($input: CreateFieldModuleInput!) {
  createFieldModule(input: $input) {
    fieldModule {
      id
      databaseId
      privateSchemaId
      tableId
      fieldId
      data
      triggers
      functions
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a FieldModule */
interface FieldModuleCreateInput {
  databaseId?: string | null;
  privateSchemaId?: string | null;
  tableId?: string | null;
  fieldId?: string | null;
  data?: unknown | null;
  triggers?: string[] | null;
  functions?: string[] | null;
}

export interface CreateFieldModuleMutationVariables {
  input: {
    fieldModule: FieldModuleCreateInput;
  };
}

export interface CreateFieldModuleMutationResult {
  createFieldModule: {
    fieldModule: FieldModule;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a FieldModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateFieldModuleMutation();
 *
 * mutate({
 *   input: {
 *     fieldModule: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateFieldModuleMutation(
  options?: Omit<
    UseMutationOptions<
      CreateFieldModuleMutationResult,
      Error,
      CreateFieldModuleMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateFieldModuleMutationVariables) =>
      execute<
        CreateFieldModuleMutationResult,
        CreateFieldModuleMutationVariables
      >(createFieldModuleMutationDocument, variables),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['fieldmodule', 'list'] });
    },
    ...options,
  });
}
