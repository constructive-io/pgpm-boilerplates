/**
 * Update mutation hook for OrgPermission
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type OrgPermission } from '../types';

// Re-export entity type for convenience
export type { OrgPermission };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateOrgPermissionMutationDocument = `
mutation UpdateOrgPermissionMutation($input: UpdateOrgPermissionInput!) {
  updateOrgPermission(input: $input) {
    orgPermission {
      id
      name
      bitnum
      bitstr
      description
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a OrgPermission - all fields optional */
interface OrgPermissionPatch {
  name?: string | null;
  bitnum?: number | null;
  bitstr?: string | null;
  description?: string | null;
}

export interface UpdateOrgPermissionMutationVariables {
  input: {
    id: string;
    patch: OrgPermissionPatch;
  };
}

export interface UpdateOrgPermissionMutationResult {
  updateOrgPermission: {
    orgPermission: OrgPermission;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a OrgPermission
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateOrgPermissionMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateOrgPermissionMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateOrgPermissionMutationResult,
      Error,
      UpdateOrgPermissionMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateOrgPermissionMutationVariables) =>
      execute<
        UpdateOrgPermissionMutationResult,
        UpdateOrgPermissionMutationVariables
      >(updateOrgPermissionMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['orgpermission', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['orgpermission', 'list'] });
    },
    ...options,
  });
}
