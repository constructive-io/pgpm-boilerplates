/**
 * Update mutation hook for PhoneNumber
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type PhoneNumber } from '../types';

// Re-export entity type for convenience
export type { PhoneNumber };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updatePhoneNumberMutationDocument = `
mutation UpdatePhoneNumberMutation($input: UpdatePhoneNumberInput!) {
  updatePhoneNumber(input: $input) {
    phoneNumber {
      id
      ownerId
      cc
      number
      isVerified
      isPrimary
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a PhoneNumber - all fields optional */
interface PhoneNumberPatch {
  ownerId?: string | null;
  cc?: string | null;
  number?: string | null;
  isVerified?: boolean | null;
  isPrimary?: boolean | null;
  createdAt?: string | null;
  updatedAt?: string | null;
}

export interface UpdatePhoneNumberMutationVariables {
  input: {
    id: string;
    patch: PhoneNumberPatch;
  };
}

export interface UpdatePhoneNumberMutationResult {
  updatePhoneNumber: {
    phoneNumber: PhoneNumber;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a PhoneNumber
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdatePhoneNumberMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdatePhoneNumberMutation(
  options?: Omit<
    UseMutationOptions<
      UpdatePhoneNumberMutationResult,
      Error,
      UpdatePhoneNumberMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdatePhoneNumberMutationVariables) =>
      execute<
        UpdatePhoneNumberMutationResult,
        UpdatePhoneNumberMutationVariables
      >(updatePhoneNumberMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['phonenumber', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['phonenumber', 'list'] });
    },
    ...options,
  });
}
