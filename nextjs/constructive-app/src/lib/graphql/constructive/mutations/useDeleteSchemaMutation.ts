/**
 * Delete mutation hook for Schema
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deleteSchemaMutationDocument = `
mutation DeleteSchemaMutation($input: DeleteSchemaInput!) {
  deleteSchema(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeleteSchemaMutationVariables {
  input: {
    id: string;
  };
}

export interface DeleteSchemaMutationResult {
  deleteSchema: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a Schema
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteSchemaMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeleteSchemaMutation(
  options?: Omit<
    UseMutationOptions<
      DeleteSchemaMutationResult,
      Error,
      DeleteSchemaMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeleteSchemaMutationVariables) =>
      execute<DeleteSchemaMutationResult, DeleteSchemaMutationVariables>(
        deleteSchemaMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['schema', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['schema', 'list'] });
    },
    ...options,
  });
}
