/**
 * Delete mutation hook for FieldModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deleteFieldModuleMutationDocument = `
mutation DeleteFieldModuleMutation($input: DeleteFieldModuleInput!) {
  deleteFieldModule(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeleteFieldModuleMutationVariables {
  input: {
    id: string;
  };
}

export interface DeleteFieldModuleMutationResult {
  deleteFieldModule: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a FieldModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteFieldModuleMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeleteFieldModuleMutation(
  options?: Omit<
    UseMutationOptions<
      DeleteFieldModuleMutationResult,
      Error,
      DeleteFieldModuleMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeleteFieldModuleMutationVariables) =>
      execute<
        DeleteFieldModuleMutationResult,
        DeleteFieldModuleMutationVariables
      >(deleteFieldModuleMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['fieldmodule', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['fieldmodule', 'list'] });
    },
    ...options,
  });
}
