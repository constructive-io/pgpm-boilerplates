/**
 * Update mutation hook for AppAchievement
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type AppAchievement } from '../types';

// Re-export entity type for convenience
export type { AppAchievement };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateAppAchievementMutationDocument = `
mutation UpdateAppAchievementMutation($input: UpdateAppAchievementInput!) {
  updateAppAchievement(input: $input) {
    appAchievement {
      id
      actorId
      name
      count
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a AppAchievement - all fields optional */
interface AppAchievementPatch {
  actorId?: string | null;
  name?: string | null;
  count?: number | null;
  createdAt?: string | null;
  updatedAt?: string | null;
}

export interface UpdateAppAchievementMutationVariables {
  input: {
    id: string;
    patch: AppAchievementPatch;
  };
}

export interface UpdateAppAchievementMutationResult {
  updateAppAchievement: {
    appAchievement: AppAchievement;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a AppAchievement
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppAchievementMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateAppAchievementMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateAppAchievementMutationResult,
      Error,
      UpdateAppAchievementMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateAppAchievementMutationVariables) =>
      execute<
        UpdateAppAchievementMutationResult,
        UpdateAppAchievementMutationVariables
      >(updateAppAchievementMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['appachievement', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['appachievement', 'list'] });
    },
    ...options,
  });
}
