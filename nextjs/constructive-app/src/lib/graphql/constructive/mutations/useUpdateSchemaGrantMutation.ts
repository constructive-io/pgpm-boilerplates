/**
 * Update mutation hook for SchemaGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type SchemaGrant } from '../types';

// Re-export entity type for convenience
export type { SchemaGrant };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateSchemaGrantMutationDocument = `
mutation UpdateSchemaGrantMutation($input: UpdateSchemaGrantInput!) {
  updateSchemaGrant(input: $input) {
    schemaGrant {
      id
      databaseId
      schemaId
      granteeName
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a SchemaGrant - all fields optional */
interface SchemaGrantPatch {
  databaseId?: string | null;
  schemaId?: string | null;
  granteeName?: string | null;
  createdAt?: string | null;
  updatedAt?: string | null;
}

export interface UpdateSchemaGrantMutationVariables {
  input: {
    id: string;
    patch: SchemaGrantPatch;
  };
}

export interface UpdateSchemaGrantMutationResult {
  updateSchemaGrant: {
    schemaGrant: SchemaGrant;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a SchemaGrant
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateSchemaGrantMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateSchemaGrantMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateSchemaGrantMutationResult,
      Error,
      UpdateSchemaGrantMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateSchemaGrantMutationVariables) =>
      execute<
        UpdateSchemaGrantMutationResult,
        UpdateSchemaGrantMutationVariables
      >(updateSchemaGrantMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['schemagrant', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['schemagrant', 'list'] });
    },
    ...options,
  });
}
