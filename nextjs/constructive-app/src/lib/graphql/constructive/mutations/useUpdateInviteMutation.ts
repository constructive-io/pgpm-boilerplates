/**
 * Update mutation hook for Invite
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Invite } from '../types';

// Re-export entity type for convenience
export type { Invite };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateInviteMutationDocument = `
mutation UpdateInviteMutation($input: UpdateInviteInput!) {
  updateInvite(input: $input) {
    invite {
      id
      email
      senderId
      inviteToken
      inviteValid
      inviteLimit
      inviteCount
      multiple
      data
      expiresAt
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a Invite - all fields optional */
interface InvitePatch {
  email?: string | null;
  senderId?: string | null;
  inviteToken?: string | null;
  inviteValid?: boolean | null;
  inviteLimit?: number | null;
  inviteCount?: number | null;
  multiple?: boolean | null;
  data?: unknown | null;
  expiresAt?: string | null;
  createdAt?: string | null;
  updatedAt?: string | null;
}

export interface UpdateInviteMutationVariables {
  input: {
    id: string;
    patch: InvitePatch;
  };
}

export interface UpdateInviteMutationResult {
  updateInvite: {
    invite: Invite;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a Invite
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateInviteMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateInviteMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateInviteMutationResult,
      Error,
      UpdateInviteMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateInviteMutationVariables) =>
      execute<UpdateInviteMutationResult, UpdateInviteMutationVariables>(
        updateInviteMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['invite', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['invite', 'list'] });
    },
    ...options,
  });
}
