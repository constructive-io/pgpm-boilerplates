/**
 * Create mutation hook for SchemaGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type SchemaGrant } from '../types';

// Re-export entity type for convenience
export type { SchemaGrant };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createSchemaGrantMutationDocument = `
mutation CreateSchemaGrantMutation($input: CreateSchemaGrantInput!) {
  createSchemaGrant(input: $input) {
    schemaGrant {
      id
      databaseId
      schemaId
      granteeName
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a SchemaGrant */
interface SchemaGrantCreateInput {
  databaseId?: string | null;
  schemaId?: string | null;
  granteeName?: string | null;
}

export interface CreateSchemaGrantMutationVariables {
  input: {
    schemaGrant: SchemaGrantCreateInput;
  };
}

export interface CreateSchemaGrantMutationResult {
  createSchemaGrant: {
    schemaGrant: SchemaGrant;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a SchemaGrant
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateSchemaGrantMutation();
 *
 * mutate({
 *   input: {
 *     schemaGrant: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateSchemaGrantMutation(
  options?: Omit<
    UseMutationOptions<
      CreateSchemaGrantMutationResult,
      Error,
      CreateSchemaGrantMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateSchemaGrantMutationVariables) =>
      execute<
        CreateSchemaGrantMutationResult,
        CreateSchemaGrantMutationVariables
      >(createSchemaGrantMutationDocument, variables),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['schemagrant', 'list'] });
    },
    ...options,
  });
}
