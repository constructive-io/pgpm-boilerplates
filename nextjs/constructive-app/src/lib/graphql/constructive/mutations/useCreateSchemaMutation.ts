/**
 * Create mutation hook for Schema
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Schema } from '../types';
import { type ObjectCategory } from '../schema-types';

// Re-export entity type for convenience
export type { Schema };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createSchemaMutationDocument = `
mutation CreateSchemaMutation($input: CreateSchemaInput!) {
  createSchema(input: $input) {
    schema {
      id
      databaseId
      name
      schemaName
      label
      description
      smartTags
      category
      module
      scope
      tags
      isPublic
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a Schema */
interface SchemaCreateInput {
  databaseId?: string | null;
  name?: string | null;
  schemaName?: string | null;
  label?: string | null;
  description?: string | null;
  smartTags?: unknown | null;
  category?: ObjectCategory | null;
  module?: string | null;
  scope?: number | null;
  tags?: string[] | null;
  isPublic?: boolean | null;
}

export interface CreateSchemaMutationVariables {
  input: {
    schema: SchemaCreateInput;
  };
}

export interface CreateSchemaMutationResult {
  createSchema: {
    schema: Schema;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a Schema
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateSchemaMutation();
 *
 * mutate({
 *   input: {
 *     schema: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateSchemaMutation(
  options?: Omit<
    UseMutationOptions<
      CreateSchemaMutationResult,
      Error,
      CreateSchemaMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateSchemaMutationVariables) =>
      execute<CreateSchemaMutationResult, CreateSchemaMutationVariables>(
        createSchemaMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['schema', 'list'] });
    },
    ...options,
  });
}
