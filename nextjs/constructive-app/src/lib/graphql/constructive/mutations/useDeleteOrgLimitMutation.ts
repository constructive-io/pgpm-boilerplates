/**
 * Delete mutation hook for OrgLimit
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deleteOrgLimitMutationDocument = `
mutation DeleteOrgLimitMutation($input: DeleteOrgLimitInput!) {
  deleteOrgLimit(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeleteOrgLimitMutationVariables {
  input: {
    id: string;
  };
}

export interface DeleteOrgLimitMutationResult {
  deleteOrgLimit: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a OrgLimit
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteOrgLimitMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeleteOrgLimitMutation(
  options?: Omit<
    UseMutationOptions<
      DeleteOrgLimitMutationResult,
      Error,
      DeleteOrgLimitMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeleteOrgLimitMutationVariables) =>
      execute<DeleteOrgLimitMutationResult, DeleteOrgLimitMutationVariables>(
        deleteOrgLimitMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['orglimit', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['orglimit', 'list'] });
    },
    ...options,
  });
}
