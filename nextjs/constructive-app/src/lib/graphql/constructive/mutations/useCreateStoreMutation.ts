/**
 * Create mutation hook for Store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Store } from '../types';

// Re-export entity type for convenience
export type { Store };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createStoreMutationDocument = `
mutation CreateStoreMutation($input: CreateStoreInput!) {
  createStore(input: $input) {
    store {
      id
      name
      databaseId
      hash
      createdAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a Store */
interface StoreCreateInput {
  name?: string | null;
  databaseId?: string | null;
  hash?: string | null;
}

export interface CreateStoreMutationVariables {
  input: {
    store: StoreCreateInput;
  };
}

export interface CreateStoreMutationResult {
  createStore: {
    store: Store;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a Store
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateStoreMutation();
 *
 * mutate({
 *   input: {
 *     store: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateStoreMutation(
  options?: Omit<
    UseMutationOptions<
      CreateStoreMutationResult,
      Error,
      CreateStoreMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateStoreMutationVariables) =>
      execute<CreateStoreMutationResult, CreateStoreMutationVariables>(
        createStoreMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['store', 'list'] });
    },
    ...options,
  });
}
