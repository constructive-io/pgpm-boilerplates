/**
 * Delete mutation hook for SiteMetadatum
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deleteSiteMetadatumMutationDocument = `
mutation DeleteSiteMetadatumMutation($input: DeleteSiteMetadatumInput!) {
  deleteSiteMetadatum(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeleteSiteMetadatumMutationVariables {
  input: {
    id: string;
  };
}

export interface DeleteSiteMetadatumMutationResult {
  deleteSiteMetadatum: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a SiteMetadatum
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteSiteMetadatumMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeleteSiteMetadatumMutation(
  options?: Omit<
    UseMutationOptions<
      DeleteSiteMetadatumMutationResult,
      Error,
      DeleteSiteMetadatumMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeleteSiteMetadatumMutationVariables) =>
      execute<
        DeleteSiteMetadatumMutationResult,
        DeleteSiteMetadatumMutationVariables
      >(deleteSiteMetadatumMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['sitemetadatum', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['sitemetadatum', 'list'] });
    },
    ...options,
  });
}
