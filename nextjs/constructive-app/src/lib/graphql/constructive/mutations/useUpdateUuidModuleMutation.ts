/**
 * Update mutation hook for UuidModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type UuidModule } from '../types';

// Re-export entity type for convenience
export type { UuidModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateUuidModuleMutationDocument = `
mutation UpdateUuidModuleMutation($input: UpdateUuidModuleInput!) {
  updateUuidModule(input: $input) {
    uuidModule {
      id
      databaseId
      schemaId
      uuidFunction
      uuidSeed
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a UuidModule - all fields optional */
interface UuidModulePatch {
  databaseId?: string | null;
  schemaId?: string | null;
  uuidFunction?: string | null;
  uuidSeed?: string | null;
}

export interface UpdateUuidModuleMutationVariables {
  input: {
    id: string;
    patch: UuidModulePatch;
  };
}

export interface UpdateUuidModuleMutationResult {
  updateUuidModule: {
    uuidModule: UuidModule;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a UuidModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateUuidModuleMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateUuidModuleMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateUuidModuleMutationResult,
      Error,
      UpdateUuidModuleMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateUuidModuleMutationVariables) =>
      execute<
        UpdateUuidModuleMutationResult,
        UpdateUuidModuleMutationVariables
      >(updateUuidModuleMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['uuidmodule', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['uuidmodule', 'list'] });
    },
    ...options,
  });
}
