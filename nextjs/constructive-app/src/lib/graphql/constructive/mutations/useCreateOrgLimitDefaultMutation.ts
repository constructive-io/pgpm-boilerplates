/**
 * Create mutation hook for OrgLimitDefault
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type OrgLimitDefault } from '../types';

// Re-export entity type for convenience
export type { OrgLimitDefault };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createOrgLimitDefaultMutationDocument = `
mutation CreateOrgLimitDefaultMutation($input: CreateOrgLimitDefaultInput!) {
  createOrgLimitDefault(input: $input) {
    orgLimitDefault {
      id
      name
      max
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a OrgLimitDefault */
interface OrgLimitDefaultCreateInput {
  name?: string | null;
  max?: number | null;
}

export interface CreateOrgLimitDefaultMutationVariables {
  input: {
    orgLimitDefault: OrgLimitDefaultCreateInput;
  };
}

export interface CreateOrgLimitDefaultMutationResult {
  createOrgLimitDefault: {
    orgLimitDefault: OrgLimitDefault;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a OrgLimitDefault
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgLimitDefaultMutation();
 *
 * mutate({
 *   input: {
 *     orgLimitDefault: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateOrgLimitDefaultMutation(
  options?: Omit<
    UseMutationOptions<
      CreateOrgLimitDefaultMutationResult,
      Error,
      CreateOrgLimitDefaultMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateOrgLimitDefaultMutationVariables) =>
      execute<
        CreateOrgLimitDefaultMutationResult,
        CreateOrgLimitDefaultMutationVariables
      >(createOrgLimitDefaultMutationDocument, variables),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['orglimitdefault', 'list'] });
    },
    ...options,
  });
}
