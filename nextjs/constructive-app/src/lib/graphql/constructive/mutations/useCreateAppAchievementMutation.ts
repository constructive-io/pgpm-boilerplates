/**
 * Create mutation hook for AppAchievement
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type AppAchievement } from '../types';

// Re-export entity type for convenience
export type { AppAchievement };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createAppAchievementMutationDocument = `
mutation CreateAppAchievementMutation($input: CreateAppAchievementInput!) {
  createAppAchievement(input: $input) {
    appAchievement {
      id
      actorId
      name
      count
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a AppAchievement */
interface AppAchievementCreateInput {
  actorId?: string | null;
  name?: string | null;
  count?: number | null;
}

export interface CreateAppAchievementMutationVariables {
  input: {
    appAchievement: AppAchievementCreateInput;
  };
}

export interface CreateAppAchievementMutationResult {
  createAppAchievement: {
    appAchievement: AppAchievement;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a AppAchievement
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateAppAchievementMutation();
 *
 * mutate({
 *   input: {
 *     appAchievement: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateAppAchievementMutation(
  options?: Omit<
    UseMutationOptions<
      CreateAppAchievementMutationResult,
      Error,
      CreateAppAchievementMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateAppAchievementMutationVariables) =>
      execute<
        CreateAppAchievementMutationResult,
        CreateAppAchievementMutationVariables
      >(createAppAchievementMutationDocument, variables),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['appachievement', 'list'] });
    },
    ...options,
  });
}
