/**
 * Create mutation hook for Commit
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Commit } from '../types';

// Re-export entity type for convenience
export type { Commit };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createCommitMutationDocument = `
mutation CreateCommitMutation($input: CreateCommitInput!) {
  createCommit(input: $input) {
    commit {
      id
      message
      databaseId
      storeId
      parentIds
      authorId
      committerId
      treeId
      date
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a Commit */
interface CommitCreateInput {
  message?: string | null;
  databaseId?: string | null;
  storeId?: string | null;
  parentIds?: string[] | null;
  authorId?: string | null;
  committerId?: string | null;
  treeId?: string | null;
  date?: string | null;
}

export interface CreateCommitMutationVariables {
  input: {
    commit: CommitCreateInput;
  };
}

export interface CreateCommitMutationResult {
  createCommit: {
    commit: Commit;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a Commit
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateCommitMutation();
 *
 * mutate({
 *   input: {
 *     commit: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateCommitMutation(
  options?: Omit<
    UseMutationOptions<
      CreateCommitMutationResult,
      Error,
      CreateCommitMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateCommitMutationVariables) =>
      execute<CreateCommitMutationResult, CreateCommitMutationVariables>(
        createCommitMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['commit', 'list'] });
    },
    ...options,
  });
}
