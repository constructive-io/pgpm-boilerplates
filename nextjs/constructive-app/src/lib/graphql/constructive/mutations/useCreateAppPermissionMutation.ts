/**
 * Create mutation hook for AppPermission
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type AppPermission } from '../types';

// Re-export entity type for convenience
export type { AppPermission };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createAppPermissionMutationDocument = `
mutation CreateAppPermissionMutation($input: CreateAppPermissionInput!) {
  createAppPermission(input: $input) {
    appPermission {
      id
      name
      bitnum
      bitstr
      description
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a AppPermission */
interface AppPermissionCreateInput {
  name?: string | null;
  bitnum?: number | null;
  bitstr?: string | null;
  description?: string | null;
}

export interface CreateAppPermissionMutationVariables {
  input: {
    appPermission: AppPermissionCreateInput;
  };
}

export interface CreateAppPermissionMutationResult {
  createAppPermission: {
    appPermission: AppPermission;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a AppPermission
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateAppPermissionMutation();
 *
 * mutate({
 *   input: {
 *     appPermission: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateAppPermissionMutation(
  options?: Omit<
    UseMutationOptions<
      CreateAppPermissionMutationResult,
      Error,
      CreateAppPermissionMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateAppPermissionMutationVariables) =>
      execute<
        CreateAppPermissionMutationResult,
        CreateAppPermissionMutationVariables
      >(createAppPermissionMutationDocument, variables),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['apppermission', 'list'] });
    },
    ...options,
  });
}
