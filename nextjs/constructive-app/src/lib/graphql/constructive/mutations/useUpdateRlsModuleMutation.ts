/**
 * Update mutation hook for RlsModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type RlsModule } from '../types';

// Re-export entity type for convenience
export type { RlsModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateRlsModuleMutationDocument = `
mutation UpdateRlsModuleMutation($input: UpdateRlsModuleInput!) {
  updateRlsModule(input: $input) {
    rlsModule {
      id
      databaseId
      apiId
      schemaId
      privateSchemaId
      tokensTableId
      usersTableId
      authenticate
      authenticateStrict
      currentRole
      currentRoleId
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a RlsModule - all fields optional */
interface RlsModulePatch {
  databaseId?: string | null;
  apiId?: string | null;
  schemaId?: string | null;
  privateSchemaId?: string | null;
  tokensTableId?: string | null;
  usersTableId?: string | null;
  authenticate?: string | null;
  authenticateStrict?: string | null;
  currentRole?: string | null;
  currentRoleId?: string | null;
}

export interface UpdateRlsModuleMutationVariables {
  input: {
    id: string;
    patch: RlsModulePatch;
  };
}

export interface UpdateRlsModuleMutationResult {
  updateRlsModule: {
    rlsModule: RlsModule;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a RlsModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateRlsModuleMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateRlsModuleMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateRlsModuleMutationResult,
      Error,
      UpdateRlsModuleMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateRlsModuleMutationVariables) =>
      execute<UpdateRlsModuleMutationResult, UpdateRlsModuleMutationVariables>(
        updateRlsModuleMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['rlsmodule', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['rlsmodule', 'list'] });
    },
    ...options,
  });
}
