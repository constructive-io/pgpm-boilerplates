/**
 * Update mutation hook for AppLimit
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type AppLimit } from '../types';

// Re-export entity type for convenience
export type { AppLimit };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateAppLimitMutationDocument = `
mutation UpdateAppLimitMutation($input: UpdateAppLimitInput!) {
  updateAppLimit(input: $input) {
    appLimit {
      id
      name
      actorId
      num
      max
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a AppLimit - all fields optional */
interface AppLimitPatch {
  name?: string | null;
  actorId?: string | null;
  num?: number | null;
  max?: number | null;
}

export interface UpdateAppLimitMutationVariables {
  input: {
    id: string;
    patch: AppLimitPatch;
  };
}

export interface UpdateAppLimitMutationResult {
  updateAppLimit: {
    appLimit: AppLimit;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a AppLimit
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppLimitMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateAppLimitMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateAppLimitMutationResult,
      Error,
      UpdateAppLimitMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateAppLimitMutationVariables) =>
      execute<UpdateAppLimitMutationResult, UpdateAppLimitMutationVariables>(
        updateAppLimitMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['applimit', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['applimit', 'list'] });
    },
    ...options,
  });
}
