/**
 * Update mutation hook for OrgOwnerGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type OrgOwnerGrant } from '../types';

// Re-export entity type for convenience
export type { OrgOwnerGrant };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateOrgOwnerGrantMutationDocument = `
mutation UpdateOrgOwnerGrantMutation($input: UpdateOrgOwnerGrantInput!) {
  updateOrgOwnerGrant(input: $input) {
    orgOwnerGrant {
      id
      isGrant
      actorId
      entityId
      grantorId
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a OrgOwnerGrant - all fields optional */
interface OrgOwnerGrantPatch {
  isGrant?: boolean | null;
  actorId?: string | null;
  entityId?: string | null;
  grantorId?: string | null;
  createdAt?: string | null;
  updatedAt?: string | null;
}

export interface UpdateOrgOwnerGrantMutationVariables {
  input: {
    id: string;
    patch: OrgOwnerGrantPatch;
  };
}

export interface UpdateOrgOwnerGrantMutationResult {
  updateOrgOwnerGrant: {
    orgOwnerGrant: OrgOwnerGrant;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a OrgOwnerGrant
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateOrgOwnerGrantMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateOrgOwnerGrantMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateOrgOwnerGrantMutationResult,
      Error,
      UpdateOrgOwnerGrantMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateOrgOwnerGrantMutationVariables) =>
      execute<
        UpdateOrgOwnerGrantMutationResult,
        UpdateOrgOwnerGrantMutationVariables
      >(updateOrgOwnerGrantMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['orgownergrant', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['orgownergrant', 'list'] });
    },
    ...options,
  });
}
