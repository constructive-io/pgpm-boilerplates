/**
 * Update mutation hook for RoleType
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type RoleType } from '../types';

// Re-export entity type for convenience
export type { RoleType };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateRoleTypeMutationDocument = `
mutation UpdateRoleTypeMutation($input: UpdateRoleTypeInput!) {
  updateRoleType(input: $input) {
    roleType {
      id
      name
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a RoleType - all fields optional */
interface RoleTypePatch {
  name?: string | null;
}

export interface UpdateRoleTypeMutationVariables {
  input: {
    id: number;
    patch: RoleTypePatch;
  };
}

export interface UpdateRoleTypeMutationResult {
  updateRoleType: {
    roleType: RoleType;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a RoleType
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateRoleTypeMutation();
 *
 * mutate({
 *   input: {
 *     id: 123,
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateRoleTypeMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateRoleTypeMutationResult,
      Error,
      UpdateRoleTypeMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateRoleTypeMutationVariables) =>
      execute<UpdateRoleTypeMutationResult, UpdateRoleTypeMutationVariables>(
        updateRoleTypeMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['roletype', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['roletype', 'list'] });
    },
    ...options,
  });
}
