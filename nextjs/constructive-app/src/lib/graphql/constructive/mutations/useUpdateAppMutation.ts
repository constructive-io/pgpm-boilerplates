/**
 * Update mutation hook for App
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type App } from '../types';

// Re-export entity type for convenience
export type { App };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateAppMutationDocument = `
mutation UpdateAppMutation($input: UpdateAppInput!) {
  updateApp(input: $input) {
    app {
      id
      databaseId
      siteId
      name
      appImage
      appStoreLink
      appStoreId
      appIdPrefix
      playStoreLink
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a App - all fields optional */
interface AppPatch {
  databaseId?: string | null;
  siteId?: string | null;
  name?: string | null;
  appImage?: unknown | null;
  appStoreLink?: string | null;
  appStoreId?: string | null;
  appIdPrefix?: string | null;
  playStoreLink?: string | null;
}

export interface UpdateAppMutationVariables {
  input: {
    id: string;
    patch: AppPatch;
  };
}

export interface UpdateAppMutationResult {
  updateApp: {
    app: App;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a App
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateAppMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateAppMutationResult,
      Error,
      UpdateAppMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateAppMutationVariables) =>
      execute<UpdateAppMutationResult, UpdateAppMutationVariables>(
        updateAppMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['app', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['app', 'list'] });
    },
    ...options,
  });
}
