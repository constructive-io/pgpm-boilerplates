/**
 * Delete mutation hook for Database
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deleteDatabaseMutationDocument = `
mutation DeleteDatabaseMutation($input: DeleteDatabaseInput!) {
  deleteDatabase(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeleteDatabaseMutationVariables {
  input: {
    id: string;
  };
}

export interface DeleteDatabaseMutationResult {
  deleteDatabase: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a Database
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteDatabaseMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeleteDatabaseMutation(
  options?: Omit<
    UseMutationOptions<
      DeleteDatabaseMutationResult,
      Error,
      DeleteDatabaseMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeleteDatabaseMutationVariables) =>
      execute<DeleteDatabaseMutationResult, DeleteDatabaseMutationVariables>(
        deleteDatabaseMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['database', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['database', 'list'] });
    },
    ...options,
  });
}
