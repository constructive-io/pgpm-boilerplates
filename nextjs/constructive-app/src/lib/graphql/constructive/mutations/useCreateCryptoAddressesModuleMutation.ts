/**
 * Create mutation hook for CryptoAddressesModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type CryptoAddressesModule } from '../types';

// Re-export entity type for convenience
export type { CryptoAddressesModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createCryptoAddressesModuleMutationDocument = `
mutation CreateCryptoAddressesModuleMutation($input: CreateCryptoAddressesModuleInput!) {
  createCryptoAddressesModule(input: $input) {
    cryptoAddressesModule {
      id
      databaseId
      schemaId
      privateSchemaId
      tableId
      ownerTableId
      tableName
      cryptoNetwork
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a CryptoAddressesModule */
interface CryptoAddressesModuleCreateInput {
  databaseId?: string | null;
  schemaId?: string | null;
  privateSchemaId?: string | null;
  tableId?: string | null;
  ownerTableId?: string | null;
  tableName?: string | null;
  cryptoNetwork?: string | null;
}

export interface CreateCryptoAddressesModuleMutationVariables {
  input: {
    cryptoAddressesModule: CryptoAddressesModuleCreateInput;
  };
}

export interface CreateCryptoAddressesModuleMutationResult {
  createCryptoAddressesModule: {
    cryptoAddressesModule: CryptoAddressesModule;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a CryptoAddressesModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateCryptoAddressesModuleMutation();
 *
 * mutate({
 *   input: {
 *     cryptoAddressesModule: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateCryptoAddressesModuleMutation(
  options?: Omit<
    UseMutationOptions<
      CreateCryptoAddressesModuleMutationResult,
      Error,
      CreateCryptoAddressesModuleMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateCryptoAddressesModuleMutationVariables) =>
      execute<
        CreateCryptoAddressesModuleMutationResult,
        CreateCryptoAddressesModuleMutationVariables
      >(createCryptoAddressesModuleMutationDocument, variables),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({
        queryKey: ['cryptoaddressesmodule', 'list'],
      });
    },
    ...options,
  });
}
