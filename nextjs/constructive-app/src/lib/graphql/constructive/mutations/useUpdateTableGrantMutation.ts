/**
 * Update mutation hook for TableGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type TableGrant } from '../types';

// Re-export entity type for convenience
export type { TableGrant };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateTableGrantMutationDocument = `
mutation UpdateTableGrantMutation($input: UpdateTableGrantInput!) {
  updateTableGrant(input: $input) {
    tableGrant {
      id
      databaseId
      tableId
      privilege
      roleName
      fieldIds
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a TableGrant - all fields optional */
interface TableGrantPatch {
  databaseId?: string | null;
  tableId?: string | null;
  privilege?: string | null;
  roleName?: string | null;
  fieldIds?: string[] | null;
  createdAt?: string | null;
  updatedAt?: string | null;
}

export interface UpdateTableGrantMutationVariables {
  input: {
    id: string;
    patch: TableGrantPatch;
  };
}

export interface UpdateTableGrantMutationResult {
  updateTableGrant: {
    tableGrant: TableGrant;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a TableGrant
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateTableGrantMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateTableGrantMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateTableGrantMutationResult,
      Error,
      UpdateTableGrantMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateTableGrantMutationVariables) =>
      execute<
        UpdateTableGrantMutationResult,
        UpdateTableGrantMutationVariables
      >(updateTableGrantMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['tablegrant', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['tablegrant', 'list'] });
    },
    ...options,
  });
}
