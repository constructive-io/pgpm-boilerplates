/**
 * Update mutation hook for FieldModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type FieldModule } from '../types';

// Re-export entity type for convenience
export type { FieldModule };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateFieldModuleMutationDocument = `
mutation UpdateFieldModuleMutation($input: UpdateFieldModuleInput!) {
  updateFieldModule(input: $input) {
    fieldModule {
      id
      databaseId
      privateSchemaId
      tableId
      fieldId
      data
      triggers
      functions
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a FieldModule - all fields optional */
interface FieldModulePatch {
  databaseId?: string | null;
  privateSchemaId?: string | null;
  tableId?: string | null;
  fieldId?: string | null;
  data?: unknown | null;
  triggers?: string[] | null;
  functions?: string[] | null;
}

export interface UpdateFieldModuleMutationVariables {
  input: {
    id: string;
    patch: FieldModulePatch;
  };
}

export interface UpdateFieldModuleMutationResult {
  updateFieldModule: {
    fieldModule: FieldModule;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a FieldModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateFieldModuleMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateFieldModuleMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateFieldModuleMutationResult,
      Error,
      UpdateFieldModuleMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateFieldModuleMutationVariables) =>
      execute<
        UpdateFieldModuleMutationResult,
        UpdateFieldModuleMutationVariables
      >(updateFieldModuleMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['fieldmodule', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['fieldmodule', 'list'] });
    },
    ...options,
  });
}
