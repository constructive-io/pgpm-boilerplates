/**
 * Update mutation hook for AppLevel
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type AppLevel } from '../types';

// Re-export entity type for convenience
export type { AppLevel };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateAppLevelMutationDocument = `
mutation UpdateAppLevelMutation($input: UpdateAppLevelInput!) {
  updateAppLevel(input: $input) {
    appLevel {
      id
      name
      description
      image
      ownerId
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a AppLevel - all fields optional */
interface AppLevelPatch {
  name?: string | null;
  description?: string | null;
  image?: unknown | null;
  ownerId?: string | null;
  createdAt?: string | null;
  updatedAt?: string | null;
}

export interface UpdateAppLevelMutationVariables {
  input: {
    id: string;
    patch: AppLevelPatch;
  };
}

export interface UpdateAppLevelMutationResult {
  updateAppLevel: {
    appLevel: AppLevel;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a AppLevel
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppLevelMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateAppLevelMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateAppLevelMutationResult,
      Error,
      UpdateAppLevelMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateAppLevelMutationVariables) =>
      execute<UpdateAppLevelMutationResult, UpdateAppLevelMutationVariables>(
        updateAppLevelMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['applevel', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['applevel', 'list'] });
    },
    ...options,
  });
}
