/**
 * Update mutation hook for Schema
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Schema } from '../types';
import { type ObjectCategory } from '../schema-types';

// Re-export entity type for convenience
export type { Schema };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateSchemaMutationDocument = `
mutation UpdateSchemaMutation($input: UpdateSchemaInput!) {
  updateSchema(input: $input) {
    schema {
      id
      databaseId
      name
      schemaName
      label
      description
      smartTags
      category
      module
      scope
      tags
      isPublic
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a Schema - all fields optional */
interface SchemaPatch {
  databaseId?: string | null;
  name?: string | null;
  schemaName?: string | null;
  label?: string | null;
  description?: string | null;
  smartTags?: unknown | null;
  category?: ObjectCategory | null;
  module?: string | null;
  scope?: number | null;
  tags?: string[] | null;
  isPublic?: boolean | null;
  createdAt?: string | null;
  updatedAt?: string | null;
}

export interface UpdateSchemaMutationVariables {
  input: {
    id: string;
    patch: SchemaPatch;
  };
}

export interface UpdateSchemaMutationResult {
  updateSchema: {
    schema: Schema;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a Schema
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateSchemaMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateSchemaMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateSchemaMutationResult,
      Error,
      UpdateSchemaMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateSchemaMutationVariables) =>
      execute<UpdateSchemaMutationResult, UpdateSchemaMutationVariables>(
        updateSchemaMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['schema', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['schema', 'list'] });
    },
    ...options,
  });
}
