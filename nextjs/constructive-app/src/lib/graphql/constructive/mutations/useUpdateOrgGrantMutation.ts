/**
 * Update mutation hook for OrgGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type OrgGrant } from '../types';

// Re-export entity type for convenience
export type { OrgGrant };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateOrgGrantMutationDocument = `
mutation UpdateOrgGrantMutation($input: UpdateOrgGrantInput!) {
  updateOrgGrant(input: $input) {
    orgGrant {
      id
      permissions
      isGrant
      actorId
      entityId
      grantorId
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a OrgGrant - all fields optional */
interface OrgGrantPatch {
  permissions?: string | null;
  isGrant?: boolean | null;
  actorId?: string | null;
  entityId?: string | null;
  grantorId?: string | null;
  createdAt?: string | null;
  updatedAt?: string | null;
}

export interface UpdateOrgGrantMutationVariables {
  input: {
    id: string;
    patch: OrgGrantPatch;
  };
}

export interface UpdateOrgGrantMutationResult {
  updateOrgGrant: {
    orgGrant: OrgGrant;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a OrgGrant
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateOrgGrantMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateOrgGrantMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateOrgGrantMutationResult,
      Error,
      UpdateOrgGrantMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateOrgGrantMutationVariables) =>
      execute<UpdateOrgGrantMutationResult, UpdateOrgGrantMutationVariables>(
        updateOrgGrantMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['orggrant', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['orggrant', 'list'] });
    },
    ...options,
  });
}
