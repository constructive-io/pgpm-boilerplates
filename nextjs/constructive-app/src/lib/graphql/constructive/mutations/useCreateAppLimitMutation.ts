/**
 * Create mutation hook for AppLimit
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type AppLimit } from '../types';

// Re-export entity type for convenience
export type { AppLimit };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createAppLimitMutationDocument = `
mutation CreateAppLimitMutation($input: CreateAppLimitInput!) {
  createAppLimit(input: $input) {
    appLimit {
      id
      name
      actorId
      num
      max
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a AppLimit */
interface AppLimitCreateInput {
  name?: string | null;
  actorId?: string | null;
  num?: number | null;
  max?: number | null;
}

export interface CreateAppLimitMutationVariables {
  input: {
    appLimit: AppLimitCreateInput;
  };
}

export interface CreateAppLimitMutationResult {
  createAppLimit: {
    appLimit: AppLimit;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a AppLimit
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateAppLimitMutation();
 *
 * mutate({
 *   input: {
 *     appLimit: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateAppLimitMutation(
  options?: Omit<
    UseMutationOptions<
      CreateAppLimitMutationResult,
      Error,
      CreateAppLimitMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateAppLimitMutationVariables) =>
      execute<CreateAppLimitMutationResult, CreateAppLimitMutationVariables>(
        createAppLimitMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['applimit', 'list'] });
    },
    ...options,
  });
}
