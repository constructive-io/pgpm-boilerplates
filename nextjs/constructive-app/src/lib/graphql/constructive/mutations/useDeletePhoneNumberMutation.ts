/**
 * Delete mutation hook for PhoneNumber
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deletePhoneNumberMutationDocument = `
mutation DeletePhoneNumberMutation($input: DeletePhoneNumberInput!) {
  deletePhoneNumber(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeletePhoneNumberMutationVariables {
  input: {
    id: string;
  };
}

export interface DeletePhoneNumberMutationResult {
  deletePhoneNumber: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a PhoneNumber
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeletePhoneNumberMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeletePhoneNumberMutation(
  options?: Omit<
    UseMutationOptions<
      DeletePhoneNumberMutationResult,
      Error,
      DeletePhoneNumberMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeletePhoneNumberMutationVariables) =>
      execute<
        DeletePhoneNumberMutationResult,
        DeletePhoneNumberMutationVariables
      >(deletePhoneNumberMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['phonenumber', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['phonenumber', 'list'] });
    },
    ...options,
  });
}
