/**
 * Update mutation hook for DatabaseExtension
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type DatabaseExtension } from '../types';

// Re-export entity type for convenience
export type { DatabaseExtension };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateDatabaseExtensionMutationDocument = `
mutation UpdateDatabaseExtensionMutation($input: UpdateDatabaseExtensionInput!) {
  updateDatabaseExtension(input: $input) {
    databaseExtension {
      name
      databaseId
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a DatabaseExtension - all fields optional */
interface DatabaseExtensionPatch {
  name?: string | null;
  databaseId?: string | null;
}

export interface UpdateDatabaseExtensionMutationVariables {
  input: {
    id: string;
    patch: DatabaseExtensionPatch;
  };
}

export interface UpdateDatabaseExtensionMutationResult {
  updateDatabaseExtension: {
    databaseExtension: DatabaseExtension;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a DatabaseExtension
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateDatabaseExtensionMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateDatabaseExtensionMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateDatabaseExtensionMutationResult,
      Error,
      UpdateDatabaseExtensionMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateDatabaseExtensionMutationVariables) =>
      execute<
        UpdateDatabaseExtensionMutationResult,
        UpdateDatabaseExtensionMutationVariables
      >(updateDatabaseExtensionMutationDocument, variables),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['databaseextension', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({
        queryKey: ['databaseextension', 'list'],
      });
    },
    ...options,
  });
}
