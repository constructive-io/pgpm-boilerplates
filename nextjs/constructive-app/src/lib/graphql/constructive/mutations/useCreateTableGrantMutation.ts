/**
 * Create mutation hook for TableGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type TableGrant } from '../types';

// Re-export entity type for convenience
export type { TableGrant };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createTableGrantMutationDocument = `
mutation CreateTableGrantMutation($input: CreateTableGrantInput!) {
  createTableGrant(input: $input) {
    tableGrant {
      id
      databaseId
      tableId
      privilege
      roleName
      fieldIds
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a TableGrant */
interface TableGrantCreateInput {
  databaseId?: string | null;
  tableId?: string | null;
  privilege?: string | null;
  roleName?: string | null;
  fieldIds?: string[] | null;
}

export interface CreateTableGrantMutationVariables {
  input: {
    tableGrant: TableGrantCreateInput;
  };
}

export interface CreateTableGrantMutationResult {
  createTableGrant: {
    tableGrant: TableGrant;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a TableGrant
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateTableGrantMutation();
 *
 * mutate({
 *   input: {
 *     tableGrant: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateTableGrantMutation(
  options?: Omit<
    UseMutationOptions<
      CreateTableGrantMutationResult,
      Error,
      CreateTableGrantMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateTableGrantMutationVariables) =>
      execute<
        CreateTableGrantMutationResult,
        CreateTableGrantMutationVariables
      >(createTableGrantMutationDocument, variables),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['tablegrant', 'list'] });
    },
    ...options,
  });
}
