/**
 * Create mutation hook for Policy
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Policy } from '../types';
import { type ObjectCategory } from '../schema-types';

// Re-export entity type for convenience
export type { Policy };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createPolicyMutationDocument = `
mutation CreatePolicyMutation($input: CreatePolicyInput!) {
  createPolicy(input: $input) {
    policy {
      id
      databaseId
      tableId
      name
      roleName
      privilege
      permissive
      disabled
      template
      data
      smartTags
      category
      module
      scope
      tags
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a Policy */
interface PolicyCreateInput {
  databaseId?: string | null;
  tableId?: string | null;
  name?: string | null;
  roleName?: string | null;
  privilege?: string | null;
  permissive?: boolean | null;
  disabled?: boolean | null;
  template?: string | null;
  data?: unknown | null;
  smartTags?: unknown | null;
  category?: ObjectCategory | null;
  module?: string | null;
  scope?: number | null;
  tags?: string[] | null;
}

export interface CreatePolicyMutationVariables {
  input: {
    policy: PolicyCreateInput;
  };
}

export interface CreatePolicyMutationResult {
  createPolicy: {
    policy: Policy;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a Policy
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreatePolicyMutation();
 *
 * mutate({
 *   input: {
 *     policy: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreatePolicyMutation(
  options?: Omit<
    UseMutationOptions<
      CreatePolicyMutationResult,
      Error,
      CreatePolicyMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreatePolicyMutationVariables) =>
      execute<CreatePolicyMutationResult, CreatePolicyMutationVariables>(
        createPolicyMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['policy', 'list'] });
    },
    ...options,
  });
}
