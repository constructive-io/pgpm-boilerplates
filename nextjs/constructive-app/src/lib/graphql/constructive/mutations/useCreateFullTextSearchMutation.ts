/**
 * Create mutation hook for FullTextSearch
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type FullTextSearch } from '../types';

// Re-export entity type for convenience
export type { FullTextSearch };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createFullTextSearchMutationDocument = `
mutation CreateFullTextSearchMutation($input: CreateFullTextSearchInput!) {
  createFullTextSearch(input: $input) {
    fullTextSearch {
      id
      databaseId
      tableId
      fieldId
      fieldIds
      weights
      langs
      createdAt
      updatedAt
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a FullTextSearch */
interface FullTextSearchCreateInput {
  databaseId?: string | null;
  tableId?: string | null;
  fieldId?: string | null;
  fieldIds?: string[] | null;
  weights?: string[] | null;
  langs?: string[] | null;
}

export interface CreateFullTextSearchMutationVariables {
  input: {
    fullTextSearch: FullTextSearchCreateInput;
  };
}

export interface CreateFullTextSearchMutationResult {
  createFullTextSearch: {
    fullTextSearch: FullTextSearch;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a FullTextSearch
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateFullTextSearchMutation();
 *
 * mutate({
 *   input: {
 *     fullTextSearch: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateFullTextSearchMutation(
  options?: Omit<
    UseMutationOptions<
      CreateFullTextSearchMutationResult,
      Error,
      CreateFullTextSearchMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateFullTextSearchMutationVariables) =>
      execute<
        CreateFullTextSearchMutationResult,
        CreateFullTextSearchMutationVariables
      >(createFullTextSearchMutationDocument, variables),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['fulltextsearch', 'list'] });
    },
    ...options,
  });
}
