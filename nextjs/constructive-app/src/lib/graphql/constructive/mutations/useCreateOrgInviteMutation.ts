/**
 * Create mutation hook for OrgInvite
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type OrgInvite } from '../types';

// Re-export entity type for convenience
export type { OrgInvite };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createOrgInviteMutationDocument = `
mutation CreateOrgInviteMutation($input: CreateOrgInviteInput!) {
  createOrgInvite(input: $input) {
    orgInvite {
      id
      email
      senderId
      receiverId
      inviteToken
      inviteValid
      inviteLimit
      inviteCount
      multiple
      data
      expiresAt
      createdAt
      updatedAt
      entityId
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a OrgInvite */
interface OrgInviteCreateInput {
  email?: string | null;
  senderId?: string | null;
  receiverId?: string | null;
  inviteToken?: string | null;
  inviteValid?: boolean | null;
  inviteLimit?: number | null;
  inviteCount?: number | null;
  multiple?: boolean | null;
  data?: unknown | null;
  expiresAt?: string | null;
  entityId?: string | null;
}

export interface CreateOrgInviteMutationVariables {
  input: {
    orgInvite: OrgInviteCreateInput;
  };
}

export interface CreateOrgInviteMutationResult {
  createOrgInvite: {
    orgInvite: OrgInvite;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a OrgInvite
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgInviteMutation();
 *
 * mutate({
 *   input: {
 *     orgInvite: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateOrgInviteMutation(
  options?: Omit<
    UseMutationOptions<
      CreateOrgInviteMutationResult,
      Error,
      CreateOrgInviteMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateOrgInviteMutationVariables) =>
      execute<CreateOrgInviteMutationResult, CreateOrgInviteMutationVariables>(
        createOrgInviteMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['orginvite', 'list'] });
    },
    ...options,
  });
}
