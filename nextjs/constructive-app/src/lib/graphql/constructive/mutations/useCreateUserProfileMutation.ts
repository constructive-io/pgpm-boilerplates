/**
 * Create mutation hook for UserProfile
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type UserProfile } from '../types';

// Re-export entity type for convenience
export type { UserProfile };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createUserProfileMutationDocument = `
mutation CreateUserProfileMutation($input: CreateUserProfileInput!) {
  createUserProfile(input: $input) {
    userProfile {
      id
      ownerId
      profilePicture
      bio
      firstName
      lastName
      tags
      desired
      createdAt
      updatedAt
      createdBy
      updatedBy
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a UserProfile */
interface UserProfileCreateInput {
  ownerId?: string | null;
  profilePicture?: unknown | null;
  bio?: string | null;
  firstName?: string | null;
  lastName?: string | null;
  tags?: string[] | null;
  desired?: string[] | null;
  createdBy?: string | null;
  updatedBy?: string | null;
}

export interface CreateUserProfileMutationVariables {
  input: {
    userProfile: UserProfileCreateInput;
  };
}

export interface CreateUserProfileMutationResult {
  createUserProfile: {
    userProfile: UserProfile;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a UserProfile
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateUserProfileMutation();
 *
 * mutate({
 *   input: {
 *     userProfile: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateUserProfileMutation(
  options?: Omit<
    UseMutationOptions<
      CreateUserProfileMutationResult,
      Error,
      CreateUserProfileMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateUserProfileMutationVariables) =>
      execute<
        CreateUserProfileMutationResult,
        CreateUserProfileMutationVariables
      >(createUserProfileMutationDocument, variables),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['userprofile', 'list'] });
    },
    ...options,
  });
}
