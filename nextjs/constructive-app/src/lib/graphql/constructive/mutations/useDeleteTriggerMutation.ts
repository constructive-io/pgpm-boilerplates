/**
 * Delete mutation hook for Trigger
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deleteTriggerMutationDocument = `
mutation DeleteTriggerMutation($input: DeleteTriggerInput!) {
  deleteTrigger(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeleteTriggerMutationVariables {
  input: {
    id: string;
  };
}

export interface DeleteTriggerMutationResult {
  deleteTrigger: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a Trigger
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteTriggerMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeleteTriggerMutation(
  options?: Omit<
    UseMutationOptions<
      DeleteTriggerMutationResult,
      Error,
      DeleteTriggerMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeleteTriggerMutationVariables) =>
      execute<DeleteTriggerMutationResult, DeleteTriggerMutationVariables>(
        deleteTriggerMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['trigger', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['trigger', 'list'] });
    },
    ...options,
  });
}
