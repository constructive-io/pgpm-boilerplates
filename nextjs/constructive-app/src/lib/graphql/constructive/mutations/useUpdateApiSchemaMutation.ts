/**
 * Update mutation hook for ApiSchema
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type ApiSchema } from '../types';

// Re-export entity type for convenience
export type { ApiSchema };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateApiSchemaMutationDocument = `
mutation UpdateApiSchemaMutation($input: UpdateApiSchemaInput!) {
  updateApiSchema(input: $input) {
    apiSchema {
      id
      databaseId
      schemaId
      apiId
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a ApiSchema - all fields optional */
interface ApiSchemaPatch {
  databaseId?: string | null;
  schemaId?: string | null;
  apiId?: string | null;
}

export interface UpdateApiSchemaMutationVariables {
  input: {
    id: string;
    patch: ApiSchemaPatch;
  };
}

export interface UpdateApiSchemaMutationResult {
  updateApiSchema: {
    apiSchema: ApiSchema;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a ApiSchema
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateApiSchemaMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateApiSchemaMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateApiSchemaMutationResult,
      Error,
      UpdateApiSchemaMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateApiSchemaMutationVariables) =>
      execute<UpdateApiSchemaMutationResult, UpdateApiSchemaMutationVariables>(
        updateApiSchemaMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['apischema', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['apischema', 'list'] });
    },
    ...options,
  });
}
