/**
 * Update mutation hook for Domain
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type Domain } from '../types';

// Re-export entity type for convenience
export type { Domain };

// ============================================================================
// GraphQL Document
// ============================================================================
export const updateDomainMutationDocument = `
mutation UpdateDomainMutation($input: UpdateDomainInput!) {
  updateDomain(input: $input) {
    domain {
      id
      databaseId
      apiId
      siteId
      subdomain
      domain
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Patch type for updating a Domain - all fields optional */
interface DomainPatch {
  databaseId?: string | null;
  apiId?: string | null;
  siteId?: string | null;
  subdomain?: string | null;
  domain?: string | null;
}

export interface UpdateDomainMutationVariables {
  input: {
    id: string;
    patch: DomainPatch;
  };
}

export interface UpdateDomainMutationResult {
  updateDomain: {
    domain: Domain;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for updating a Domain
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateDomainMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-here',
 *     patch: {
 *       // ... fields to update
 *     },
 *   },
 * });
 * ```
 */
export function useUpdateDomainMutation(
  options?: Omit<
    UseMutationOptions<
      UpdateDomainMutationResult,
      Error,
      UpdateDomainMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: UpdateDomainMutationVariables) =>
      execute<UpdateDomainMutationResult, UpdateDomainMutationVariables>(
        updateDomainMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Invalidate specific item and list queries
      queryClient.invalidateQueries({
        queryKey: ['domain', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['domain', 'list'] });
    },
    ...options,
  });
}
