/**
 * Create mutation hook for ApiSchema
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';
import { type ApiSchema } from '../types';

// Re-export entity type for convenience
export type { ApiSchema };

// ============================================================================
// GraphQL Document
// ============================================================================
export const createApiSchemaMutationDocument = `
mutation CreateApiSchemaMutation($input: CreateApiSchemaInput!) {
  createApiSchema(input: $input) {
    apiSchema {
      id
      databaseId
      schemaId
      apiId
    }
  }
}
`;

// ============================================================================
// Types
// ============================================================================
/** Input type for creating a ApiSchema */
interface ApiSchemaCreateInput {
  databaseId?: string | null;
  schemaId?: string | null;
  apiId?: string | null;
}

export interface CreateApiSchemaMutationVariables {
  input: {
    apiSchema: ApiSchemaCreateInput;
  };
}

export interface CreateApiSchemaMutationResult {
  createApiSchema: {
    apiSchema: ApiSchema;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for creating a ApiSchema
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateApiSchemaMutation();
 *
 * mutate({
 *   input: {
 *     apiSchema: {
 *       // ... fields
 *     },
 *   },
 * });
 * ```
 */
export function useCreateApiSchemaMutation(
  options?: Omit<
    UseMutationOptions<
      CreateApiSchemaMutationResult,
      Error,
      CreateApiSchemaMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: CreateApiSchemaMutationVariables) =>
      execute<CreateApiSchemaMutationResult, CreateApiSchemaMutationVariables>(
        createApiSchemaMutationDocument,
        variables
      ),
    onSuccess: () => {
      // Invalidate list queries to refetch
      queryClient.invalidateQueries({ queryKey: ['apischema', 'list'] });
    },
    ...options,
  });
}
