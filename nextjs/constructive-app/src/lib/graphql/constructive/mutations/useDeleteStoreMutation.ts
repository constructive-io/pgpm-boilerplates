/**
 * Delete mutation hook for Store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import {
  useMutation,
  useQueryClient,
  type UseMutationOptions,
} from '@tanstack/react-query';
import { execute } from '../client';

// ============================================================================
// GraphQL Document
// ============================================================================
export const deleteStoreMutationDocument = `
mutation DeleteStoreMutation($input: DeleteStoreInput!) {
  deleteStore(input: $input) {
    clientMutationId
    deletedId
  }
}
`;

// ============================================================================
// Types
// ============================================================================
export interface DeleteStoreMutationVariables {
  input: {
    id: string;
  };
}

export interface DeleteStoreMutationResult {
  deleteStore: {
    clientMutationId: string | null;
    deletedId: string | null;
  };
}

// ============================================================================
// Hook
// ============================================================================
/**
 * Mutation hook for deleting a Store
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteStoreMutation();
 *
 * mutate({
 *   input: {
 *     id: 'value-to-delete',
 *   },
 * });
 * ```
 */
export function useDeleteStoreMutation(
  options?: Omit<
    UseMutationOptions<
      DeleteStoreMutationResult,
      Error,
      DeleteStoreMutationVariables
    >,
    'mutationFn'
  >
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (variables: DeleteStoreMutationVariables) =>
      execute<DeleteStoreMutationResult, DeleteStoreMutationVariables>(
        deleteStoreMutationDocument,
        variables
      ),
    onSuccess: (_, variables) => {
      // Remove from cache and invalidate list
      queryClient.removeQueries({
        queryKey: ['store', 'detail', variables.input.id],
      });
      queryClient.invalidateQueries({ queryKey: ['store', 'list'] });
    },
    ...options,
  });
}
